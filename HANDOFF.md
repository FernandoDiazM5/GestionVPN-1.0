# 📦 Handoff — MikroTikVPN Remote Manager (`GestionVPN-1.0`)

> **Contexto DURABLE y vigente.** Léelo al iniciar cualquier sesión nueva.
> La narrativa cronológica por sesión vive en [`HANDOFF_LOG.md`](./HANDOFF_LOG.md) (append-only).
> Mantenimiento gobernado por la skill **`handoff-keeper`** (`.claude/skills/handoff-keeper/`).
> Rama de trabajo: **`vps_uni`** · Remote: `github.com/FernandoDiazM5/GestionVPN-1.0`. La instalación aislada más reciente está en `165.22.187.115`; `143.244.169.142` permanece intacta como producción/rollback y no se cambió DNS.
> La rama **`vps_prod`** corresponde a otro VPS y no debe desplegarse en este servidor.
> La **Plataforma Central** pertenece a otra rama: no documentar aquí su estado, despliegues ni pendientes.

---

## 0) Estado actual

- **Asistente de configuración del Core retirado de la versión estable (2026-09-01; runtime `4cbe51e`, rama `codex/d006-no-core-assistant`):** hotfix basado exactamente en `d006f3c`. Se eliminó de la UI el asistente de cuatro pasos, preparación/reemplazo de Core, previsualización de supernet e historial asociado; se retiraron del cliente sus tipos/llamadas y del backend las rutas `/provision-preview`, `/provision-history` y `/provision`. Permanecen estado del Core, health y respaldos. Pruebas focalizadas 3/3, TypeScript/build, lint y backend 6/6 verdes. Producción confirma texto ausente del bundle y rutas ausentes del contenedor; raíz/health 200, DB/backend healthy, cero reinicios, Telegram long-polling activo y WireGuard con handshake/tráfico. Se creó dump e imágenes `pre-remove-core-assistant-*` antes del deploy.

- **Integraciones recuperadas desde `.env` (2026-09-01; VPS `165.22.187.115`):** Telegram fue importado mediante el migrador oficial, validado, cifrado en `platform_integrations` y activado como `@mivpnmanager_bot`; `TELEGRAM_BOT_ENABLED=true` y el backend confirma long-polling iniciado. Gmail fue detectado pero no guardado porque la validación SMTP falla con `ESOCKET`: desde la Gota, `smtp.gmail.com` 587/465 está bloqueado o agota timeout; `smtp-relay.brevo.com:2525` sí es alcanzable. El `.env` no contiene Brevo ni Gemini. Firebase tiene valores web, pero falta un Service Account JSON válido, por lo que no se activó parcialmente. Archivos temporales de secretos eliminados; backend healthy y WireGuard conserva handshake.

- **Dominio y TLS válidos en la VPS nueva (2026-09-01):** `joinpoint.cloud` y `www.joinpoint.cloud` resuelven a `165.22.187.115`. Se reemplazó el certificado autofirmado por Let's Encrypt para ambos nombres, vigente hasta 2026-12-01; HTTPS valida sin errores, raíz 200, `www` redirige 301 al dominio canónico y `/api/health` responde correctamente. `APP_BASE_URL` quedó en `https://joinpoint.cloud/`. `certbot.timer` está activo y se instalaron hooks que detienen brevemente el frontend, copian el certificado renovado al bind mount y lo reinician. DB/backend healthy, cero reinicios y WireGuard conservó handshake/tráfico.

- **VPS limpia aislada desplegada y WireGuard enlazado (2026-09-01; `165.22.187.115`, runtime base `d006f3c`):** Ubuntu 24.04 con Docker/Compose, UFW, fail2ban, WireGuard tools y swap de 2 GiB. Se creó MariaDB nueva, secretos nuevos, TLS autofirmado y los tres contenedores; raíz HTTPS 200, `/api/health` correcto, setup inicial `needsSetup=true`, DB/backend healthy y cero reinicios. Nginx recibió un ajuste mínimo `default_server` para servir correctamente por IP; UFW permite 3001/TCP sólo desde la red Docker `172.18.0.0/16`. El usuario confirmó que no existe otra VPS activa, por lo que WireGuard usa la IP histórica `10.12.250.60/32`; endpoint `213.173.36.232:13232`, peer Core `R79mbZb0bRcu5+NL40ZAMiX5VtjWJ9JJ+AqltA15uQM=` y `AllowedIPs=10.12.248.0/22,10.11.250.0/24,10.11.251.0/24`. La clave privada mostrada accidentalmente fue rotada; nueva clave pública VPS `TFMMD351Nk5QYbAQlV/+LZX5f1oHjjb4SfT977uyczk=`. Enlace verificado: handshake reciente, tráfico bidireccional, ping `10.12.250.1` 4/4 con ~87 ms, API 8728/TCP, Winbox 8291/TCP y SSH 22/TCP accesibles por túnel; 8729/TCP cerrado/filtrado. El DNS posteriormente fue migrado a esta VPS y TLS oficial quedó activo.

- **Puertos requeridos visibles en el asistente (2026-09-01; runtime `011752d`):** el paso 1 muestra antes de enlazar una tabla de puertos libres/reenvíos: `8728/TCP` temporal y restringido a la VPS para API RouterOS; `13232–13234/UDP` permanentes para WireGuard VPS/clientes/admin; puerto SSTP configurado (por defecto `4443/TCP`) permanente sólo si se usa SSTP. Advierte no exponer Winbox `8291` públicamente. Se recreó sólo frontend; raíz/health 200, DB/backend healthy y cero reinicios. Rollback frontend `gestionvpn-10-frontend:pre-required-ports-20260902T015500Z`.

- **Paso inicial simplificado (2026-09-01; runtime `ce6f058`):** el asistente ya no solicita la IP privada del MikroTik porque Joinpoint nunca la usa como endpoint. Sólo pide IP pública/dominio, credenciales RouterOS y redes locales autorizadas. La ayuda aclara que la IP privada se utiliza exclusivamente en el router del proveedor para apuntar el reenvío NAT temporal de `8728/TCP`. Se reconstruyó/recreó sólo frontend; raíz/health 200, DB/backend healthy, cero reinicios y `wg0` preservada. Rollback frontend `gestionvpn-10-frontend:pre-private-ip-removal-20260902T014600Z`.

- **Falso 409 del alta de Core corregido y desplegado (2026-09-01; runtime `9880f87`):** al guardar por primera vez `10.12.248.0/22`, el validador confundía la propia `wg0` administrada (`10.12.250.60/32`) con un solapamiento externo porque `management_supernet` aún no existía en la DB. Ahora excluye únicamente esa combinación exacta y continúa bloqueando cualquier otra dirección/interfaz superpuesta. El guardado propaga fallos reales, el progreso no salta etapas por la clave antigua del VPS si el nuevo Core no responde y el error distingue credenciales de conectividad. Producción confirma preview `canSave=true`, raíz/health 200, DB/backend healthy, cero reinicios y `wg0` preservada. Diagnóstico externo vigente: `38.253.171.61` responde ICMP, pero TCP 8728/8729/8291/22 no es alcanzable desde la VPS; se debe reenviar temporalmente API 8728 o 8729 hacia `192.168.18.2` y limitar el origen a la VPS. Backup `/root/backups/pre-wg-connect-fix-20260901T051200Z/vpn_manager.sql.gz`.

- **Rediseño seguro del asistente MikroTik↔VPS desplegado (2026-08-31; runtime `2794fd3` en `143.244.169.142`):** Administración usa seis vistas consecutivas con flechas: datos del servidor, preparación del MikroTik, activación VPS, intercambio de claves, validación y cierre público. Solicita endpoint público (admite NAT 1:1, como `38.25.114.72` hacia `192.168.18.2`), credenciales y redes locales editables; obtiene automáticamente clave/puerto WireGuard del Core y reutiliza la clave pública del agente VPS. El cierre final sólo se habilita con gestión por el túnel, peer administrado, handshake <=120 s, `LIST-WAN` y miembro WAN; restringe Winbox/API/SSH/web a LAN autorizada+VPN y confirma reconexión por túnel. Invariante: conservar NAT, redirecciones, WAN, ruta por defecto, DNS y acceso LAN a Internet; la limpieza sólo puede afectar el dominio VPN/administración gestionado por Joinpoint. Producción: raíz/health 200, CORS 204/403, ruta protegida 401, DB/backend healthy, cero reinicios y sin errores recientes. `wg0` preservó `10.12.250.60/32`, ruta `10.12.248.0/22`, peer y handshake reciente; no se ejecutó ninguna mutación RouterOS/WireGuard. Backup `/root/backups/pre-wg-router-wizard-20260901T042500Z/vpn_manager.sql.gz`; rollback `gestionvpn-10-{backend|frontend}:pre-wg-router-wizard-20260901T042500Z`. Verificación local: 22 pruebas backend focalizadas, 6 frontend, TypeScript, lint, inventario de rutas, build y `check:all` verdes.

- **Despliegue reconciliado con Compose exclusivamente productivo (2026-08-31; nueva Gota `143.244.169.142`, checkout `f3810eb`):** se detectó que backend y frontend activos conservaban etiquetas de una creación accidental con `docker-compose.yml` + `docker-compose.prod.yml`, patrón capaz de reintroducir CORS local y errores 500 de login. Se etiquetaron ambos rollbacks como `pre-prod-only-20260831T183947Z`, se reconstruyeron y recrearon sólo backend/frontend con `docker compose -f docker-compose.prod.yml`; MariaDB no se recreó. Los tres contenedores declaran ahora únicamente el compose productivo, 8080 dejó de estar publicado, backend/DB están healthy y todos tienen 0 reinicios. Validación: raíz y health 200, preflight canónico 204, origen hostil 403, credenciales inválidas 401 `BAD_CREDENTIALS`, sin errores recientes; `wg0` permanece presente en `10.12.250.60/32`. Disco 38 %, 30 GiB libres.

- **Grupos operativos, creación masiva y rutas de fibra desplegados (2026-08-31; nueva Gota `143.244.169.142`, runtime `a8bcf4f`):** el menú y la vista pasan de “Historial de clientes” a “Grupos operativos”. Cada grupo declara perfil `CLIENT_TRACKING`, `FIBER_ROUTES` o `GENERAL` y capacidades; la vinculación conserva OWNER + bot exacto y muestra permisos faltantes. La cola de clientes es secuencial, pausable/reanudable e idempotente; rutas de fibra agrega recorrido, fusiones, mediciones, evidencias y eventos auditables. La nueva MariaDB inicia vacía y el backend confirmó migraciones completas, incluyendo las 17 tablas del dominio MikroWisp/Telegram. Backend y frontend están healthy; `joinpoint.cloud`, `www.joinpoint.cloud` y `/api/health` responden 200. DNS ya apunta a la nueva IP y Let’s Encrypt fue emitido con renovación automática hasta 2026-11-29. Telegram y credenciales MikroWisp quedaron desactivados hasta configurarlos desde la web.

- **Runbook de recuperación VPS creado (2026-08-31):** `docs/implementation/PLAN_RECUPERACION_DESPLIEGUE_VPS_NUEVO.md` documenta requisitos, secretos, despliegue por etapas, validaciones, TLS/DNS, rollback y diagnóstico. En la nueva Gota se actualizó `APP_BASE_URL` a `https://joinpoint.cloud/` y se instaló un hook de renovación Let’s Encrypt que copia certificados y recrea sólo el frontend. Backend volvió a healthy después de la recreación; `/api/health` HTTPS responde 200. El administrador y las integraciones siguen pendientes de alta desde la web, sin guardar credenciales en el repositorio.

- **Plan de configuración WireGuard desde web creado (2026-08-31):** `docs/implementation/PLAN_CONFIGURACION_WIREGUARD_DESDE_WEB.md` define el asistente administrativo, parámetros, validación, agente privilegiado allowlist, respaldo, rollback, sincronización con el Core y criterios de aceptación. El sistema actual ya guarda la clave pública del VPS, pero el asistente completo de `wg0` y aplicación segura queda como fase futura; no se debe enviar shell arbitrario desde el navegador.

- **WireGuard web Fase 1 cerrada (2026-08-31; sólo lectura):** el VPS nuevo no tiene instalado `wg`, no existe `wg0`, `wg-quick@wg0` ni `/etc/wireguard/wg0.conf`, y no escucha UDP WireGuard. IPv4 forwarding está activo y UFW inactivo. La supernet prevista `10.12.248.0/22` no se solapa con las redes del host (`10.10/16`, `10.116/20`) ni Docker (`172.17/16`, `172.18/16`). Contrato inicial: `wg0`, MTU 1420, puerto candidato 13232/UDP y peer histórico `10.12.250.60/32`, ambos pendientes de contraste con el Core antes de aplicar. Próxima fase: diagnóstico backend/UI estrictamente read-only.

- **WireGuard web Fase 2 desplegada (2026-08-31; runtime `01e8328`):** el status administrativo del Core incluye diagnóstico read-only del WireGuard del VPS con estados `ACTIVE|DEGRADED|NOT_CONFIGURED`, interfaz, direcciones, puerto, clave pública, rutas y disponibilidad de herramientas. Producción confirma `NOT_CONFIGURED`, herramientas disponibles, `wg0` ausente y ningún dato de túnel, como corresponde antes del aprovisionamiento. No lee `wg0.conf` ni claves privadas; backend permanece no-root. HTTPS/health 200, DB/backend healthy y cero reinicios. Backup `/root/backups/pre-wireguard-readonly-20260831T134158Z/vpn_manager.sql.gz`; rollback `gestionvpn-10-{backend|frontend}:pre-wireguard-readonly-20260831T134158Z`. Próxima fase: previsualización y validación, aún sin aplicar.

- **WireGuard web Fase 3 desplegada (2026-08-31; runtime `7d7ce58`):** Administración permite previsualizar `wg0`, VPS `10.12.250.60/32`, Core `213.173.36.232:13232`, MTU, clave pública, AllowedIPs y keepalive. El backend valida el contrato, bloquea default route, IP fuera del segmento, ausencia de `10.12.248.0/22` y conflictos con host/Docker. Precisión: 13232/UDP es el puerto del Core, no el puerto local del VPS.

- **WireGuard web Fases 4–6 desplegadas (2026-08-31; runtime base `7d7ce58`, hotfix `8a987a6`):** agente host allowlist recibe intención JSON atómica del backend no-root, genera/conserva la clave privada sólo en `/etc/wireguard`, respalda antes de `APPLY|ROTATE`, restaura automáticamente ante fallo y admite rollback explícito. La web exige frases de confirmación y muestra resultado no secreto. La integración Core valida clave/puerto de `VPN-WG-VPS` y crea/actualiza sólo `GVPN:VPS`, preservando otros peers. Solicitudes y sync quedan auditados. El usuario aplicó la configuración y `wg0` existe con `10.12.250.60/32`. Hotfix `8a987a6` evita que la previsualización confunda la propia `wg0` con un solapamiento del host; las demás interfaces siguen bloqueando conflictos reales. La AllowedIP vigente debe ser exactamente `10.12.248.0/22`; `10.12.148.0/22` es válida como CIDR pero pertenece a otro bloque y se rechaza. Rollback del hotfix: `gestionvpn-10-backend:pre-wg-overlap-fix-20260831T163008Z`.
- **WireGuard audit hotfix desplegado (2026-08-31; backend `5210630`):** la aplicación sí se completaba en el agente, pero la API devolvía 500 al insertar `outcome=QUEUED` en un enum que sólo admite `SUCCESS|FAILED`. APPLY, ROTATE y ROLLBACK registran ahora la solicitud aceptada como `SUCCESS`. Producción conserva `wg0` en `10.12.250.60/32`, agente activo, DB/backend healthy y cero reinicios. Rollback `gestionvpn-10-backend:pre-wg-audit-fix-20260831T170210Z`. Revisar antes del peer Core: el último formulario usó puerto `13234`, mientras `VPN-WG-VPS` está planificado en `13232`.
- **Flujo guiado WireGuard VPS↔Core desplegado (2026-08-31; runtime `8cffe17` en `143.244.169.142`):** Administración presenta una secuencia única conectar Core → inspeccionar interfaz/clave/puerto reales → aplicar VPS con polling del agente → mostrar/copiar clave pública → sincronizar sólo `GVPN:VPS` → verificar peer, handshake y rutas. El status reutiliza `wireguardAgent.publicKey` cuando el backend no-root no puede obtenerla mediante `wg show`, recupera el borrador persistido y marca el VPS activo si interfaz, dirección y clave confiable están presentes. La inspección autoritativa rellena clave/puerto del Core, evitando conservar `13234` si `VPN-WG-VPS` escucha en `13232`; si faltan credenciales aparece CTA directo. Producción: backend/DB healthy, HTTPS/health 200, CORS 204/403, login inválido 401, rutas administrativas protegidas 401, cero reinicios y logs recientes sin errores. `wg0` conserva `10.12.250.60/32`, un peer y handshake reciente; no se aplicó ni sincronizó WireGuard durante el deploy. Backup `/root/backups/pre-wg-guided-20260901T031227Z/vpn_manager.sql.gz`; rollback `gestionvpn-10-{backend|frontend}:pre-wg-guided-20260901T031227Z`. Verificación local: backend **140/776**, frontend **83/277**, focalizadas **3/3 + 6/6**, TypeScript, lint, inventario, build y `check:all` verdes. Pendiente humano: revisión visual autenticada del wizard y, tras configurar credenciales Core, validación guiada del peer sin alterar otros peers.

- **Historial Telegram y autorización de Sitios desplegados (2026-08-29; runtime/checkout `d006f3c`):** producción incorpora el módulo lateral Historial de clientes, sólo `GetRouters`, sincronización y recuperación de escenarios externos de grupos/temas/participantes, y autorización Joinpoint estricta para todos los comandos y callbacks de Sitios. Antes de desplegar se creó y verificó el respaldo `/root/backups/pre-telegram-history-20260829T163621Z`; se conservaron los rollbacks `gestionvpn-10-{backend|frontend}:pre-telegram-history-20260829T163621Z`. Backend se recreó primero y alcanzó estado healthy; luego se recreó frontend; el despliegue terminó con `/api/health` correcto y `DEPLOY_OK`, y el usuario confirmó visualmente el funcionamiento. Esta entrada reemplaza el estado “pendiente deploy” de las cinco entregas inmediatamente siguientes. La comprobación SSH posterior quedó pendiente porque dos conexiones consecutivas agotaron el tiempo de espera; no confundir esa limitación de administración con un fallo funcional confirmado.

- **Autorización Joinpoint para comandos de Sitios endurecida (2026-08-29; local, pendiente deploy):** `/estado`, `/misitio`, `/sitios`, `/activar`, `/desactivar`, selección numérica y callbacks revalidan en cada ejecución chat privado, vínculo bot/chat, usuario no suspendido/eliminado, workspace exacto activo y membresía vigente. MEMBER sólo ve asignaciones actuales; antes de activar se vuelve a consultar la visibilidad, así que botones/listas antiguos no sirven si se retiró el sitio. `/link` queda prohibido en grupos. Retirar una asignación desde el panel cierra inmediatamente la sesión activa coincidente y limpia mangles best-effort. Invariante: Telegram identifica, Joinpoint autoriza; nunca confiar en pertenencia a grupo, callback o selección almacenada. Verificación: backend **135/763**, frontend **82/274**, focalizadas **47/47**, TypeScript, lint, inventario de rutas y `check:all` verdes. Pendiente canary real y deploy.

- **Escenarios externos Telegram completados (2026-08-29; local, pendiente deploy):** temas creados directamente en Telegram se descubren como `UNREGISTERED`, aparecen en Historial y `/registrartema ID_CLIENTE` renombra el tema y convierte el mismo registro a `ACTIVE`; una protección temporal evita duplicarlos con temas creados por Joinpoint. Usuarios del workspace añadidos directamente se marcan `PRESENT_UNAUTHORIZED` y nunca acceden a consultas hasta pasar por el flujo controlado; la UI permite retirarlos. Invitaciones vencidas pasan a `INVITE_EXPIRED` y pueden regenerarse, mientras códigos de grupo vencidos se marcan `EXPIRED` y dejan de mostrarse. Verificación: backend **135/757**, frontend **82/274**, focalizadas **16/16 + 3/3**, build, TypeScript, lint, inventario de rutas y `check:all` verdes. Pendiente canary real y deploy.

- **Sincronización Telegram→Joinpoint implementada (2026-08-29; local, pendiente deploy):** Historial permite eliminar temas mediante Telegram conservando su fila como `DELETED`, y recrear estados `DELETED`/`DELETE_UNKNOWN`. “Verificar con Telegram” reconcilia título, foro, bot, permisos y prueba temas registrados; sólo respuestas inequívocas de tópico inexistente generan baja lógica, nunca timeout/falla ambigua. `forum_topic_edited` actualiza el nombre y el polling incluye `my_chat_member` para `BOT_REMOVED`/`MISSING_PERMISSIONS`; grupos con problemas mantienen historial visible pero pausan escrituras. Verificación: backend **135/753**, frontend **82/274**, focalizadas backend **12/12**, frontend **3/3**, build, TypeScript, lint, inventario de rutas y `check:all` verdes. Pendiente canary real y deploy.

- **UX completa de Historial de clientes implementada (2026-08-29; local, pendiente deploy):** módulo OWNER con encabezado y CTA, métricas de grupos/temas/participantes activos, buscador y maestro–detalle en escritorio, navegación lista→detalle en móvil, tabs Temas/Participantes, estados semánticos, menú contextual cerrar/reabrir/recrear, modal de alta de tema con búsqueda/vista previa, asistente de grupo en tres pasos y comandos plegables/copiables. Reutiliza Dialog accesible, controles ≥44 px y tokens existentes. Verificación completa: backend **135/750**, frontend **82/273**, focalizadas **3/3**, TypeScript, lint, build y `check:all` verdes.

- **Historial de clientes separado de Integraciones (2026-08-29; local, pendiente deploy):** MikroWisp conserva únicamente el catálogo `ROUTERS`/`GetRouters`; `GetMonitoreo` y `GetCajasNap` fueron retirados de la UI, tipos, servicio y validación de rutas. El moderador OWNER dispone del nuevo módulo lateral “Historial de clientes” (`/dm/:workspace/client-history`) con grupos, temas, participantes, alta de grupos y referencia de `/informacion`, `/servicios`, `/facturacion`, `/ayuda` y `/registrartema ID_CLIENTE`. MEMBER y plataforma no reciben el módulo. Verificación: backend **135/750**, frontend **82/273**, focalizadas **35/35**, TypeScript, lint, build y `check:all` verdes.

- **VPS limpiado y despliegue completo verificado (2026-08-29; checkout `a7f4021`):** se retiraron cinco rollbacks backend y cuatro frontend anteriores ya superados, caché APT y 152 MB de journal; se conservaron volúmenes, dumps, contenedores y el rollback `pre-mikrowisp-20260828T2152`. Disco 91%→74%, con 6.4 GiB libres. Backend y frontend se reconstruyeron con `docker-compose.prod.yml`; migración MikroWisp confirmó 9 tablas, frontend contiene la nueva jerarquía, HTTPS/health 200, MySQL/RouterOS/SMTP `ok`, CORS 204/403 y 0 reinicios.

- **Jerarquía visual de grupos y temas Telegram desplegada (2026-08-29; runtime/checkout `3fa3ed9`):** Integraciones lista primero “Mis grupos” como tarjetas y muestra los temas del grupo seleccionado antes de participantes y alta de tema; “Agregar nuevo grupo” quedó como flujo independiente al final, con guía plegable. Pruebas focalizadas **2/2**, TypeScript, lint, build y `check:all` verdes. En producción sólo se recreó el frontend; bundle confirmado, HTTPS y `/api/health` 200, backend/DB healthy y 0 reinicios. Health global `degraded` únicamente por RouterOS `stale`. El primer intento de exportar también una imagen backend agotó disco sin sustituir contenedores; se descartaron capas temporales y luego se activó exclusivamente el frontend. Disco final 91%/2.3 GiB libres; conservar los rollbacks MikroWisp y auditar imágenes antiguas antes de otro build.

- **MikroWisp + foros Telegram fases 1–5 implementadas y migración local verificada (2026-08-28; sin deploy):** MikroWisp sigue estrictamente read-only y sólo permite `GetClientsDetails` más `GetRouters`, `GetMonitoreo` y `GetCajasNap`. Allowlist cliente/servicio/facturación confirmada; PPP user/pass, comunidad SNMP, token y campos futuros se eliminan. Grupos, temas, participantes verificados y comandos `/informacion`, `/servicios`, `/facturacion`, `/ayuda` están implementados sin persistir respuestas. Fase 6 parcial: MariaDB/XAMPP local permitió detectar y corregir UUID/FK a `CHAR(36)` + `utf8mb4_unicode_ci`; nueve tablas y 17 FK se crearon correctamente y la inicialización fue idempotente. Verificación: backend **134/747**, frontend **81/271**, build, lint, TypeScript, inventario de **193 rutas** y `check:all` verdes; el token compartido por el usuario no aparece en archivos. Falta canary real, push y deploy. Se requiere bot de workspace + supergrupo foro + usuario piloto de laboratorio; no usar producción como canary.

- **Validaciones de canales y mejora visual de Integraciones desplegadas (2026-08-24; runtime `0921bb6`):** Email sólo puede activarse si el workspace tiene Brevo/Gmail `ACTIVE` y correo verificado; Telegram exige token `ACTIVE`, código confirmado contra el mismo bot y preferencia marcada. Retirar/reemplazar bot invalida la huella vinculada. Las integraciones aparecen contraídas, sólo una se despliega y cada formulario incluye pasos, portal oficial y advertencias para Brevo, Gmail, Telegram, Gemini y Firebase. Telegram distingue sin bot, falta vincular, espera, vinculado y activo; bots de workspace usan long-polling aislado y no admiten tokens duplicados/admin. El notifier no usa proveedores globales como fallback del moderador. Verificación local: backend **129/711**, frontend **79/266**, build/lint/`check:all` verdes. Producción: migración `notification_subscriptions.telegram_bot_fingerprint` presente, bundle guía confirmado, raíz/legacy/health 200, CORS 204/403, login inválido 401, MySQL/RouterOS/SMTP `ok`, tres contenedores con 0 reinicios y logs recientes sin errores. Backup restaurado en DB aislada con **58 tablas**: `/root/pre-notification-validation-20260825T043400Z`; rollback `gestionvpn-10-{backend|frontend}:pre-notification-validation-20260825T043400Z`; disco 80%/5.0 GiB libres.

- **Accesos Telegram por sitio desplegados (2026-08-25; runtime `8a40ff0`):** Notificaciones vincula Email con Brevo y Telegram con su integración; `/link` envía comandos y publica el menú nativo. El bot usa lenguaje sitio/acceso, botones por sitio, `/misitio`, confirmación ligada a la sesión exacta y aliases legacy silenciosos. Cada activación Telegram queda aislada al usuario, dura **15 minutos fijos**, no se renueva desde web, avisa 5 minutos antes y el job revoca sólo su regla; la activación escrita se limita a sitios visibles/asignados. Migración aditiva aplicada: `lease_source` y `expiry_warning_sent_at`. Verificación local: backend **129/717**, frontend **79/267**, focalizadas **41/41**, build/inventario/TypeScript/lint/`check:all` verdes. Producción: 58 tablas preservadas, ambos bots iniciados, raíz/legacy/health 200, CORS 204/403, login inválido 401, MySQL/RouterOS/SMTP `ok`, 0 reinicios y logs de arranque sin errores. Backup restaurado `/root/pre-telegram-sites-20260825T051148Z`; rollback `gestionvpn-10-{backend|frontend}:pre-telegram-sites-20260825T051148Z`; disco 83%/4.3 GiB libres.

- **Integraciones globales y por workspace desplegadas (2026-08-24; runtime `51ca7bb`):** Administración → Configuración gestiona Brevo/Gmail, Telegram administrativo, Gemini global y Firebase Google Login; cada OWNER gestiona aparte Brevo/Gmail, Telegram y Gemini de su workspace. Secretos validados y cifrados AES-256-GCM, nunca retornados; APIs globales exclusivas de `platform_admin` y APIs de workspace aisladas por `workspace_id`. Producción importó y validó las credenciales preexistentes de **Brevo, Telegram, Gemini y Firebase**; las cuatro están `ACTIVE`, Gmail queda como alternativa no configurada. Firebase `vpn-noc` está habilitado; Telegram usa `@mivpnmanager_bot`; Gemini usa `gemini-3.1-flash-lite`. Verificación local: backend **127/702**, frontend **78/261**, build/lint/`check:all` verdes. Producción: tablas `platform_integrations` y `workspace_integrations`, health MySQL/SMTP `ok`, raíz/legacy 200, preflight 204, origen hostil 403, login inválido 401, backend/DB healthy, 0 reinicios y sin errores recientes. Backup `/root/pre-integrations-20260825T033714Z`; rollback `gestionvpn-10-{backend|frontend}:pre-integrations-20260825T033714Z`; disco 80%/5.1 GiB libres.

- **Incidente de suspensión administrativa corregido y desplegado (2026-08-22; runtime `6056648`):** al suspender Housenet, una membresía histórica hacía que el `platform_admin` también figurara como OWNER del workspace cliente; la operación masiva suspendió al Administrador y revocó sus sesiones. Producción fue reparada con respaldo `/root/backups/auth-incident-20260823T025802Z/auth-tables.sql`: Administrador activo, sólo conserva su workspace propio y el moderador permanece suspendido. El código excluye `is_platform_admin=1` tanto del cambio de estado como de los objetivos de revocación, y una migración idempotente retira membresías administrativas ajenas. Validación local: backend **123/687**, `check:all` y Semgrep security verdes. Producción: HTTPS/health 200, backend healthy, 0 reinicios y migración `removed=0`. Rollback `gestionvpn-10-backend:pre-auth-isolation-20260823T030306Z`.
- **Asistente de Servidor VPN y escaneo VPS-only desplegados (2026-08-22; runtime `11aea0a`):** la pestaña separa conectar un Core existente, preparar un MikroTik vacío y reemplazar el Core (este último permanece no disponible hasta tener migración recuperable). La preparación usa cuatro pasos obligatorios y `core_provision_runs` registra actor, destino, red, tiempos, pasos y error sin secretos. Escaneo ya no ofrece selector/IP local: UI y backend operan sólo con pool VPS por workspace, arranque normaliza `scan_mode=vps` y API rechaza `local`. Backend **121/685**, frontend **77/259**, build y `check:all` verdes. Producción HTTPS/servicios sanos, contenedores 0 reinicios y scan-route Housenet 1/1. Backup `/root/pre-vps-uni-11aea0a-20260823T011144Z`; rollback único vigente `gestionvpn-10-{backend|frontend}:pre-11aea0a-20260823T011144Z`. Limpieza 2026-08-22 dejó disco 68%/7.8 GiB libres.
- **Paquete administrador, configuración y WireGuard desplegado (2026-08-22; runtime `acc8424`):** producción incorpora modales móviles, correo vigente en Usuarios VPN, Dashboard responsive, gestión de Moderadores con último acceso, Seguridad con filtros/paginación/exportación y Configuración del Router Core reorganizada con prueba read-only protegida. Backend **120/683**, frontend **76/258**, build y `check:all` verdes. HTTPS y health 200; MySQL/RouterOS/SMTP `ok`; `wg0`, API del Core y scan-route Housenet 1/1 verificados sin escribir en MikroTik. Backup: `/root/pre-vps-uni-acc8424-20260823T001911Z`; rollback `gestionvpn-10-{backend|frontend}:pre-acc8424-20260823T001911Z`.
- **Responsive móvil y login desplegados (2026-08-22; runtime `0a8a52b`):** “Mi equipo” usa filtros/tarjetas móviles sin overflow, separa sitio/correo/rol y adapta Miembros/Actividad; Configuración usa navegación compacta y adapta Perfil, WireGuard/QR, Workspace, Notificaciones y Respaldo/importación. Se retiró la leyenda inferior del login sin alterar autenticación. Frontend **76/258**, AuthForms **7/7**, build, `check:all` y lint verdes. Producción HTTPS 200, contenedores con 0 reinicios; scan-route Housenet 1/1 en dry-run. Rollback: `gestionvpn-10-frontend:pre-mobile-ui-0a8a52b-20260822T231432Z`.

- **Core MikroTik endurecido y parser WG corregido/desplegado (2026-08-22; runtime `f23211b`):** backup dual cifrado+RSC verificado antes del cambio (`servervpn_2026-08-22_17-26-59_GW-VPN-CORE-ISP.*`). Se retiraron la IP/listas/rutas conectadas legacy CLIENTES `10.13.250.0/24` y 8 reglas firewall duplicadas/obsoletas; firewall quedó 21→13. Se conservó explícitamente ADMIN legacy `10.14.250.0/24` porque el peer `10.14.250.2/32` debe permanecer, dejando comentarios `LEGACY requerido`. API 8728 acepta sólo `10.12.250.60/32` (VPS por WG), API-SSL sin certificado está deshabilitada, Winbox acepta pools actuales y el peer admin legacy. Bandwidth server, SNMP y neighbor discovery están deshabilitados. `parseHandshakeSecs` ahora incluye semanas+días; backend **120/683**, frontend **76/258**, `check:all` verde. Producción: HTTPS/health 200, tres contenedores sanos y sin reinicios. El Core sufrió un arranque alrededor de 17:39 durante la ventana; no hay `autosupout`, historial de reboot ni evidencia de watchdog, por lo que RouterOS no conserva causa concluyente. Housenet/SSTP y seis rutas VRF se recuperaron. Las mangles quedaron vacías correctamente porque BD confirma 0 sesiones ACTIVE: nunca recrearlas de forma permanente; activación/keepalive las repone.

- **Auditoría read-only del MikroTik Core (2026-08-22; sin cambios):** `GW-VPN-CORE-ISP` (RB750GL, RouterOS 7.19.3) está operativo: uptime 8+ semanas, Internet/API OK, 3 WG de gestión, SSTP 4443 y Housenet activo por SSTP durante 2d20h. RouterOS y BD coinciden 1:1 en nodo, secret, VRF y LAN; 0 VRF/secrets huérfanos, 0 Allowed Address duplicados, sesión y scan mangle vigentes. Hallazgos: plano legado `10.13.250/24`+`10.14.250/24` convive con supernet actual `10.12.248/22`, reglas firewall antiguas duplican las `GVPN:*`, peer Admin lleva 5d sin handshake, API allowlist conserva redes/IP legacy, y `parseHandshakeSecs` ignora días (puede producir falsos estados recientes). Servicios btest/SNMP/discovery siguen habilitados pero el input WAN termina en drop. Requiere plan/backup antes de limpiar; no se mutó RouterOS.

- **Indicador SSH privado y estable desplegado (2026-08-22; incluido desde runtime `f23211b`):** el badge de Buscar equipos no muestra nombres de usuario, sólo `SSH configurado · N credenciales`. La hidratación depende del `ppp_user` estable, por lo que refrescar el mismo sitio no limpia ni repite la consulta. Pruebas focalizadas **8/8**, build y `check:all` verdes.

- **Aviso SSH de Buscar equipos corregido y desplegado (2026-08-22; runtime `e6666ef`):** producción confirma que Housenet (`VRF-ND2-HOUSENET`, `ppp-housenet-nd2`) tiene 3 credenciales SSH de nodo con contraseña y un AP guardado con credencial propia. La vista hidrata credenciales al seleccionar el nodo, evita respuestas cruzadas al cambiar rápidamente de sitio y distingue carga, ausencia real, 403 por rol, fallo de consulta y credenciales propias de equipos; el escaneo consume la misma fuente ya hidratada. Verificación local: frontend **76 archivos/257 pruebas**, build y `check:all` verdes. Producción: sólo frontend recreado, HTTPS/health 200, bundle confirmado, backend/DB preservados y sanos, frontend 0 reinicios y scan-route Housenet 1/1. Rollback `gestionvpn-10-frontend:pre-ssh-status-e6666ef-20260822T180740Z`; disco 80%/4.9 GiB libres.

- **Equipo/Actividad y membresías corregidos y desplegados (2026-08-22; runtime `4c7d08e`):** `joinpoint.cloud` sirve la vista Equipo responsiva (tarjetas móviles/tabla escritorio) y estados WG reales `Deshabilitado|Conectado|Desconectado`; el endpoint ya no confunde handshake residual con peer activo. Actividad exporta CSV/JSON/PDF y conserva físicamente como máximo 7 días mediante purga al arranque y cada 5 minutos. Miembros excluye cuentas de plataforma y futuras invitaciones OWNER retiran el placeholder transaccionalmente; producción ya tenía 0 placeholders activos al desplegar. Verificación: backend **119/679**, frontend **75/252**, build Docker limpio, HTTPS 200, contenedores sanos, MySQL/RouterOS/SMTP `ok`, WG handshake y ruta al Core correctos, scan-route 1/1, 12 eventos y 0 vencidos. Backup: `/root/pre-vps-uni-b477872-20260822T165819Z`; rollback `gestionvpn-10-{backend|frontend}:pre-b477872-20260822T165819Z`. Disco 77%/5.7 GiB libres tras retirar únicamente el rollback backend anterior `pre-scan-fix-20260820T042302Z`, ya superado.

- **Escaneo VPS `/22` corregido y desplegado (2026-08-19; `cda8ad7`):** `134.199.212.232` trabaja ya desde `vps_uni`; backend, DB y frontend están sanos. El env persistente usa `SCAN_IP_POOL_BASE=10.12.248.` y `SCAN_RETURN_SUBNET=10.12.248.0/24`; `check:scanroute` carga el supernet y confirma la ruta VRF. En el Core, `10.12.248.0/24` está en `vpn-activa` y `LIST-MGMT-TRUSTED`, y el peer `VPN-WG-VPS` quedó con `10.12.250.60/32,10.12.248.0/24`; se retiraron del peer/lista los residuos `10.11.252.0/24`. El código reconcilia en adelante listas y Allowed Address de peers existentes. Verificación local: backend **117 archivos/676 pruebas**, `check:all` verde; producción HTTPS 200, migraciones sin error, `wg0`/autosync activos y ruta a `10.1.1.0/24`. Backup: `/root/pre-vps-uni-scan-fix-20260820T042302Z`; rollback de backend `pre-scan-fix-20260820T042302Z`. Disco 70%/7.3 GiB libres tras retirar sólo rollbacks `pre-supernet-e7b40b8` ya superados. Pendiente humano: activar el túnel Housenet y ejecutar un escaneo real; sin sesión activa no existe `SCAN-WS` por diseño.

- **Core y supernet de gestión `/22` aprovisionados en producción (2026-08-15; `c7bc79e`):** se configuró `management_supernet=10.12.248.0/22` y quedó inmutable tras registrar `core_provisioned_at`. El Core `GW-VPN-CORE-ISP` conserva gateways específicos `10.12.249.1/24`, `10.12.250.1/24` y `10.12.251.1/24`, peer VPS con handshake reciente, SSTP `4443`, listas y siete reglas GVPN administradas. Antes del cambio se generó el par `.backup` cifrado + `.rsc`, se validó y se envió al correo verificado del Administrador; la contraseña se generó en el VPS, se almacenó cifrada y nunca se registró. Estado posterior: Core `HEALTHY`, ping 0 % pérdida, `wg0` con única ruta/AllowedIPs `10.12.248.0/22`, HTTPS 200, MySQL/RouterOS/SMTP `ok`, contenedores sanos, cero sitios/equipos/huérfanos, 3 workspaces y 1 Administrador preservados. Pendiente humano: crear el primer sitio real y ejecutar el canary integral; no inventar un CPE.

- **Reinicio operativo y migración VPS `/22` completados (2026-08-15; producción):** tras restaurar correctamente un dump temporal, se eliminaron 80 AP, 14 grupos, 450 CPE, 1 asignación y 31 estados de monitoreo huérfanos. Estado final: cero datos operativos de sitios; 3 workspaces y 1 Administrador preservados; 2 scan-IP migradas a `10.12.248.x`. Las FKs nodo→AP/torre y AP→CPE usan CASCADE y triggers limpian dependencias textuales/grupos en cada borrado futuro. `wg0` conserva únicamente ruta/AllowedIPs `10.12.248.0/22`, 49 scan-IP nuevas y autosync vacío; pool/rutas históricos retirados. Core responde sin pérdida y contenedores sanos. Verificación local: backend **115/669**, `check:all`, Semgrep security **516 archivos/0 hallazgos** y diff correctos. Dump `/root/pre-orphan-site-cleanup-20260815T200030Z/vpn_manager.sql.gz`; rollback WG `/root/wg0-before-obsolete-route-removal-20260815T195736Z.conf`.

- **Reinicio operativo ejecutado:** se conserva la cuenta/datos del Administrador, 3 workspaces y la información/configuración de Administración; el dominio operativo de sitios quedó vacío y verificado. No volver a introducir las redes históricas fuera de un rollback explícito.

- **Inventario y preview global del reinicio operativo implementados (2026-08-15; local y sólo lectura):** `GET /api/admin/operational-reset-preview`, exclusivo del Administrador, separa conteos operativos, ambiguos y preservados; siempre responde `readOnly: true` y `canExecuteReset: false`. El estado limpio productivo fue confirmado. Documento: `docs/implementation/INVENTARIO_REINICIO_OPERATIVO_2026-08-15.md`.

- **Supernet de gestión `/22` configurable sólo en la instalación inicial (implementación endurecida 2026-08-15; local, sin deploy):** Administración elegirá una red privada `10.x.x.0/22` alineada; default `10.12.248.0/22`, derivando escaneo `.248/24`, clientes `.249/24`, VPS `.250/24` y admin `.251/24`. El guardado de setting y scan-IP es atómico y auditado; el preview autoritativo bloquea solapamientos con host, LAN de sitios, redes reservadas y direcciones del Core; repetir el valor activo es idempotente. Después del primer aprovisionamiento o si existen nodos queda inmutable. El Core conserva rutas `/24` por gateways distintos y los sitios remotos generan una sola ruta `/22`. Verificación: backend **113 archivos/664 pruebas**, frontend **73/263**, `check:all`, build, inventario de seguridad, Semgrep **514 archivos/0 hallazgos** y diff correctos. Antes del deploy faltan inventario exacto conservar/eliminar, canary RouterOS 7.x, prueba integral de reinicio/rollback y documentación operativa final. Documento: `docs/implementation/CONFIGURACION_INICIAL_SUPERNET_GESTION_2026-08-12.md`.

- **Borrado seguro de sitio + equipos implementado localmente (2026-08-10; pendiente de despliegue):** preview autenticado con conteos/huella, confirmación por nombre, bloqueo de AP ambiguos, cascada MySQL transaccional aislada por `workspace_id + node_id`, cierre de sesiones, asignaciones/invitaciones/monitoring, eliminación AP/CPE/snapshots/RF/torres, auditoría y limpieza de cachés. RouterOS ya no oculta fallos parciales; el error DB posterior a router es explícito. Corregido `wsUserIds` en borrado de moderador. Verificación: backend **111 archivos/653 pruebas**, frontend **73/262**, focalizadas nuevas **2 archivos/8 pruebas**, `check:all`, build, inventario 160 rutas y Semgrep security **513 archivos/0 hallazgos**. Falta dry-run productivo y despliegue canary; job durable automático queda como hardening posterior. Documento: `docs/implementation/PLAN_BORRADO_CASCADA_SITIO_EQUIPOS_2026-08-10.md`.

- **Plan de suscripciones por workspace refinado (2026-08-10; sin implementar):** Básico = 5 sitios/2 miembros y solo gestión remota; Intermedio = 10/5 + escaneo, guardado e inventario de equipos; Avanzado = sitios/miembros ilimitados bajo uso razonable + monitor, diagnóstico y exportaciones completas. IA AirOS queda fuera de los planes como piloto temporal autorizado. Ciclos mensual/anual y precios aún provisionales S/149, S/299 y S/549. Factura interna, emisión parametrizable, comprobantes OWNER, pagos parciales/completos, avisos app/correo, vencimiento fin de día Perú, gracia manual completa y cierre inmediato de túneles. Documento: `docs/implementation/PLAN_SUSCRIPCIONES_WORKSPACES_2026-08-10.md`.

- **Auditoría de seguridad de solo lectura completada (2026-08-10):** postura general buena y sin riesgos altos confirmados pendientes; Semgrep security/secrets sin hallazgos, dependencias sin vulnerabilidades altas/críticas y una moderada transitiva, TLS/UFW/Fail2ban/SSH/cookies-CSRF-RBAC verificados. El usuario confirmó clave privada WG rotada, aislamiento entre sitios, altas/ediciones/bajas de redes LAN y control de secretos activos. Permanecen sólo mejoras graduales de hardening. Informe: `docs/security/SECURITY_AUDIT_2026-08-10.md`. No se modificó producción.

- **Sincronización de LAN adicionales en WireGuard desplegada (2026-08-09 22:32 America/Lima / 2026-08-10 03:32 UTC):** producción funcional `922b1c6`. Corrige el falso éxito al editar nodos históricos sin `wg_public_key`: actualiza sólo campos enviados, conserva fila/FK, resuelve el peer por clave o por único peer de interfaz, aborta ante cero/múltiples candidatos, sincroniza altas y bajas con conjunto autoritativo, verifica RouterOS y autocura la clave histórica. `WG-ND13-ROSMERYND2` conserva `10.11.250.13/32`, `192.168.100.0/24` y `192.168.30.0/24`; las rutas repetidas de otros sitios siguen aisladas en sus propias tablas VRF. Se reconstruyó y recreó sólo backend; frontend y MariaDB conservaron contenedores/datos. Verificación: backend local **109 archivos/645 pruebas**, focalizadas **3 archivos/35 pruebas**, sintaxis, inventario de seguridad y diff; producción health MySQL/SMTP `ok`, raíz 200, preflight 204, login inválido 401, migraciones idempotentes, 55 tablas/3 workspaces/17 nodos, tres contenedores con 0 reinicios. Respaldo `/root/pre-lan-peer-sync-20260810T032832Z`; rollback `gestionvpn-10-backend:pre-lan-peer-sync-20260810T032832Z`; disco 75%/6.3 GiB libres. Tras el recreate se requiere reactivar el sitio para una prueba de tráfico desde sesión viva.

- **Raíz y frontend ordenados de forma recuperable (2026-08-08):** 13 documentos históricos de la raíz, 24 documentos de refactor del frontend, 3 scripts puntuales y 2 salidas antiguas de lint fueron trasladados a `docs/archive/` y `scripts/archive/`; no se eliminó información. README, arquitectura, despliegue, manual, configuración, secretos locales, código y documentación vigente permanecen en su ubicación. Los índices de archivo explican qué puede revisarse y eliminarse después.

- **Exportaciones del monitor ampliadas localmente (2026-08-08; pendiente de despliegue):** el informe por sitio permite PDF, Excel y JSON; la lista visible de clientes/CPE permite CSV, PDF, Excel y JSON y respeta búsqueda/filtro. Los formatos pesados cargan sus dependencias sólo al usarse y el menú flotante evita recortes dentro de tablas. Verificación: frontend build, lint, pruebas focalizadas del monitor y diff correctos. Producción continúa en `c9f3131`.

- **Validación de redes LAN `/24–/32` implementada localmente (2026-08-08; pendiente de despliegue):** creación, edición y CSV bloquean formatos inválidos, prefijos `/0–/23`, direcciones no canónicas, redes especiales, duplicados, solapamientos internos y redes de gestión; `/31` y `/32` se aceptan con advertencia. El backend repite la validación antes de RouterOS. Las redes históricas amplias se conservan y pueden retirarse, pero no volver a agregarse. Verificación: backend **107 archivos/635 pruebas**, frontend **73 archivos/262 pruebas**, contratos, lint, build y diff correctos. Informe: `docs/implementation/INFORME_IMPLEMENTACION_VALIDACION_LAN_2026-08-08.md`. Producción continúa en `c9f3131`.

- **Retención de actividad y mejoras de Sitios desplegadas (2026-08-08 16:12 America/Lima / 21:12 UTC):** producción funcional `c9f3131`. Actividad reciente queda limitada server-side y purgada físicamente a siete días; la exportación sólo ofrece ese periodo. El selector de columnas muestra acciones permanentes de agregar/quitar. Agregar sitio remoto refina SSTP/WireGuard, datos generados, opciones avanzadas, LAN, pasos y terminología sin cambiar el aprovisionamiento. El título UTF-8 es `Joinpoint NOC | Gestión de redes`. Se reconstruyeron y recrearon backend/frontend con `docker-compose.prod.yml`; MariaDB conservó contenedor, volumen y datos, sin migraciones nuevas. Verificación: frontend **73 archivos/248 pruebas**, backend **107 archivos/633 pruebas**, focalizadas **12/12 + 2/2 + 1/1**, lint, build, `check:all`, auditor visual **309/0**, raíz/legacy 200, CORS 204/403, login inválido 401, MySQL/RouterOS/SMTP `ok`, bundle y título UTF-8 confirmados, tres contenedores con 0 reinicios. Rollback `gestionvpn-10-{backend,frontend}:pre-c9f3131`; disco 80%/5.1 GiB libres.

- **Login y comunicaciones Joinpoint desplegados (2026-08-08 15:06 America/Lima / 20:06 UTC):** producción funcional `ca23dae`. Se reconstruyeron y recrearon backend y frontend exclusivamente con `docker-compose.prod.yml`; MariaDB conservó contenedor, volumen y datos, y las migraciones idempotentes terminaron correctamente. El login dividido/refinado y el branding `Joinpoint NOC` de correo y Telegram están activos; `SMTP_FROM` continúa como fuente de verdad de la dirección remitente. Validación: raíz/legacy 200, preflight 204, origen hostil 403, login inválido 401, MySQL/RouterOS/SMTP `ok`, backend/DB healthy, bundle nuevo y branding backend confirmados, y los tres contenedores con 0 reinicios. Disco 71%/7.1 GiB libres. La poda previa liberó 4.3 GB pero retiró también las imágenes históricas etiquetadas; no hay rollback inmediato por imagen y, si fuera necesario, debe reconstruirse el commit anterior `6376197`. Pendiente manual opcional: nombre/foto/descripción pública del bot mediante BotFather.

- **Identidad visual Joinpoint NOC desplegada (2026-08-06):** producción funcional `6376197`. El producto visible adopta `JOINPOINT NOC` y el lema `Tu red, bajo control`, con símbolo SVG propio basado en una J y tres nodos conectados. Se actualizaron login, sidebar de escritorio/móvil, favicon, título/metadatos del navegador, pantalla de acceso restringido, pies de PDF y autor de Excel. Las menciones operativas a MikroTik/WireGuard y las claves/rutas internas `GestionVPN-1.0` se conservaron para no alterar contratos ni datos. Verificación local: frontend **73 archivos/248 pruebas**, lint, build, auditor visual **309 archivos/0 errores/0 warnings** y diff correctos. Se reconstruyó y recreó exclusivamente `vpn-frontend`; backend y MariaDB conservaron contenedores y datos. Producción: raíz/legacy/workspace 200, preflight 204, origen hostil 403, login inválido 401, MySQL/SMTP `ok`, RouterOS `stale` transitorio, bundle Joinpoint confirmado y los tres contenedores con 0 reinicios. Rollback `gestionvpn-10-frontend:pre-joinpoint-6376197`; disco 79%/5.3 GiB libres.

- **Encabezados completos y reloj de acceso corregidos y desplegados (2026-08-06 00:29 America/Lima / 05:29 UTC):** producción funcional `356ddb0`. Las once columnas ordenables conservan sus nombres completos y usan scroll horizontal si no caben; el reloj calcula color/estado contra el lease real de 5 minutos, inicia verde y solo muestra `Expira pronto` durante el último 25 %. Se reconstruyó y recreó exclusivamente `vpn-frontend`; backend y MariaDB conservaron contenedores y datos, sin migraciones. Verificación local: pruebas focalizadas **6/6** de columnas y **7/7** del reloj/activación, lint, build y diff correctos. Producción: raíz/workspace/health 200, preflight 204, origen hostil 403, login inválido 401, MySQL/RouterOS/SMTP `ok`, bundle nuevo confirmado y los tres contenedores con 0 reinicios. Rollback `gestionvpn-10-frontend:pre-header-clock-20260806T052521Z`; disco 81%/4.7 GiB libres.

- **Mejoras móviles, botones globales y orden de equipos desplegados (2026-08-05 22:00 America/Lima / 2026-08-06 03:00 UTC):** producción funcional `8eafba1`. Sitios usa tarjetas adaptadas por debajo de 640 px; las acciones elevadas y los secundarios blancos comparten el sistema visual aprobado; Estado de equipos permite ordenar once columnas por sitio en escritorio y móvil, con persistencia local. Se reconstruyó y recreó exclusivamente `vpn-frontend`; backend y MariaDB conservaron contenedores y datos, sin migraciones. Verificación local: frontend **72 archivos/245 pruebas**, lint, build, auditor visual **308 archivos/0 errores/0 warnings** y diff correctos. Producción: raíz/workspace/health 200, preflight 204, origen hostil 403, login inválido 401, MySQL/RouterOS/SMTP `ok`, bundle nuevo confirmado y los tres contenedores con 0 reinicios. Rollback `gestionvpn-10-frontend:pre-ui-sort-20260806T025351Z`; disco 78%/5.4 GiB libres.

- **Recuperación de sesión y módulos en pestañas antiguas desplegada (2026-08-04 07:46 America/Lima / 12:46 UTC):** producción `424c552`. Al recuperar visibilidad o recibir `pageshow`, `useSessionExpiry` revalida inmediatamente `/api/account/session-status`; el `401 SESSION_EXPIRED`/`NO_SESSION` activa el logout global existente y vuelve al login conservando la ruta. Las comprobaciones concurrentes se deduplican. Los fallos de chunks lazy obsoletos tras un despliegue se distinguen de errores de render y provocan una sola recarga automática por minuto; si persisten, el fallback ofrece `Actualizar página`. Se reconstruyó y recreó únicamente el frontend; backend y DB conservaron contenedores y datos. Verificación local: **71 archivos/238 pruebas**, lint, build y diff; producción: raíz/workspace 200, health MySQL/SMTP `ok` (RouterOS `stale` transitorio), bundles confirmados, backend/DB healthy y 0 reinicios. Rollback `gestionvpn-10-frontend:pre-session-resume-20260804T124200Z`; disco 75%/6.1 GiB libres.

- **Diagnóstico y último estado de AP desplegados (2026-08-04 07:25 America/Lima / 12:25 UTC):** producción funcional `93245c7`. Estado de equipos consulta AP sin estadísticas o con lectura mayor a 60 s en lotes de 2, deduplica solicitudes y completa Señal, CCQ, Potencia, Tiempo en línea y CPU. Cada lectura exitosa persiste solo esas cinco métricas y `captured_at` en `ap_status_snapshots`; el inventario conserva el último estado cuando nodo/túnel está inactivo. Diagnóstico técnico y secretos no se persisten. `Revisar diagnóstico` carga datos actuales y admite credenciales propias o heredadas del nodo dueño con aislamiento/anti-SSRF. Se reconstruyeron backend/frontend con `docker-compose.prod.yml`; DB no se recreó. Migración automática creó la tabla con 7 columnas: producción pasó 54→55 tablas y el snapshot inicia en 0 hasta la primera lectura; datos 5 usuarios/3 workspaces/15 nodos intactos. Verificación: raíz/legacy/health 200, preflight 204, origen hostil 403, MySQL/RouterOS/SMTP `ok`, bundle confirmado, backend/DB healthy, 0 reinicios y 0 errores de backend/esquema. Respaldo restaurado/verificado `/root/pre-ap-status-20260804T122149Z/vpn_manager.sql.gz`; rollback `gestionvpn-10-{backend,frontend}:pre-ap-status-20260804T122206Z`. Se liberaron 1.961 GB de build cache; disco 72%/6.8 GiB libres.

- **Ajuste de columnas de Buscar equipos desplegado (2026-08-04 00:04 America/Lima / 05:04 UTC):** producción funcional `20c2851`. Se retiraron la columna fija `SSH` vacía y el icono para copiar de la celda IP. `Nombre / Modelo` y `SSID / AP` usan el mismo ancho flexible mínimo de 220 px y muestran contenido multilínea sin elipsis; la IP conserva visibilidad completa, enlace, orden numérico, posición fija y resize, con ancho por defecto 180 px/mínimo 160 px. Se reconstruyó y recreó exclusivamente el frontend mediante `docker-compose.prod.yml`; backend y MariaDB conservaron sus contenedores, no hubo migraciones. Verificación: local **6/6**, lint, build y diff correctos; producción raíz/legacy/health 200, MySQL/RouterOS/SMTP `ok`, bundle nuevo confirmado y los tres contenedores con 0 reinicios. Rollback: `gestionvpn-10-frontend:pre-equipment-columns-20260804T050233Z`; disco 68% con 7.9 GiB libres.

- **Correcciones visuales y workspace inmutable desplegados en producción (2026-08-03 23:14 America/Lima / 2026-08-04 04:14 UTC):** producción funcional `2e7f7d0`; este despliegue supera los estados locales pendientes inmediatamente debajo: tabla IP/MAC de Buscar equipos, `X` visible del selector, nombre/slug de workspace inmutables y buscador de Estado de equipos desbloqueado. Se reconstruyeron y recrearon exclusivamente backend/frontend mediante `docker-compose.prod.yml`; MariaDB no se recreó y no hubo migraciones nuevas (las automáticas fueron idempotentes). Verificación: raíz/legacy 200, health 200 con MySQL/RouterOS/SMTP `ok`, preflight 204, origen hostil 403, login inválido 401, bundles nuevos confirmados, datos **54 tablas/5 usuarios/3 workspaces/15 nodos**, backend/DB healthy y los tres contenedores con 0 reinicios. Rollback preservado: `gestionvpn-10-{backend,frontend}:pre-visual-fixes-20260804T040439Z`. Se retiraron cinco rollbacks antiguos ya superados; disco cerró en 65% con 8.5 GiB libres.

- **Tabla de Buscar equipos mejorada en IP/MAC y navegación horizontal, pendiente de despliegue (2026-08-03):** las IPv4 se ordenan numéricamente por octetos y los valores inválidos quedan al final. La columna fija `IP` usa 196 px por defecto, mínimo legible de 180 px, permanece visible durante el scroll, no trunca la dirección, permite copiarla, muestra IP/MAC/nombre/modelo en la ayuda contextual y se redimensiona por ratón o teclado conservando el ancho en preferencias. `MAC` pasa a ser columna independiente, opcional y oculta por defecto en el selector; móvil evita duplicarla. La columna fija de acciones baja de 180 a 116 px. Verificación: pruebas focalizadas **18/18**, lint, build, auditor visual **306 archivos/0 errores/0 warnings** y `git diff --check` correctos. Producción no cambia.

- **Acción de retirar columnas visible en Buscar equipos, pendiente de despliegue (2026-08-03):** en el selector `Columnas`, la `X` de cada columna visible dejó de depender del hover. Ahora permanece visible con tratamiento rojo, fondo suave, icono de 16 px, ayuda contextual y área táctil de 44 px; hover/focus sólo refuerzan la interacción. Verificación: prueba focalizada **3/3**, lint, build y `git diff --check` correctos. Producción no cambia.

- **Nombre del espacio de trabajo convertido en inmutable localmente, pendiente de despliegue (2026-08-03):** el nombre se define exclusivamente al crear o invitar el workspace. Después se muestra como dato bloqueado tanto al propietario como al administrador; se retiró `PATCH /api/workspace/name`, el contrato administrativo rechaza `workspaceName` en actualizaciones y no queda ningún `UPDATE workspaces SET name` en el código vigente. Esto conserva estable el slug y evita divergencias entre nombre y rutas. Verificación: contratos compilados, pruebas focalizadas **3/3**, sintaxis backend, lint, build y `git diff --check` correctos. Producción no cambia.

- **Buscador de Estado de equipos corregido localmente, pendiente de despliegue (2026-08-03):** el campo ya no se bloquea cuando la búsqueda actual deja cero coincidencias. Permanece editable mientras contenga texto, permitiendo corregirlo o limpiarlo; sólo se deshabilita cuando no existen equipos visibles y tampoco hay una búsqueda activa. Verificación: prueba focalizada **4/4**, lint, build y `git diff --check` correctos. Producción no cambia.

- **Despliegue visual completado en producción (2026-08-03):** el commit funcional `84b4fda` despliega y da por superados los estados “pendiente de despliegue” descritos debajo: sistema visual compartido, Buscar equipos, menú del moderador y corrección CSP. También incluye Estado de equipos, Mi equipo, iconografía de Sitios y menús/selectores flotantes. Producción: raíz/legacy 200, preflight 204, origen hostil 403, MySQL/RouterOS/SMTP `ok`, backend healthy, frontend activo y cero reinicios. Sin migraciones ni reinicio de backend/DB. Rollback: `gestionvpn-10-frontend:pre-ui-20260803T013525Z`.

- **Primera fase del sistema visual compartido aplicada, pendiente de despliegue (2026-08-03):** `components/Common/ui.tsx` concentra `PageHeader`, `EmptyState`, `Button`, `StatusBadge`, `SectionCard`, `SearchInput` y `SegmentedControl`. Buscar equipos, **Estado de equipos** (antes Estado de antenas), Sitios, Mi equipo y ambos módulos de Ajustes reutilizan los patrones canónicos; `btn-md` garantiza 44 px, encabezados 18/20 px y estados vacíos 64/32/18/14 con espaciado uniforme. Toda la terminología genérica de la vista `/monitor` usa ahora “equipos”; “antena” se conserva únicamente para atributos físicos reales. La escala y reglas de uso están documentadas en `vpn-manager/src/styles/DESIGN_SYSTEM.md`; el auditor suma DS10–DS13. Verificación: build y lint correctos; pruebas focalizadas actualizadas. Producción no cambia.
- **Vista Buscar equipos rediseñada visualmente, pendiente de despliegue (2026-08-02):** el encabezado adopta la composición de Ajustes (icono índigo en contenedor, tipografía, tarjeta, bordes, radios y sombra), con el nombre coherente `Buscar equipos` y lenguaje cotidiano. Sin conexión se presenta un estado vacío central que explica el siguiente paso y ofrece `Ir a Sitios` y `Ver guardados`; este último navega directamente a `Estado de antenas`, fuente visual de los equipos guardados, sin panel intermedio. Ambos botones mantienen texto e icono en una sola fila, altura táctil de 44 px, ancho igual en escritorio, ancho completo en móvil y la misma sombra índigo/efecto hover; el secundario conserva fondo blanco y borde. Con conexión muestra nombre del sitio, red efectiva y estado `Conexión activa`; se omite deliberadamente `Última búsqueda` porque no existe una fecha real integrada en esa tarjeta. Los estados ocioso, buscando, finalizado, sin resultados y error conservan la lógica previa pero usan terminología de búsqueda en vez de escaneo/Nodos. Sin cambios en API, backend, BD o motor de descubrimiento. Verificación: pruebas focalizadas **6/6**, TypeScript, lint, build, auditor visual **305/0** y `git diff --check` correctos. Producción todavía no cambia.

- **Menú de moderadores y miembros simplificado localmente, pendiente de despliegue (2026-08-02):** la navegación operativa se redujo a `Operación` (Sitios, Buscar equipos, Estado de antenas) y `Cuenta` (Mi equipo, Configuración), respetando el filtrado RBAC; Plataforma permanece sólo cuando corresponde al administrador. Se retiraron los bloques redundantes `Servicio disponible / Conexión principal` y `Todo funciona correctamente`. El perfil inferior muestra nombre legible y rol, con el correo completo como ayuda contextual; el tema usa únicamente un botón Sol/Luna de 44 px con nombre accesible. El elemento activo incorpora fondo, borde interior, sombra, mayor contraste e indicador lateral integrado, además de `aria-current`. Verificación: pruebas focalizadas **4/4**, TypeScript, lint, build, auditor visual **305/0** y `git diff --check` correctos. Producción todavía no cambia.

- **Advertencia CSP `upgrade-insecure-requests` corregida localmente, pendiente de despliegue (2026-08-02):** se retiró la directiva exclusivamente de `Content-Security-Policy-Report-Only`, donde la especificación obliga al navegador a ignorarla. HTTPS, HSTS y el resto de la política de observación permanecen intactos. Una prueba de regresión impide reintroducirla en el encabezado Report-Only; verificación focalizada **3/3** y `git diff --check` correctos. Producción todavía no cambia.

- **Base productiva replicada sobre local para pruebas (2026-08-02 22:20–22:36 America/Lima):** `vpn_manager` local fue reemplazada, por solicitud explícita, con un dump consistente y de solo lectura de la MariaDB productiva. La copia compatible con XAMPP/MariaDB 10.4 conserva **54 tablas, 5 usuarios, 3 workspaces, 15 nodos y 5 membresías**, incluido 1 `platform_admin` activo; backend local consulta MySQL/SMTP `ok`. La base local anterior quedó respaldada fuera del repo en `C:\Users\i201720174\Desktop\GestionVPN-local-backups\vpn_manager-before-prod-20260802T222000.sql`; el dump original y su variante XAMPP están en la misma carpeta. No se copiaron secretos de entorno/DB del VPS: credenciales RouterOS cifradas con la clave productiva no se descifran localmente. Para el flujo de correcciones visuales, `.env` local mantiene Telegram, monitoreo RouterOS y polling AP automáticos desactivados; los endpoints manuales permanecen disponibles. Los temporales productivos fueron eliminados y el VPS no sufrió cambios funcionales.

- **Columna fija de Opciones corregida y desplegada (2026-08-02 21:51–22:10 America/Lima):** producción funcional `c393c78`. En la tabla de Sitios, el texto de las columnas desplazables ya no se transparenta detrás de `Conectar`: la celda sticky usa fondos 100 % opacos según estado/alternancia, hover opaco, borde izquierdo y sombra lateral; el encabezado `Opciones` adopta el mismo panel sólido. Verificación local: pruebas frontend focalizadas **16/16**, TypeScript, lint, auditor visual **305/0**, `check:all` y diff correctos. Producción: raíz/legacy 200, health MySQL/RouterOS/SMTP `ok`, preflight 204, origen hostil 403, backend/DB healthy, frontend activo y 0 reinicios. Rollback `gestionvpn-10-{backend,frontend}:pre-ui-rbac-20260803T025500Z`; se retiraron 870.7 MiB de caché recuperable y el disco cerró en 67%/8.1 GiB libres.

- **Seguridad restringida exclusivamente al Administrador y desplegada (2026-08-02):** producción funcional `c393c78`. El moderador `OWNER` ya no recibe el módulo `security` en su matriz de navegación y cualquier navegación persistida/directa se redirige al primer módulo permitido. El backend completo de `/api/admin/security/*` exige ahora `platform_admin`, incluidas la consulta y liberación de cuentas bloqueadas; no existe acceso parcial por workspace para moderadores. Verificación: permisos frontend **15/15**, acceso backend **13/13**, `check:all`, inventario de rutas, endpoint anónimo 401 y `git diff --check` correctos.

- **Login productivo restaurado y CORS endurecido (2026-08-02 07:54–08:20 America/Lima):** producción `2c8fcdb`. El `POST /api/auth/login` y `/api/error-reports` devolvían `500` porque el backend recreado accidentalmente con `docker-compose.yml` + `docker-compose.prod.yml` heredó `CORS_ORIGINS` local (`localhost/:8080`) y rechazaba el origen canónico `https://joinpoint.cloud` antes de validar credenciales. Recuperación inmediata: recreado backend exclusivamente con `docker compose -f docker-compose.prod.yml`; root y `www` vuelven a estar permitidos. Prevención permanente: el Compose local ya no fija CORS, producción falla al arrancar si CORS está vacío, mal formado, contiene rutas o usa HTTP, y un origen hostil responde `403 CORS_ORIGIN_DENIED` controlado en lugar de `500`. Tanto Compose productivo solo como una mezcla accidental resuelven ahora la misma lista HTTPS. Verificación: backend **105 archivos/627 pruebas**, 10 pruebas focalizadas, `check:all`, imagen con **0 vulnerabilidades**, preflight login/error-report `204`, login inválido `401 BAD_CREDENTIALS`, origen hostil `403`, health MySQL/RouterOS/SMTP `ok`, backend/DB healthy, frontend activo, 0 reinicios y WG reciente. `admin@local.app` existe, está verificado, activo, es platform admin y no está bloqueado (0 fallos registrados); no se leyó ni cambió su contraseña. Rollback backend `gestionvpn-10-backend:pre-cors-hardening-20260802T131102Z`; se retiró el rollback backend superado y 2.478 GiB de caché; disco 50%/13 GiB libres.

- **Estado de Seguridad estabilizado y tabla de IPs bloqueadas rediseñada/desplegada (2026-08-02 00:08–00:24 America/Lima):** producción `f48f3b3`. La causa de los `500` era el timeout backend de 5 s mientras el agente recorría 15 jails y materializaba el historial completo; se observaban éxitos de 3.9–4.9 s y abortos a ~5.0 s con `BrokenPipeError`. El agente ahora resume el historial en streaming, ignora las detecciones duplicadas de jails auxiliares y cuenta únicamente eventos reales `[sshd] Found`; el backend usa margen de 10 s, caché compartida de 3 s, consolidación de solicitudes concurrentes, invalidación segura tras mutaciones y devuelve `503 SECURITY_AGENT_TIMEOUT/UNAVAILABLE` controlado en vez de `500`. En producción la primera lectura midió **3.3 s**, la reutilizada **0 ms**, hubo **0 BrokenPipe** nuevos y los conteos dejaron de estar inflados (por ejemplo, 73→10 detecciones SSH únicas). La tabla aplica el patrón visual canónico: encabezado neutro, jerarquía clara, columna `Intentos SSH`, skeletons, estados vacíos, acciones agrupadas y tarjetas móviles ordenadas; bundle productivo confirmado. Verificación: backend completo **104 archivos/622 pruebas** más focalizadas **19/19**, frontend **66/220**, agente **8/8**, `check:all`, build y auditor visual **305/0**. Producción: HTTPS/ruta 200, endpoint anónimo 401 esperado, backend/DB healthy, frontend y agente activos, 15 jails, 0 reinicios, confianza efectiva `134.199.212.232/32`, `179.6.169.75/32`, `213.173.36.232/32`; RouterOS puede aparecer `stale` por no haber escrituras recientes. Respaldo restaurado/verificado `/root/pre-security-table-20260802T051227Z`; rollback `gestionvpn-10-{backend,frontend}:pre-security-table-20260802T051227Z`. Se retiró el rollback anterior y 3.26 GiB de caché; disco 49%/13 GiB libres.

- **Fuentes web del sistema protegidas y desplegadas (2026-08-01 23:19–23:40 America/Lima):** producción `280924c` excluye automáticamente `134.199.212.232/32` (IP pública del VPS) y `213.173.36.232/32` (MikroTik que conecta con el backend) del registro, análisis y escalada de ataques web; ambas se muestran en Administración → Seguridad como “Protegidas por el sistema”, separadas de la confianza administrada y sin acción de retiro. La observación distingue actividad actual/histórica, oculta contadores actuales en cero por defecto y usa “Protección automática activa” al 100%. Fail2ban ignora efectivamente ambas IP en SSH y jails web; también se conserva `179.6.169.75/32` como confianza administrada/protegida. Verificación: backend **104/618**, frontend **66/220**, agente **6/6**, `check:all`, build y auditor visual **305/0**; en producción las dos fuentes retornan `SYSTEM_TRUSTED_SOURCE`, no aparecen en eventos visibles, HTTPS 200, backend/DB healthy, frontend activo, 15 jails, agente y UFW activos, datos 5 usuarios/3 workspaces/14 nodos, 0 reinicios. Respaldo restaurado/verificado `/root/pre-system-trusted-20260802T042255Z`; rollback `gestionvpn-10-{backend,frontend}:pre-system-trusted-20260802T042255Z`. Se retiró el rollback `pre-uuid` ya superado y 3.26 GB de build cache; disco 50%/13 GiB libres. El historial de seguridad no se borró: queda retenido para auditoría, pero las fuentes del sistema se filtran al consultar.

- **Limpieza conservadora del VPS completada (2026-08-01 21:38–21:43 America/Lima):** sin recrear ni detener contenedores, se retiraron siete tags/imágenes de rollback ya superados (`pre-e27184b`, `pre-phase9`, `pre-maintenance` y `pre-db-wait`), la caché APT y el journal excedente. Se conserva exclusivamente el par de rollback vigente `gestionvpn-10-{backend,frontend}:pre-uuid-20260802T022039Z`, sus imágenes actuales, MariaDB, ambos volúmenes activos, el dump restaurado/verificado y los respaldos pequeños de red/SSH/configuración. Journal persistente limitado en `/etc/systemd/journald.conf.d/90-gestionvpn-retention.conf` a 256 MiB, 14 días y 2 GiB libres mínimos; bajó de 592 a 224 MiB. Disco **76%/5.9 GiB → 49%/13 GiB libres**; APT **583 MiB → 12 KiB**; Docker queda con 5 imágenes, 3 contenedores, 2 volúmenes activos y 0 build cache. Cierre: datos **54/5/3/14**, DB/backend healthy, frontend activo, 0 reinicios, rutas 200, servicios/agente/UFW/WireGuard correctos y 0 unidades fallidas. RouterOS puede figurar `stale` sin escritura reciente; handshake y ping reales permanecen correctos.

- **Avisos transitivos de `uuid` resueltos y desplegados (2026-08-01 21:20–21:31 America/Lima):** producción `2c3eb1c`; el monorepo fija npm `12.0.1`, fuerza `uuid 11.1.1` en ExcelJS y las cadenas opcionales Google/Firebase, y mantiene una lista explícita de scripts de instalación revisados. El lockfile cambió sólo las resoluciones necesarias (`uuid`, `js-yaml`, `postcss` y su auxiliar `nanoid`), sin `npm audit fix --force`. Auditoría completa y de producción: **0 vulnerabilidades**; prueba XLSX real nueva cubre el camino interno de UUID. Verificación: backend **103/615**, frontend **65/217**, agente **6/6**, builds Docker local/VPS, módulos Argon2/SSH/Firebase y Nginx correctos. Producción: DB/backend healthy, frontend activo, 0 reinicios, health MySQL/RouterOS/SMTP `ok`, HTTPS/rutas/cabeceras 200, 15 jails, UFW, agente y WireGuard correctos; datos **54/5/3/14**. Respaldo restaurado `/root/pre-uuid-remediation-20260802T022039Z`; rollback `gestionvpn-10-{backend,frontend}:pre-uuid-20260802T022039Z`. Se retiraron 2.56 GB de build cache; disco 76%/5.9 GiB libres. Pendientes humanos sin cambio: Telegram y validación autenticada antes de convertir CSP Report-Only en obligatoria.

- **Fases de mantenimiento 2–9 cerradas y desplegadas (2026-08-01 19:09–20:22 America/Lima):** producción `f9091e9`; backend/DB healthy, frontend activo y los tres con 0 reinicios en el cierre; raíz, legacy, workspace y health 200, con MySQL/RouterOS/SMTP `ok`. Se creó swap persistente de 1 GiB; npm pasó de 11 avisos (3 altos) a 7 moderados y 0 altos/críticos; auditor visual 304/0; CSP Report-Only y Referrer-Policy públicas; Semgrep local/JavaScript/TypeScript/React/secrets/security completó sin hallazgos en las reglas seleccionadas; Docker 29.7.1 y 0 APT pendientes. Durante la actualización se detectó y corrigió la carrera backend↔MariaDB con espera TCP de 120 s y 2 pruebas nuevas; validación final backend 103/615, frontend 64/216 y agente 6/6. Dump `/root/pre-maintenance-20260801T195018` restaurado/verificado con 54 tablas, 5 usuarios, 3 workspaces y 14 nodos; rollback actual y disco 67%/8.1 GiB libres tras retirar 4.27 GB de build cache. Único pendiente excluido por el usuario: vincular Telegram con `platform_admin`; CSP debe pasar de observación a obligatorio sólo después de validar login local/Google autenticado. Informe: `docs/security/VPS_HARDENING_AUDIT_2026-08-01.md`.

- **Protección web al 100% y auditoría integral completadas (2026-08-01 14:30–14:55 America/Lima):** producción checkout `ae5fe5e` con imágenes funcionales de `d25dca4`; backend/frontend/DB 0 reinicios, HTTPS 200, 54 tablas y conteos 5/3/14 intactos. Estado efectivo `enforce_temp`, rollout 100%, confirmaciones temporal e indefinida activas. El primer escáner real (`216.144.249.201`, intentos `/.env`/`.git/*`) quedó `APPLIED` por 1 h en `gestionvpn-web-sensitive` y UFW; 10 reglas REJECT totales al cierre. Auditoría: backend 612/612, frontend 216/216, agente 6/6, `check:all` correcto; puertos externos sólo 22/80/443; SSH, Fail2ban, UFW, HMAC, WireGuard, TLS, Nginx, MySQL y SMTP correctos. Pendientes: 0 `platform_admin` con Telegram enlazado (alerta web sin destinatario), 11 avisos npm (3 altos, 7 moderados, 1 bajo), CSP/Referrer-Policy del shell, swap, 3 paquetes APT, 6 observaciones visuales y Semgrep inconcluso por timeout. Informe: `docs/security/VPS_FULL_AUDIT_2026-08-01.md`.

- **Fase 9 iniciada en producción, observación 0% (2026-08-01 14:21 America/Lima):** producción avanzó linealmente a `d25dca4` usando exclusivamente `docker-compose.prod.yml`. Respaldo root-only `/root/pre-phase9-20260801T191019Z`; dump restaurado en MariaDB aislada con 50 tablas, 5 usuarios, 3 workspaces y 14 nodos coincidentes, SHA-256 `365b4d98550abdcbc56a97d90c7be6247dbe782cf51ea247739cb06d7334fd42`. Rollback: `gestionvpn-10-{backend,frontend}:pre-phase9-20260801T191019Z`. Tras migrar hay 54 tablas y los conteos críticos siguen intactos. Backend/frontend/DB tienen 0 reinicios; HTTPS 200, Nginx, MySQL, SMTP, agente, Fail2ban, UFW y WireGuard correctos. RouterOS alterna `ok/stale` por tiempo desde la última escritura, con handshake WG vigente; no tocar RouterOS. Hay 15 jails y se reconciliaron dos bans históricos faltantes en UFW (`171.25.158.87`, `45.148.10.183`), quedando 9/9 efectivos. Protección web: `OBSERVE_ONLY`, rollout 0%, confirmaciones temporal/indefinida vacías; 0 acciones automáticas. Disco 64%/8.8 GiB libres. Pendiente antes de canary 10%: observar al menos 48 h, analizar falsos positivos y vincular/verificar Telegram.

- **Fases originales 7–8 cerradas localmente; sin despliegue (2026-08-01):** Administración → Seguridad queda separada en IPs bloqueadas, usuarios bloqueados, direcciones confiables, actividad reciente y políticas automáticas. El `platform_admin` conserva todas las operaciones de Seguridad; desde 2026-08-02 el moderador `OWNER` no ve ni puede operar este módulo. La actividad combina acciones manuales y automáticas y filtra Login, API sensible, Escaneo, SSH y Reincidencia. Fail2ban separa `gestionvpn-web-auth`, `gestionvpn-web-rate`, `gestionvpn-web-scan`, `gestionvpn-web-sensitive` y `gestionvpn-web-recidive`, más el nivel scan 24 h y el jail legacy de compatibilidad. Todos usan UFW y bloquean la IP en los puertos públicos 22/80/443; auth y recidive son indefinidos. Se conservan HMAC, anti-autobloqueo, confianza, IPv4/IPv6, auditoría y Telegram. Validación: backend **102/612**, frontend **64/216**, `check:all`, agente **6/6**, `py_compile` y diff correctos. Único bloque funcional pendiente del plan: Fase 9, despliegue gradual y validación. Producción sigue en `e27184b`; no desplegar aún.

- **Fases originales 3–6 cerradas localmente; sin despliegue (2026-08-01):** la clasificación ya separa una cuenta conocida con contraseña olvidada, fuerza bruta sobre ≥3 identidades, ≥10 intentos con identidades inexistentes y una cuenta que vuelve a bloquearse después de recuperación/desbloqueo. Sólo los 429 de autenticación cuentan: 20/10 min → 1 h; el tercer bloqueo web temporal en 7 días puede pasar a indefinido con la confirmación independiente. Escaneo: 30 solicitudes/5 min y ≥10 rutas distintas → 6 h, segundo episodio/7 días → 24 h, tercero → indefinido. Los endpoints sensibles cubren login local/Google, registro, verificación, recuperación, invitaciones, reautenticación y Administración; distinguen credenciales, tokens, sesión, permisos y frecuencia. Tres errores legítimos aislados no bloquean: las rutas hostiles requieren 3/10 min; endpoints reales, 10 fallos y ≥2 rutas/10 min. Health, loopback, IPs administrativas y direcciones/CIDR confiables quedan excluidos. Agente acepta únicamente los jails web fijos 1 h/scan 6 h/scan 24 h/indefinido y revalida confianza/autobloqueo. Validación: backend 101/603, frontend 64/216, `check:all`, agente 6/6, `py_compile` y diff correctos. Pendientes: Fases originales 7–8 (administración unificada y jails separados por vector) y Fase 9 gradual. Producción sigue en `e27184b`; no desplegar aún.

- **Plan original de nueve fases: Fases 1 y 2 cerradas localmente; producción intacta (2026-08-01):** tras contrastar el código con el plan original del adjunto, no se considera terminado el conjunto de nueve fases. La Fase 1 original quedó cerrada en `9de5b89`: retención de incidentes web por 90 días, registro seguro de 401/403/404/429, distinción entre 404 de recurso conocido y escaneo de ruta desconocida, contadores, decisión automática y evidencia resumida enlazada a la acción. La Fase 2 original quedó en `14f0b5a`: inicio real del bloqueo, IP reciente, estado separado del bloqueo global de IP, desbloqueo administrativo que limpia cuenta + límites de identidad y usuario/IP sin tocar el límite global, y recuperación de contraseña que libera también la identidad. Validación Fase 2: backend 101/593, frontend 64/216, `check:all` y `git diff --check`. Producción continúa en `e27184b`; no desplegar todavía.

- **Preflight de protección web en GO técnico, pendiente autorización de despliegue (2026-08-01):** producción continúa en `e27184b`; código de fases 1–5 publicado y avance lineal. Se creó `/root/pre-web-security-20260801T171455Z/vpn_manager.sql.gz`, gzip/SHA-256 válido, y se restauró en MariaDB aislada con 50 tablas, 5 usuarios, 3 workspaces y 14 nodos idénticos; temporales retirados. Se eliminaron sólo ocho tags antiguos y 2.366 GB de build cache; current, `pre-e27184b` y MariaDB conservaron sus IDs. Disco 87%/3.4 GiB → 43%/14 GiB. Las tres copias de entorno quedaron 600; `.save` y `.save.1` difieren del activo y se preservaron. Cierre: HTTPS/health 200, backend/DB healthy, 0 reinicios, Docker/Fail2ban/WireGuard/agente activos, 8 jails y datos intactos. RouterOS seguía `stale` pero WireGuard tenía handshake reciente; revalidar/no desplegar durante operación de túnel. No mezclar 3 updates APT pendientes. Siguiente acción requiere autorización explícita: desplegar exclusivamente con compose de producción en `observe`/rollout 0%. Informe: `docs/security/WEB_SECURITY_PREDEPLOY_REPORT_2026-08-01.md`.

- **Fases 1–5 de protección web terminadas y publicadas, sin despliegue (2026-08-01; cierre `4d20246`):** fases 1–4 cubren bloqueo reversible de cuentas, observación, bloqueos temporales idempotentes y escalada indefinida sólo por abuso distribuido o tercer episodio separado en 7 días. Fase 5 añade rollout determinista por IP de 0–100%; `0`, modo `observe` o un porcentaje inválido actúan como kill switch. El avance previsto es 10→25→50→100 y la escalada indefinida se habilita sólo después de estabilizar el temporal. Acciones automáticas aplicadas/fallidas notifican una vez a cada chat de administrador activo, enlazado y no pausado, y se muestran como actividad reciente responsive en Seguridad. Todos los ejemplos quedan en observación/0%. Runbook respaldado y reversible: `docs/security/WEB_SECURITY_ROLLOUT_RUNBOOK.md`; producción sólo con `docker compose -f docker-compose.prod.yml`. Verificación final: `check:all`, backend **100/582**, frontend **64/216**, agente Python **5/5**, `py_compile` y auditor visual sin nuevos hallazgos en Seguridad (quedan 5 preexistentes ajenos). Producción continúa intacta; el siguiente paso es el informe predespliegue y autorización explícita.

- **Seguridad VPS con reincidencia automática indefinida desplegada (2026-08-01; producción/agente `e27184b`):** `sshd` conserva la escalada 1h→2h→4h… con máximo 1 día; el jail manual `gestionvpn-7d` sólo corresponde a selecciones administrativas. El nuevo `gestionvpn-recidive` cuenta exclusivamente bans automáticos de `sshd` —no acciones manuales— y aplica `bantime=-1` tras **3 bans separados dentro de 7 días** (mínimo práctico 15 fallos). El administrador puede desbloquearlo; un ataque posterior puede reactivarlo. La UI lo etiqueta `Fail2ban · Reincidente`, muestra `Sin vencimiento` y oculta la acción redundante de conversión. Seis IP históricas calificaron y quedaron indefinidas; la copia `sshd`/manual de `45.148.10.183` fue retirada después de confirmar el nuevo bloqueo. Producción: health/HTTPS 200, 8 jails, Nginx/agente/Fail2ban correctos y frontend/backend/DB con 0 reinicios. Validación: regex 94/94 bans SSH, 10/10 pruebas focalizadas, TypeScript+Vite y `py_compile`. Respaldo `/root/pre-recidive-e27184b`; imágenes `gestionvpn-10-{backend,frontend}:pre-e27184b`. Producción debe desplegarse exclusivamente con `docker compose -f docker-compose.prod.yml`.

- **Módulo administrativo de Seguridad VPS desplegado (2026-07-31; producción funcional `a5e0358`):** sólo `platform_admin` puede consultar los 7 jails de Fail2ban, ver bloqueos/fechas/vencimiento/intentos SSH, desbloquear, bloquear manualmente por 15m/1h/6h/24h/7d/indefinido y administrar IP/CIDR confiables permanentes. Las acciones requieren categoría, motivo y reautenticación de un solo uso por contraseña o Google; hay rate limit, protección doble contra autobloqueo, máximo /24 IPv4 y /64 IPv6, confirmación adicional para CIDR/indefinido, compensación DB↔Fail2ban, auditoría 365 días y Telegram. El backend sin privilegios usa un agente root HMAC en `127.0.0.1:8788`; el secreto real coincide fuera de Git en el agente y `server/.env.production`. La lista administrada se escribe en `zz-gestionvpn-trusted.local` para cargarse después de overrides existentes; `179.6.169.75` quedó confirmada en el `ignoreip` efectivo de `sshd`. Producción: dump restaurado **47/47** antes del cambio, migración a **50 tablas**, datos críticos preservados (**14 nodos, 5 usuarios, 3 workspaces**), backend healthy/frontend/DB activos con 0 reinicios, backend→agente 7/7 jails, bundle Security 200, endpoint anónimo 401, HTTPS/health `ok`, SSH/Fail2ban/Docker/WireGuard activos y disco 67% tras limpiar sólo build cache. Respaldo root-only `/root/pre-security-admin-20260731T230425Z`; rollback `gestionvpn-10-{backend,frontend}:pre-security-20260731T230425Z`. Verificación local: **backend 95/556**, **frontend 64/216**, build, ESLint, agente, contratos e inventario correctos. Pendiente humano: validar una acción real autenticada y recepción Telegram desde la pantalla.

- **Autosync remoto backend-only desplegado (2026-07-31; producción `69c368f`):** altas y ediciones comparten `lib/remoteNetworkSync.js`; CIDR IPv4 se valida/canonicaliza con `lib/ipv4Cidr.js`, se bloquea `0.0.0.0/0`, y las mutaciones de address-list/rutas se serializan y abortan si no puede leerse RouterOS. La edición carga `lan_subnets` completa, deduplica altas, elimina sólo rutas del VRF y preserva siempre `LIST-NET-REMOTE-TOWERS`/AllowedIPs globales. `lib/wg0Intent.js` sincroniza inmediatamente altas/ediciones; la intención se escribe atómicamente y `wg0Reconcile` se ejecuta al arrancar y cada 10 minutos. Producción: backend nuevo healthy y sin reinicios, DB/frontend no recreados, migraciones idempotentes OK, reconciliación sin pendientes, watcher/timer/WireGuard activos, ruta pública real por `wg0`, 47 tablas/14 nodos conservados y cero errores backend. Respaldo `/root/pre-network-sync-20260731T172817Z`; rollback `gestionvpn-10-backend:pre-69c368f-20260731T172817Z`.

- **Endurecimiento SSH, Fail2ban y actualización del VPS completados por fases (2026-07-30):** se creó y probó primero el usuario alternativo `vpsadmin` con dos llaves independientes y `sudo` no interactivo; después se deshabilitaron el acceso SSH directo de `root`, contraseñas y keyboard-interactive, dejando únicamente clave pública, `AllowUsers vpsadmin`, `MaxAuthTries 3` y `LoginGraceTime 30`. `sshd -t`, acceso principal, fallback y rechazo de root/password se verificaron antes de retirar el rollback automático. Fail2ban quedó habilitado con jail `sshd`, backend systemd, acción UFW, 5 intentos/10 min y ban incremental desde 1 h hasta 1 día. Se instalaron todas las actualizaciones disponibles (**21 actualizados, 2 nuevos, 0 eliminados, 0 pendientes**) y se reinició al kernel `5.15.0-186-generic`; indisponibilidad HTTPS observada ~56 s. El plugin manual de Compose v5.1.0 que ocultaba el paquete actualizado se movió al respaldo; Docker usa ahora Compose v5.3.1 administrado por APT. Tras el reinicio: SSH/Fail2ban/Docker/`wg-quick@wg0` activos, 0 unidades fallidas, túnel con handshake/ruta/ping correctos, DB y backend healthy, frontend arriba, raíz y health HTTPS 200, puerto 8080 cerrado y dump/checksums intactos. El backend hizo 6 intentos transitorios durante los arranques de MariaDB/Docker y luego quedó estable y healthy, sin errores nuevos. Respaldo root-only: `/root/ssh-hardening-20260730T050400Z/system-config.tar.gz` (SHA-256 `1677ba36a7a5b5b0b626ce6d350f53bc478fd013874d68fec9f7f4daf36d288d`); el binario manual retirado quedó como `docker-compose-v5.1.0.manual.bak` en el mismo directorio. Acceso local canónico: `ssh gestionvpn-vps`; rutas y fingerprint están en el `.env` ignorado, pero la llave privada permanece exclusivamente en `%USERPROFILE%\.ssh`.

- **Limpieza segura del VPS completada (2026-07-30; sin indisponibilidad):** antes de borrar se creó `/root/pre-cleanup-20260730T044235Z/vpn_manager.sql.gz`, se validó gzip/checksum y se restauró en una MariaDB temporal aislada con **47/47 tablas** y conteos críticos coincidentes. Se conservaron las imágenes actuales, el par de rollback `pre-c396428-20260730T032852Z`, `/root/domain-migration-20260730T025213Z`, `/root/pre-dm-routing-20260730T032852Z` y el dump nuevo. Se retiraron el volumen SQLite no montado `gestionvpn-10_vpn-data`, la copia antigua `.env.production.bak-before-gemini`, el respaldo de certificado supersedido del 2026-07-25, 10 imágenes/tags obsoletos, 3.23 GB de build cache, caché APT y 1.9 GB de journal; el journal quedó en 496 MB. UFW ya no permite `8080/tcp` en IPv4 ni IPv6; SSH/80/443 y las restricciones de 3001 permanecen. Disco raíz: **77% → 47%** (19 GB → 12 GB usados). Frontend/backend/DB conservaron sus image IDs, cero reinicios, MariaDB viva y HTTPS/API health 200.

- **Recuperación de URLs de workspace obsoletas implementada (2026-07-29; `6a13563`, pendiente de deploy):** al restaurar una sesión, una URL guardada con un slug anterior o distinto ya no muestra un 404 intermitente si el módulo es válido. El frontend reemplaza el slug exclusivamente con el `workspace_slug` autorizado por el servidor y conserva el módulo solicitado; las rutas con módulos realmente desconocidos continúan en 404. No cambia autorización, API, backend ni BD. Verificado con regresión específica **11/11**, frontend completo **64 archivos / 215 pruebas**, build Vite, TypeScript, ESLint, `check:all` e inventario de seguridad de rutas.

- **Dominio y URLs por workspace desplegados (2026-07-30; producción `e465c88`):** `joinpoint.cloud` y `www` resuelven al VPS `134.199.212.232`; `www` redirige al dominio raíz y HTTPS usa el certificado Let's Encrypt válido también para `134-199-212-232.nip.io` hasta 2026-10-28. La entrada pública es `https://joinpoint.cloud/`; después de autenticar, la navegación canónica es `/dm/<workspace_slug>/<module>`. `workspace_slug` es estable y único, se genera server-side y se valida contra la sesión autorizada; la migración de producción completó 3/3 workspaces, sin faltantes ni duplicados. La ruta histórica `/GestionVPN-1.0/` sirve el shell con `no-cache` para romper bucles de navegadores que guardaron los 301 anteriores. `APP_BASE_URL=https://joinpoint.cloud/`; CORS y Firebase conservan los dominios autorizados. Producción: frontend/backend/DB activos, backend/DB sin reinicios, Nginx válido, raíz/legacy/`/dm/prueba/nodes` HTTP 200 y login verificado en navegador. RouterOS puede aparecer `stale` sin implicar fallo web; MySQL y SMTP están `ok`. Respaldo operativo de dominio: `/root/domain-migration-20260730T025213Z`; respaldo previo al cambio de rutas: `/root/pre-dm-routing-20260730T032852Z`; rollback de imágenes `gestionvpn-10-backend:pre-c396428-20260730T032852Z` y `gestionvpn-10-frontend:pre-c396428-20260730T032852Z`. Commits: `c396428`, `2a367d7`, `e465c88`. Verificación local: frontend 64/212, backend 89/536, routing 10/10, slug 4/4, build, TypeScript, ESLint, `check:all` y Nginx. Pendiente humano: login real y popup Google.

- **Disponibilidad dinámica de métricas CPE desplegada (2026-07-29; producción funcional `7e848c1`):** sobre 71 snapshots de producción, `CCQ`, `AM Qual` y `AM Cap` tienen valores en 12 CPE XW/M-series; `DL Cap` y `UL Cap` tienen valores en 45 CPE, principalmente AP WA 8.5.7. Como dependen de familia/firmware, no se retiran globalmente: cada tabla oculta automáticamente sólo las métricas cuya cobertura sea 0 en la antena abierta. El selector las conserva deshabilitadas con “Sin datos en esta antena”; valores numéricos `0` cuentan como disponibles y la carga sin estaciones no oculta prematuramente. Encabezados y filas comparten el mismo conjunto efectivo para evitar desplazamientos. También quedó desplegado el retiro visual de “Última IP” y las correcciones previas de geometría. Sin cambios en backend, BD, CSV ni informes. Local: **64 archivos / 209 pruebas frontend**, ESLint, TypeScript, build Vite y `git diff --check`. Producción: checkout exacto, Nginx válido, HTTPS y bundle 200, marcador funcional presente, health MySQL/RouterOS/SMTP `ok`, backend/BD sin reinicios. Rollback frontend: `gestionvpn-10-frontend:pre-7e848c1-20260729`.

- **Columna “Última IP” retirada de la tabla CPE (2026-07-29; pendiente de deploy):** la vista Estado de antenas ya no presenta la IP aprendida por AirOS porque puede llegar como `0.0.0.0`; “Distancia (m)” permanece disponible. Encabezado, celdas y selector comparten ahora una única definición central para evitar desalineaciones. El dato sigue disponible internamente para diagnóstico, búsqueda, detalle y exportación; no cambia backend ni BD. Verificado con prueba focalizada **3/3**, ESLint sin warnings, TypeScript y build Vite.

- **Nombre de sitio y encabezados CPE corregidos (2026-07-28; `079910d`, pendiente de deploy):** la tarjeta de cada sitio reserva una primera fila para nombre/estado y mueve métricas/acciones a una segunda fila flexible, evitando que los controles oculten el nombre. Las pistas de la tabla CPE ahora respetan mínimos legibles y cada título queda contenido dentro de su celda, sin invadir la columna siguiente; se conservan selector y scroll horizontal. Cambio exclusivamente visual/frontend; `0.0.0.0` permanece sin cambios por decisión del propietario. Verificado con **64 archivos / 206 pruebas frontend**, build Vite, `check:all`, TypeScript, ESLint y `git diff --check`.

- **Diagnóstico visual y de IP en Estado de antenas (2026-07-28; sólo lectura):** el nombre del sitio desaparece porque la cabecera flexible permite que el bloque de métricas/acciones `shrink-0` consuma el ancho disponible. En la tabla CPE, `Estado` usa una pista de sólo 28 px aunque su etiqueta requiere más y carece de contención, por lo que invade `MAC / Host`; varias pistas también son demasiado estrechas para sus títulos. Producción confirma que los AP `192.168.30.235` y `.238` (WA 8.7.11) entregaron siete estaciones con `lastip="0.0.0.0"`; **todas son XW con AirOS 6.1.7**, mientras estaciones WA recientes y una XW 6.3.24 conectadas al mismo AP sí reportan IP válidas. Esto acota la causa a la información de IP aprendida/reportada por AirOS en esa combinación antigua XW/WA, no al frontend ni a un valor fabricado por el parser. El parser y la BD preservan literalmente el sentinel, sobrescriben cualquier IP anterior y la UI lo presenta como enlace. `0.0.0.0` debe tratarse como dato ausente; para recuperar una IP real se debe probar `wstalist -p` y contrastar por MAC con ARP/neighbors y DHCP cuando el túnel esté activo, preservar la última IP válida y registrar fuente/frescura. No inferir ni inventar direcciones.

- **Checkpoints 1–4 de rendimiento desplegados (2026-07-28; producción `46ff33d`):** precarga autorizada de módulos, métricas de navegación, gzip/caché HTTP y consultas compartidas de equipos/Sitios están activos. `Sitios`, `Buscar equipos` y `Estado de antenas` reutilizan inventarios segmentados por workspace/usuario/rol/alcance; se retiraron la carga independiente de Sitios en Buscar equipos y la recarga fija a los 2 segundos. El contexto VPN permanece como fuente operativa del túnel. Producción: backend/frontend reconstruidos, DB no recreada, HTTPS y health 200, MySQL/RouterOS/SMTP `ok`, Nginx válido, HTML `no-cache`, JS con gzip+caché inmutable y cero reinicios. Rollback: `gestionvpn-10-backend:pre-46ff33d-20260728` y `gestionvpn-10-frontend:pre-46ff33d-20260728`.

- **Checkpoint 3 de caché compartida implementado (2026-07-28; `c8d5a86`, pendiente de deploy):** TanStack Query mantiene un cliente exclusivamente en memoria por combinación workspace/usuario/rol/alcance; al cambiar ámbito o cerrar sesión cancela consultas y destruye la caché. `Buscar equipos` y `Estado de antenas` comparten una única consulta `device-inventory` con vigencia de 45 s y GC de 5 min. La frontera `loadInventory` y las mutaciones eliminan siempre `sshPass`; sólo Buscar equipos hidrata una copia local desde `credCache` efímero. Crear/editar/eliminar/mover actualiza la caché exacta; una revalidación fallida conserva datos anteriores y muestra aviso. Pruebas cubren deduplicación, aislamiento, ausencia de secretos, cancelación y stale-while-revalidate. Verificado con **62 archivos / 200 pruebas frontend**, build Vite, `check:all`, TypeScript, ESLint y `git diff --check`. No cambia backend ni BD.

- **Checkpoint 2 de rendimiento HTTP implementado (2026-07-28; `163715a`, pendiente de deploy):** Nginx comprime con gzip JS, CSS, JSON, XML y SVG; `text/event-stream` queda excluido para no interferir con SSE. `index.html` y los fallbacks SPA usan `no-cache, must-revalidate`, mientras assets versionados conservan caché anual inmutable. Se preservan explícitamente HSTS, X-Frame-Options y nosniff en las ubicaciones con Cache-Control. Prueba real con Nginx 1.27: `Buscar equipos` bajó de **140,423 a 34,069 bytes** (~76%); `Vary`, gzip, caché y seguridad confirmados. `nginx -t`, `check:all`, TypeScript, ESLint y `git diff --check` correctos. No cambia backend, datos ni SSE.

- **Checkpoint 1 de rendimiento implementado (2026-07-28; `ac47312`, pendiente de deploy):** los módulos lazy comparten un registro canónico y el menú precarga sólo vistas autorizadas por hover, foco o touch; en tiempo ocioso adelanta únicamente la pareja frecuente `Sitios/Buscar equipos`. Una marca Performance mide desde el clic hasta que el contenido queda confirmado dentro de `Suspense`. Nginx incorpora un access log operacional con URI sin query, estado, tiempo total/backend y bytes, sin IP, usuario, cookies ni payload. No cambia APIs, caché de datos, SSE, backend ni permisos. Verificado con **59 archivos / 194 pruebas frontend**, build Vite, `check:all`, ESLint, TypeScript, `git diff --check` y `nginx -t` real.

- **Plan de rendimiento multiusuario en implementación (2026-07-28):** [`docs/PERFORMANCE_NAVIGATION_PLAN.md`](docs/PERFORMANCE_NAVIGATION_PLAN.md) organiza la solución en fases reversibles: observabilidad, precarga de chunks, compresión, TanStack Query segmentado por workspace/usuario/alcance, migración incremental, SSE centralizado, limpieza de sesión, UX localizada, pruebas multiusuario y despliegue gradual. Incluye escenarios de múltiples pestañas, cambios de workspace/rol, 401/403, offline, eventos duplicados/fuera de orden, chunks antiguos, concurrencia y rollback. Invariante: secretos fuera de la caché y autorización siempre server-side.

- **Diagnóstico de transición lenta entre vistas (2026-07-28; sólo lectura):** cada módulo se carga mediante `React.lazy` sin precarga al pasar por el menú; en producción los primeros accesos descargan `Sitios` 157 KB, `Buscar equipos` 140 KB y `Estado de antenas` 120 KB. Nginx aplica caché inmutable, pero no compresión gzip/Brotli (`Content-Encoding` ausente), por lo que el primer acceso y cada hash/origen nuevo transfieren el archivo completo. Al cambiar de vista, los módulos se desmontan y vuelven a consultar: `Buscar equipos` y `Estado de antenas` ejecutan `/api/db/devices`; los logs confirman alternancia de esas solicitudes incluso con respuesta 304. `Sitios` restaura caché, pero agenda un `POST /api/nodes` a los 2 segundos de cada montaje. La animación global dura sólo 300 ms y es secundaria. Recomendación pendiente de implementar: precargar chunks al hover/focus y en tiempo ocioso, habilitar compresión, compartir/deduplicar caché de equipos entre vistas y convertir las revalidaciones en segundo plano sin bloquear el contenido.

- **Monitor de antenas simplificado para usuario final (2026-07-28; `c886844`, pendiente de deploy):** la vista ahora se presenta como `Estado de antenas`, habla de sitios, antenas, clientes, actualización e información en lenguaje cotidiano y elimina el botón duplicado de recarga. Los filtros describen correctamente `Sitio conectado/Otros sitios`, los estados distinguen `En línea/Requiere atención/Actualizando/Sin información`, y las acciones usan `Actualizar información`, `Ver clientes`, `Descargar informe`, `Datos visibles` y `Credenciales requeridas`. Las filas incorporan una tarjeta móvil dedicada y controles táctiles de al menos 44 px; el informe PDF adopta la misma terminología. No cambia APIs, backend, persistencia ni lógica de monitoreo. Verificado con **57 archivos / 188 pruebas frontend**, `check:all`, TypeScript, ESLint y `git diff --check`.

- **Persistencia SSH de equipos endurecida (2026-07-28; `a37bf1f`, pendiente de deploy):** un escaneo restaurado ya no conserva `success/pending` sin una credencial recuperable; la rehidratación sólo repone `success` después de recuperar usuario/clave/puerto. Guardado rápido/masivo recupera la credencial por aliases de ID, IP, MAC LAN y WLAN o abre confirmación sin persistir; sincronizar conserva credenciales recién validadas. Backend rechaza un equipo nuevo con usuario pero sin clave, permite actualizaciones si ya existe clave cifrada y preserva usuario/puerto existentes. Contraseña vacía validada se distingue de ausencia y se cifra; al borrar se purgan todos los aliases en memoria. Caso productivo previo `142.152.7.237` permanece sin clave y requiere reescaneo/confirmación. Verificado con **57 archivos / 188 pruebas frontend**, **88 archivos / 532 pruebas backend**, TypeScript, ESLint, build Vite, `check:all`, inventario de rutas y `git diff --check`.

- **Modal de credenciales de acceso compacto, ordenable y desplegado (2026-07-28; `a1c545f`):** cada credencial ocupa una sola fila en escritorio y conserva apilado responsivo en móvil. Los controles `Subir/Bajar` permiten cambiar el orden en que se probarán y el guardado existente persiste esa secuencia; los extremos se deshabilitan y todas las acciones conservan áreas táctiles accesibles. Cambio exclusivamente visual/frontend. Verificado con **55 archivos / 183 pruebas frontend**, ESLint, TypeScript/build Vite y `git diff --check`. Producción reconstruyó sólo `vpn-frontend`: HTTPS 200, bundle nuevo confirmado, frontend con 0 reinicios y backend/DB sin recrear. MySQL/SMTP `ok`; RouterOS reportó `stale` por antigüedad de última escritura, mientras `/api/nodes` continuó respondiendo 200. Rollback: `gestionvpn-10-frontend:pre-a1c545f-20260728`.

- **Vista de Sitios compacta y orientada al usuario (2026-07-28; `2194fdd`, pendiente de deploy):** encabezado reducido a `Sitios remotos`, muestra hora de última actualización y usa `Actualizar`; búsqueda, filtros `Conexión/Estado`, `Columnas`, descarga y contador comparten una barra compacta, con chips sólo si hay filtros activos. `Conectar` es una acción primaria índigo; el menú habla de `Administrar/Consultar`, `Editar sitio`, `Acceso a equipos`, `Ver configuración`, `Actividad`, `Comprobar conexión`, `Organizar` y `Eliminar sitio`. Primera carga usa skeleton; errores ofrecen reintento y estados vacíos explican la acción siguiente. Invariante: selector, orden, columnas opcionales y preferencias persistidas permanecen intactos. No cambia backend/API/datos.

- **Ventanas de acciones de Sitios unificadas y desplegadas (2026-07-28; `b5fd7d0`):** `SiteModalHeader` es la cabecera canónica índigo para editar, configurar, consultar actividad, comprobar conexión, organizar etiquetas, administrar credenciales y eliminar un sitio. Verde/ámbar/rojo se reservan para éxito/advertencia/peligro dentro del contenido, no para identificar ventanas. Los títulos usan `Sitio`, la terminología técnica visible se simplificó y el bloque de configuración usa superficie clara profesional en modo claro (pizarra sólo en modo oscuro). `Credenciales SSH` permanece como modal accesible, responsivo y con aviso de cambios sin guardar. No cambia API ni backend. Verificado con **54 archivos / 180 pruebas frontend**, TypeScript, ESLint y build Vite; `audit:design` no introduce hallazgos y conserva sólo los 2 preexistentes de `RouterAccess.tsx`. Producción reconstruyó sólo `vpn-frontend`: HTTPS 200, health MySQL/RouterOS/SMTP `ok`, 0 reinicios y bundle nuevo confirmado. Rollback: imagen `gestionvpn-10-frontend:pre-b5fd7d0-20260728`.

- **Vista de Nodos simplificada para usuario final (2026-07-27; `7b3a6db` desplegado mediante `dab950b`):** cambio exclusivamente visual/frontend, sin modificar API, contratos, túneles, permisos ni datos. La interfaz habla de `Sitios`, `Conectar`, `Desconectar`, `En línea`, `Buscar equipos`, `Mi equipo` y `Estado de antenas`; corrige singular/plural y reemplaza acciones/controles por textos cotidianos. La vista inicial oculta VRF, LAN, IP de túnel, PPP y protocolo mediante migración local de preferencias v1→v2; siguen disponibles bajo `Datos visibles` con etiquetas comprensibles. El modal de alta se presenta como `Agregar sitio remoto`. Verificado con **53 archivos / 178 pruebas frontend**, TypeScript, ESLint, build Vite y `git diff --check`; bundle productivo confirmado. `audit:design` conserva únicamente 1 error + 1 warning preexistentes en `RouterAccess.tsx`, ajenos al cambio.

- **Lease robusto y revocación automática de túneles desplegados (2026-07-27; `7b3a6db` mediante checkout `dab950b`):** el navegador renueva cada minuto un lease server-authoritative de 5 minutos y actualiza su expiración local; un `UPDATE` condicional impide resucitar sesiones ya vencidas. Logout/logout-all revocan desde backend antes de invalidar la sesión web. Botón, logout, consulta de estado y job comparten una revocación idempotente deduplicada por usuario; usuarios distintos progresan en paralelo y el job procesa lotes de 3 cada 30 segundos. La mangle `ACCESO-USER-*` se elimina en MikroTik antes de cerrar MySQL; si RouterOS falla, la fila queda `ACTIVE` y se reintenta. Verificado con **86/527 backend + 53/178 frontend**, `check:all`, build y `git diff --check`. Producción: backend/frontend reconstruidos sin reiniciar DB ni añadir migraciones; HTTPS/TLS y health 200, MySQL/RouterOS/SMTP `ok`, job de expiración a 30 s, bundle nuevo, cero reinicios y sin errores posteriores al arranque. Imágenes rollback: tag `pre-dab950b-20260727`.

- **Informe integral por nodo en Monitor AP y consistencia del monitoreo (2026-07-27; desplegado en producción mediante `8e0b0a8`):** cada tarjeta de nodo incorpora `Informe`, que genera bajo demanda un PDF A4 horizontal sin credenciales con KPIs, frescura/calidad del snapshot, inventario, ficha técnica completa de cada AP y sus CPE con RF, tasas, airMAX, distancia, uptime y salud. Un nodo sólo aparece `Online` cuando todos sus AP están actualizados; un poll exitoso con 0 CPE es válido, los snapshots de más de 5 minutos quedan `Parcial`, y eventos seed/SSE/manual fuera de orden ya no pisan datos nuevos. La sincronización masiva usa lotes de 3 y bloquea reentradas. Fallos al cargar/eliminar equipos ya no simulan inventario vacío ni éxito: backend confirma antes de cambiar la UI/cachés. PDF renderizado e inspeccionado sin recortes ni páginas vacías; verificado con **84/521 backend + 52/177 frontend**, build Vite, `check:all` y `git diff --check`. Se reconstruyó sólo `vpn-frontend`, sin respaldo adicional por decisión del propietario, migraciones ni reinicios de backend/DB; HTTPS y health 200, MySQL/RouterOS/SMTP `ok`, cero reinicios y sin errores recientes. Rollback Git: `673d0d1` + rebuild sólo frontend.

- **Guardado de equipos AirOS corregido y endurecido (2026-07-26; desplegado en producción mediante `8e0b0a8`):** el `POST /api/db/devices` ya no recibe los `null` que AirOS usa para métricas ausentes (`chains`, MACs, seguridad y modo de red); la frontera de persistencia los omite y no envía `cachedStats`. `deviceDb.saveSingle` verifica HTTP + `success`, propaga código/campos de validación y actualiza IndexedDB/credenciales sólo después de la confirmación del backend. Escanear ya no marca falsos guardados: individual y masivo esperan confirmación, muestran progreso, conservan fallidos seleccionados y sólo cuentan éxitos reales; Monitor AP aplica el mismo orden transaccional. La IP pública global del script de nodo es de solo lectura para el moderador y deja de provocar `POST /api/settings/save` 403. Verificado: **50 archivos / 167 pruebas frontend**, build Vite, ESLint completo, `check:all` y `git diff --check`; `audit:design` mantiene únicamente 1 error + 1 warning preexistentes en `RouterAccess.tsx`.

- **Selector de columnas de Escanear estabilizado (2026-07-26; desplegado en producción mediante `8e0b0a8`):** el menú ya no se cierra al desplazarse internamente de abajo hacia arriba ni al mostrar una columna. Usa la altura real disponible calculada por `useKebabMenu` y `overscroll-contain` evita que, al alcanzar un extremo, el navegador transfiera el scroll a la página y dispare el cierre externo. El cierre por scroll realmente externo se conserva. Prueba de componente cubre selección, scroll interno, contención y cierre externo. Verificado: **162/162 frontend**, ESLint completo, TypeScript, build Vite y `git diff --check`.

- **Revocación de túnel y consola de nodo corregidas (2026-07-26; desplegada en producción mediante `8e0b0a8`):** al confirmar una revocación local, el panel de mensajes se cierra inmediatamente; si el túnel expira, se revoca desde otra pestaña/SSE o se cambia a otro nodo, también se eliminan los mensajes de activación obsoletos. Mientras se revoca, los mensajes anteriores se reemplazan por el progreso actual. Si backend, proxy o MikroTik fallan, el estado activo ya no se limpia ni se muestra un falso éxito: el error queda visible y la expiración reintenta sin solicitudes simultáneas. Los mensajes pueden cerrarse manualmente cuando no hay una operación activa y los temporizadores antiguos ya no borran mensajes de una operación nueva. Verificado: **160/160 frontend**, ESLint completo, TypeScript, build Vite y `git diff --check`.

- **Menú de acciones de Nodos corregido (2026-07-26; desplegado en producción mediante `8e0b0a8`):** el dropdown portal ya no usa una altura estimada de `280px` ni `max-height:70vh` desconectado de su ancla. `useKebabMenu` calcula espacio real arriba/abajo, abre hacia el lado con más capacidad, limita `maxHeight` al viewport con margen y ajusta la posición horizontal. El scroll dentro del propio menú ya no dispara el listener global que lo cerraba; scroll externo y resize sí lo cierran. Añadidas 6 pruebas de geometría/scroll. Verificado: **152/152 frontend**, ESLint, TypeScript, build Vite y `git diff --check`; `audit:design` conserva 1 error + 1 warning preexistentes en `RouterAccess.tsx`, ajenos a este cambio.

- **Enlace Google bloqueado por dependencia popup omitida (2026-07-25; fix `55816fa` publicado, sin deploy):** la Web API Key, Web App, dominio y proyecto `vpn-noc` sí coinciden; Identity Toolkit devuelve el número interno del mismo proyecto, no otro proyecto. Google está habilitado. La causa real es `initializeAuth()` configurado sólo con persistencia en memoria y sin `browserPopupRedirectResolver`, dependencia obligatoria para `signInWithPopup`; por eso el fallo ocurre inmediatamente en el navegador, sin popup ni `POST /api/account/federated/link`. `55816fa` añade el resolver y su prueba; prueba focalizada **5/5**, TypeScript y build Vite correctos. Pendiente de nueva autorización: respaldar la imagen actual, avanzar el VPS a `vps_prod`, reconstruir sólo frontend y verificar popup + enlace. No requiere cambiar `.env`, Firebase, backend ni MySQL.

- **Google/Firebase promovido a producción y staging eliminado (2026-07-25; último cambio `245f8e3`, publicado en `origin/vps_prod`):** producción usa `https://134-199-212-232.nip.io/GestionVPN-1.0/`, dominio autorizado en Firebase `vpn-noc`, certificado Let's Encrypt válido hasta 2026-10-22 y hook de renovación. Backend/frontend federados están habilitados; ADC vive read-only fuera del repo en `/etc/gestionvpn/firebase-admin.json`. Migraciones idempotentes completas y `firebase:preflight -- --provider` dio **LISTO** en `production`: Node 22, esquema `auth_identities`, ADC/API y configuración correctos; inventario inicial de mappings 0. El enlace Google ya no solicita contraseña local: desde una sesión activa, el botón abre directamente el selector oficial y el backend conserva CSRF, token reciente/revocable, proveedor Google, correo idéntico y unicidad; la contraseña sigue siendo obligatoria para desvincular. Pruebas focalizadas **10/10 backend + 6/6 frontend**, build y `check:all` correctos. DB/backend healthy, frontend running, HTTPS raíz y health 200 con TLS verificado. Staging y su monitor fueron eliminados. Rollback disponible: Git para código, dump pre-Firebase y copia del certificado anterior bajo `/root`. Pendiente inmediato: enlazar el primer usuario real y validar login Google, suspensión, unicidad y desvinculación.

- **Escanear y Monitor AP adaptados a móvil y publicados en `vps_prod` (2026-07-18; pendiente de deploy):** en Escanear, la franja de contadores/acciones ya no intenta mantenerse en una sola fila; los contadores envuelven y las acciones usan una cuadrícula de dos columnas. Además, por debajo de 640 px la tabla horizontal de escritorio se sustituye por tarjetas operativas: identidad/IP/MAC/modelo, rol/frecuencia, estado SSH, todas las métricas seleccionadas en una cuadrícula, selección masiva, acciones y detalle expandible, sin scroll horizontal ni columna fija superpuesta. La tabla configurable/resizable permanece intacta desde `sm`. En Monitor AP, cada cabecera de torre separa en móvil identidad/estado de contadores y `COLS AP`; el nombre se trunca dentro de su espacio sin invadir acciones. Verificado con **132/132 frontend**, prueba específica de viewport móvil, TypeScript, ESLint, build Vite, `check:all`, auditor de diseño sin violaciones y `git diff --check`.

- **PDF/WhatsApp AirOS estructurado para trabajo de campo publicado en `vps_prod` (2026-07-18; pendiente de deploy):** el PDF ya no imprime bloques continuos. Conserva una primera página de resumen de red y genera una ficha profesional por cliente con encabezado/estado, resumen, identidad (IP/AP/modelo), tarjetas TX/RX/señal/SNR/CCQ, problema principal, causas y planes separados en columnas para revisiones remotas y revisiones en campo. Las acciones equivalentes se consolidan en instrucciones canónicas para evitar repeticiones (espectro/canal/MCS/airMAX/reintentos frente a alineación/Fresnel/potencia/cableado). WhatsApp usa la misma estructura y separación. Verificado con el caso real WILDER `19/39 Mbps`, `-60 dBm`, `SNR 32`, `CCQ 82`: PDF A4 horizontal de 2 páginas renderizado e inspeccionado sin cortes ni superposiciones. También pasaron **131/131 frontend**, `check:all`, ESLint, build Vite y auditor de diseño sin violaciones.

- **Informe AirOS orientado a técnico de campo publicado en `vps_prod` (2026-07-17; pendiente de deploy):** el análisis NETWORK consolida todos los hallazgos de un STA en una sola ficha y elimina `STA-xx` del título visible. El título se construye desde los flags locales para incluir todos los problemas reales (por ejemplo, tasas TX/RX deficientes, señal en observación y CCQ aceptable). Cada parámetro observado muestra valor/estado, diagnóstico técnico y comprobaciones específicas; la interpretación y causas de Gemini quedan como diagnóstico general. El mismo formato se usa en pantalla, PDF y copia para WhatsApp. Prompt/cache actualizado a `air-os-v5-field-report`, con un hallazgo por STA y prohibición explícita de considerar saludable/adecuada la señal entre `-55` y `-60 dBm`. Verificado: **397 backend + 131 frontend**, contratos, `check:all`, build Vite, auditor de diseño sin violaciones y PDF A4 horizontal de 2 páginas renderizado sin cortes ni superposiciones.

- **DTO Gemini AirOS v4 por equipo y métrica (2026-07-17; pendiente de deploy):** cada STA se envía con campos planos `alias`, `role`, `family`, `apAlias`, `signal`, `snr`, `ccq`, `txRate`, `rxRate`, `airmaxQuality`, `airmaxCapacity`, `txRetries`, `txLatency`, `lanSpeed`, `riskScore` y `flags`. El prompt exige revisar y citar todos los flags del equipo, incluidos los de observación, y no concentrarse sólo en tasas. NETWORK admite hasta 10 hallazgos individuales y usa `air-os-v4-all-device-metrics` para invalidar caché anterior.

- **Análisis general Gemini/AirOS optimizado por preselección STA publicado en `vps_prod` (2026-07-17; pendiente de deploy):** la red visible se evalúa primero con un score determinista compartido entre frontend/backend; AP y roles desconocidos quedan fuera, sólo los STA con score `>=40` son candidatos y se preseleccionan como máximo los 10 peores sin completar con equipos sanos. El operador ve nombre, IP, AP, score y causas, y puede desmarcar/cambiar candidatos antes de confirmar. Gemini recibe exclusivamente aliases (`STA-xx`/`AP-xx`) y las métricas relevantes (señal, ruido/SNR, CCQ, TX/RX, airMAX, reintentos, latencia y LAN), nunca IP/MAC/nombre; el reporte recompone la identidad local, relaciona cada problema con sus equipos y se exporta a PDF. Sin candidatos no se consume Gemini. Las velocidades TX/RX ahora tienen umbrales absolutos, se reconocen modos `Station`/`Client` y la prelista permite inspeccionar también los STA evaluados que no se envían. Verificado: **393 backend + 129 frontend**, contratos, `check:all`, TypeScript, ESLint, build Vite, auditor de diseño sin violaciones, PDF A4 horizontal de 2 páginas renderizado/inspeccionado sin recortes y `git diff --check`. Pendiente: deploy y prueba real autenticada.
- **Prioridad RF obligatoria añadida (2026-07-17; pendiente de deploy):** un STA con señal `<=-55 dBm` y TX o RX negociado por debajo de la referencia del canal se marca `mandatory`, se coloca delante del resto al seleccionar el máximo de 10 y se visualiza con la etiqueta `Prioridad obligatoria`. La referencia es `72 Mbps` para 20 MHz y `150 Mbps` para 40 MHz; las métricas reales se conservan para que Gemini evalúe posible desalineación/interferencia.
- **Comparación de rendimiento por enlace (2026-07-17; pendiente de deploy):** TX/RX relativos sólo usan la mediana de STA que comparten AP identificado y ancho de canal; si falta AP, no se calcula una mediana de toda la red. Se retiraron los promedios globales del DTO de Gemini para no presentar una referencia engañosa.

- **Correcciones de análisis Gemini/AirOS completadas y publicadas en `vps_prod` (2026-07-17; pendientes de deploy):** corregida la causa probable del 502 `AI_INVALID_RESPONSE` en redes grandes elevando el límite de salida NETWORK de 1.200 a 3.200 tokens, reduciendo el máximo solicitado a 6 hallazgos, acotando arrays con el subconjunto JSON Schema admitido por Gemini y capturando `finishReason` cuando el JSON queda inválido. La acción individual usa ahora el mismo icono `Sparkles` que la red. El historial junto al análisis de red filtra exclusivamente `NETWORK`. El informe individual separa `Datos`, `Datos técnicos` e `Historial de resultados`: etiquetas operativas en español y volcados crudos íntegros aislados en su propia pestaña. Verificado: **384 backend + 124 frontend**, prueba focalizada del modal, `check:all`, ESLint, build Vite, auditor de diseño sin violaciones y `git diff --check`. Pendiente: rebuild backend+frontend en VPS y prueba real del análisis de 33 equipos.

- **Gemini/AirOS — mejora UX e historial por antena publicada y desplegada en `00bf042` (2026-07-17):** fases 0/1/2 e historial inicial de fase 4 operativos con SDK oficial, DTO allowlist seudonimizado por HMAC, salida Zod consultiva sin herramientas de escritura, caché/cuotas/cooldown, consentimiento y acceso individual sólo para moderadores `OWNER`. El informe por antena se organiza en pestañas `Datos`/`Historial de resultados`, añade resumen operativo sin retirar ningún dato AirOS, usa la acción `BrainCircuit`, corrige el scroll interno del resultado Gemini y filtra el historial por huella seudonimizada. Diagnósticos y snapshots tienen retención efectiva máxima de **7 días**: la API deja de mostrarlos al vencer y el job elimina análisis/snapshots —también si Gemini está deshabilitado—; no se persisten IP/MAC/nombre en el historial. Verificado localmente: **384/384 backend + 124/124 frontend**, contratos, `check:all`, ESLint, build Vite y auditor de diseño sin violaciones. En producción: migraciones idempotentes completas incluida `migrate:air-os-ai`, `vpn-db` y `vpn-backend` healthy, frontend HTTP/2 200 y health `mysql/routeros/smtp: ok`; Gemini global habilitado con claves presentes y retención 7/7. Pendiente: habilitar al moderador piloto desde el Administrador y validar el flujo visual autenticado en claro/oscuro y móvil.

- **Plan de implementación Gemini/AirOS creado (2026-07-17, sólo documentación):** [`PLAN_IMPLEMENTACION_GEMINI_AIR_OS_2026-07-16.md`](./docs/archive/completed-plans/PLAN_IMPLEMENTACION_GEMINI_AIR_OS_2026-07-16.md) convierte el análisis aprobado en 25 commits pequeños para fases 0, 1, 2 y 4. Define arquitectura backend-only, contratos/endpoints, cinco tablas MySQL, DTO allowlist y HMAC, reglas/risk score deterministas, JSON estructurado, cuota gratuita/caché/cooldown, UX individual/red/historial, pruebas, despliegue gradual y rollback. Fase 3 conversacional excluida. Acceso sólo para moderadores `OWNER`, apagado por defecto y habilitable/deshabilitable individualmente por el Administrador; MEMBER y platform admin no consumen análisis. Entitlement y consentimiento son guardas separadas. La feature nunca importa capacidades de escritura RouterOS/AirOS.
- **Aplicaciones de Gemini sobre AirOS documentadas (2026-07-17, sólo documentación):** creado [`APLICACIONES_GEMINI_DATOS_AIR_OS_2026-07-16.md`](./docs/archive/completed-plans/APLICACIONES_GEMINI_DATOS_AIR_OS_2026-07-16.md) con casos de uso por equipo y red completa, arquitectura, DTO y respuesta estructurada, privacidad, control de calidad, matriz de prioridad y hoja de ruta. Invariantes acordadas: Gemini será exclusivamente consultivo; no tendrá herramientas/permisos de escritura ni podrá modificar equipos, ejecutar comandos o tomar decisiones operativas. Toda decisión y acción corresponde al propietario u operador humano. Se usará el nivel gratuito de la API, por lo que DTO compacto, omisión de nulos, cálculo local, selección de anomalías, caché por snapshot, límites de salida y presupuesto interno de solicitudes/tokens son requisitos. Se implementarán fases 0, 1, 2 y 4; la fase 3 de asistente conversacional queda excluida. Sólo moderadores `OWNER` habilitados individualmente por el Administrador podrán acceder; el permiso nace apagado y es independiente del consentimiento.
- **PDF de Escanear sincronizado con la tabla (2026-07-16, cambios SIN commit):** el informe PDF ahora exporta únicamente las filas resultantes de búsqueda/filtros/orden actual, calcula `Total`, `Con stats` y `Guardados` sobre esas filas visibles y usa las columnas activadas en el selector de la tabla, además de las columnas básicas de identidad. CSV, JSON y Excel no fueron modificados. Verificado: **122/122 frontend**, TypeScript, ESLint focalizado, build Vite y `git diff --check`.
- **Cabecera UX de Monitor AP publicada (2026-07-16, commit `7f28782`):** la cabecera ahora tiene jerarquía explícita en tres zonas (identidad/estado SSE, métricas+controles y leyenda), textos totalmente en español, filtros segmentados con contraste AA y `aria-pressed`, controles táctiles de 44 px, búsqueda responsive, acciones de sincronizar/recargar diferenciadas y estado real `Conectando`/`En vivo`/`Reconectando` derivado de EventSource. Extraída a `ApMonitorModule/components/MonitorHeader.tsx` y cubierta por pruebas de componente y del hook SSE. Verificado: **122/122 frontend**, TypeScript, ESLint, build Vite, `audit:design` sin violaciones, `git diff --check` y pre-commit OK. Pendiente: reconstruir el frontend en el VPS y hacer revisión visual autenticada en desktop/móvil y modo oscuro.
- **Producción actualizada y sana (2026-07-16; logs VPS 2026-07-17 UTC):** el paquete `2e8d07b feat(platform): harden sessions and add VPN core management`, contenido en `d9d1603`, quedó desplegado desde `vps_prod`. Los tres servicios están arriba (`vpn-db` y `vpn-backend` healthy; `vpn-frontend` sirviendo HTTPS), las migraciones fueron idempotentes sin fallos, `/api/health` reportó `mysql/routeros/smtp: ok` y Nginx respondió HTTP/2 200. Única anomalía: `telegram-bot getUpdates HTTP 409` repetido porque otra instancia local consume el mismo token; no afecta API/UI, pero debe apagarse el bot local con `TELEGRAM_BOT_ENABLED=false`. La cabecera UX `7f28782` se publicó después de esta verificación y requiere un nuevo rebuild del frontend.
- **Servidor VPN MikroTik desde cero + salud/respaldo dual implementados (2026-07-14, cambios SIN commit):** nuevo menú `Servidor VPN` del Administrador con health del Core actual, cambio de equipo mediante credenciales, vista previa bloqueante y aprovisionamiento idempotente bajo confirmación `PREPARAR DESDE CERO`. Invariante: nunca migra/copiar/importa torres, nodos, peers, usuarios, PPP secrets o VRF; si existen objetos operativos se bloquea. El respaldo diario/manual genera el par atómico `.backup` AES-SHA256 + `.rsc` legible sin secretos, con base `servervpn_YYYY-MM-DD_HH-mm-ss_<identity-saneada>`; se envía sólo al platform admin verificado, MySQL guarda únicamente metadata/hashes y los temporales se eliminan del MikroTik/VPS incluso si SMTP falla. SSH/SFTP se habilita de forma temporal y restringida al VPS y luego restaura su estado previo. Configuración: `core_backup_*`, `core_wan_interface`, `core_vps_public_key`; tabla `core_backup_runs`; API `/api/admin/core-server/*`; job diario con deduplicación por fecha local. Plan: [`PLAN_APROVISIONAMIENTO_CORE_MIKROTIK_2026-07-14.md`](./docs/archive/completed-plans/PLAN_APROVISIONAMIENTO_CORE_MIKROTIK_2026-07-14.md). Verificado: **356 backend + 119 frontend**, build, `check:all` y `diff --check` limpio; pendiente prueba real contra un MikroTik de laboratorio y SMTP real antes de producción.
- **Tercer escenario de Core definido (2026-07-15, pendiente de implementar):** alternar temporalmente entre el servidor principal y otro MikroTik existente/de pruebas **sin aprovisionar ni modificar su configuración al seleccionarlo**, conservando ambos para volver al principal con un clic. El comportamiento actual sólo guarda un juego global `MT_IP/MT_USER/MT_PASS`: cambiarlo no ejecuta `Preparar desde cero`, pero reemplaza el Core activo, redirige hacia él los jobs/respaldos y no aísla los registros operativos de MySQL. Solución acordada: perfiles de servidor (`Producción`, `Laboratorio`, etc.) con credenciales cifradas, indicador del perfil activo, prueba de conexión previa, respaldo configurable por perfil y modos `Sólo observación`/`Operativo`. Para pruebas operativas reales, los nodos/peers/VRF y demás datos deben separarse por `core_server_id`; un simple selector de credenciales no es aislamiento suficiente.
- **Ajustes del Administrador convertidos a menu (2026-07-14, cambios SIN commit):** la pagina vertical larga ahora replica el patron de Ajustes del Moderador: cabecera + menu responsive y una sola seccion visible entre `Router Core`, `Servidor VPN`, `Escaneo`, `Reportes tecnicos` y `Cuenta`. Los formularios, persistencia y estados existentes se conservaron. Agregada prueba de navegacion entre las cinco secciones.
- **Login en raiz y restauracion anonima corregidos (2026-07-14, cambios SIN commit):** estando deslogueado, una ruta privada recordada (`/team`, etc.) ya no se reinyecta desde `localStorage`; redirige a la raiz canonica `/GestionVPN-1.0/`, incluida la barra final requerida por Vite al recargar. `GET /api/account/me` devuelve `200 user:null` solo cuando no hay cookie; cookies expiradas/suspendidas conservan 401 y logout. Un guard evita la doble restauracion de React StrictMode. Verificado en navegador real: abrir `/team` -> raiz, recargar -> login, consola 0 errores/warnings. Suite: **348/348 backend + 115/115 frontend**, `check:all`, build y diseno 0 violaciones.
- **SSE y origen local corregidos (2026-07-14, cambios SIN commit):** la repeticion de `ERR_CONNECTION_RESET 200 (OK)` afectaba `/tunnel/events` y `/events/stream` porque el frontend abierto en `127.0.0.1:5174` llamaba a `localhost:3001`; cookies SameSite, sesion y SSE quedaban entre sitios distintos. `API_BASE_URL` ahora es relativo por defecto: Vite/Nginx proxyan `/api` y solo `VITE_API_URL` explicito usa otro origen. El backend se midio estable (mismo PID/uptime; ambos SSE abiertos 32 s con heartbeat), ambos streams tambien pasaron 27 s por `127.0.0.1:5174/api` + proxy Vite con heartbeat, y el navegador quedo limpio tras login + navegacion Dashboard/Moderadores/Ajustes + 32 s de observacion. Ademas `/api/tunnel/events` envia `retry: 3000`/`ready`, evita buffering y limpia idempotentemente; el hook ignora sync tardio. Verificado: **346/346 backend + 112/112 frontend**, TypeScript, ESLint, build y diseno 0 violaciones. Un restart real aun puede dejar una linea aislada, con reconexion automatica en ~3 s.
- **Ajustes de correo para platform admin (2026-07-14, cambios SIN commit):** Ajustes del Administrador incorpora `Cuenta y recuperacion` (cambio del email/login con OTP al nuevo buzon + contrasena actual) y `Reportes tecnicos` (destinatario validado, guardar y enviar prueba). `app_settings.error_report_email` tiene prioridad sobre `ERROR_REPORT_EMAIL` y `SMTP_USER`; las credenciales SMTP siguen solo en env. `sessionBridge` reutiliza el platform admin por flag tras cambiar su email y evita recrear `admin@local.app`. Verificado: **346/346 backend + 108/108 frontend**, `check:all`, build y diseno 0 violaciones. No se envio correo real: falta que el Administrador configure su destinatario desde la UI.
- **Fix de metricas admin + limpieza local (2026-07-14, cambios SIN commit):** `/api/admin/summary` ahora excluye al administrador de plataforma y su workspace bootstrap de los contadores de clientes, igual que `/api/admin/moderators`. Se eliminaron transaccionalmente 2 usuarios locales sin membresia ni dependencias y se conservo intacto el bootstrap del platform admin; backup completo de 33 tablas guardado fuera del repo. Corregido ademas el nombre de opcion MySQL2 `keepAliveInitialDelay` (antes se ignoraba con warning). Verificado contra la API autenticada: workspaces/usuarios/moderadores/miembros/acciones = **0/0/0/0/0**; backend **342/342** y `check:backend` OK. Pendiente publicar este fix antes del deploy.
- **Auditoria de errores/async + implementacion (2026-07-14, commit `1453274`, publicada en `origin/vps_prod`, SIN deploy):** inventariados 71 archivos de produccion con `await` (239 expresiones); agregado `AsyncQueryState` (skeleton/error+reintento/vacio/exito), Error Boundaries raiz/auth/modulo, captura `window.error`/`unhandledrejection` y HTTP 5xx/red, endpoint `POST /api/error-reports` con Zod/redaccion/dedupe/rate limit y correo fijo a `ERROR_REPORT_EMAIL` (fallback `SMTP_USER`), y pagina 404 real. Corregidos fallos silenciosos en sesion, Dashboard, Moderadores, Nodos, Monitor AP, Ajustes, Notificaciones y Equipo/WireGuard; retirado `SettingsLoadingState` muerto. Informe: [`INFORME_AUDITORIA_ERRORES_ASYNC_2026-07-14.md`](./docs/archive/completed-plans/INFORME_AUDITORIA_ERRORES_ASYNC_2026-07-14.md). Verificado: **341/341 backend + 106/106 frontend**, `check:all`, build, diseno 0 findings, Semgrep focalizado 0/125 reglas, 404 movil 375x667 sin overflow y `diff --check` OK. No desplegado.
- **Plan frontend FE-0 a FE-7 completado (2026-07-14, commit `1976c59`, publicado en `origin/vps_prod`, SIN deploy):** corregidos estado de nodo y cancelación de peticiones; navegación con React Router, rutas canónicas y guardas RBAC; `ModuleErrorBoundary`; `Dialog`/`Drawer` accesibles y migración de overlays; formularios y validaciones; errores parciales en Equipo; tablas operables por teclado, táctiles, paginadas y responsive; paleta/contraste/tipografía; sesión `/account/me` centralizada; cachés acotadas con TTL; QR y exportadores pesados bajo demanda; y retirada de 42 archivos TS/TSX inalcanzables, `cpeCache`, assets y dependencias directas sin uso. IndexedDB activo (`dbService`/`deviceDb`) se conserva. Estado verificado: **100/100 tests frontend**, coverage **15.84% statements / 10.81% branches / 12.58% functions / 17.12% lines**, `check:all` OK, build/analyze OK, **10/10 E2E** desktop 1280x720 + móvil 375x667 con fixtures OWNER/MEMBER y Axe, auditor de diseño **0 findings**, Semgrep **0 findings** (125 reglas/279 archivos, con 14 timeouts de taint no bloqueantes), grafo de imports **0 archivos de producción inalcanzables** y `git diff --check` limpio. Informe y plan en los documentos enlazados abajo.
- **Auditoria frontend UX/calidad (2026-07-14, solo documentacion):** informe completo en [`INFORME_AUDITORIA_FRONTEND_2026-07-14.md`](./docs/archive/completed-plans/INFORME_AUDITORIA_FRONTEND_2026-07-14.md) y plan ejecutable en [`PLAN_CORRECCION_FRONTEND_2026-07-14.md`](./docs/archive/completed-plans/PLAN_CORRECCION_FRONTEND_2026-07-14.md). Resultado: **2 P1**, 10 grupos P2 y 8 grupos P3; 42 archivos TS/TSX inalcanzables (~1.643 lineas). Hallazgos principales: `useNodeManagement` no limpia el nodo activo eliminado porque `activeNodeVrfRef` nunca se sincroniza; drawer y 19/20 overlays carecen de semantica/focus trap/Escape; no hay Error Boundary para modulos lazy; Equipo oculta errores como estados vacios; auth/invitaciones tienen labels/autocomplete/validaciones incompletas; navegacion solo por `localStorage`; overflows en movil; tablas no operables completamente por teclado; `fetchWithTimeout` reemplaza la senal del caller; contraste y tokens inconsistentes. **IndexedDB NO esta muerto en bloque:** `dbService` y `deviceDb` siguen activos en VPN/Escanear/Monitor AP; solo `cpeCache` parece residual. Verificaciones: build/analyze OK, 66/66 frontend, auditor de diseno 21 findings, Semgrep 0 findings con 10 timeouts parciales de taint. `test:coverage` no arranco porque falta fisicamente `@vitest/coverage-v8` en la instalacion local. No se modifico codigo de producto.
- **Corrección integral de auditoría (2026-07-14, commit `a0a4ea4`, publicada en `origin/vps_prod`, SIN deploy):** cerrados los hallazgos backend/frontend: autorización por workspace/asignación en credenciales, detalle, script e historial de nodos; mutaciones WireGuard sólo moderador; Monitor AP ya no prueba credenciales SSH globales; autenticación **sólo cookie HttpOnly** (sin Bearer/query/refresh ni JWT en IndexedDB); credenciales SSH del escaneo sólo en memoria; limitador persistente `OTP_SEND`; secretos fuera de logs; y limpieza de `ScannerModule`, `VpnCard`, endpoints PPP/interfaz/WiFi y `replace-topo.js` confirmados sin consumidores. ESLint pasó de 109 warnings a gate **0 warnings**. Dependencias altas corregidas (`multer 2.2`, `nodemailer 9.0.3`, Vitest 4, DOMPurify/form-data); queda sólo `uuid@8` transitivo de ExcelJS, moderado y no alcanzable porque ExcelJS usa exclusivamente `v4()` sin buffer. **Salud final:** build producción OK · `check:all` OK · **337 backend + 66 frontend** · Semgrep 0 findings (con timeouts de taint en archivos grandes). Pendiente: deploy y validación funcional en vivo; el primer intento SSH de sólo lectura terminó por timeout y no modificó el VPS. `entrypoint.sh` ejecutará `migrateOtpSendRate.js` antes de arrancar.
- **Tips:** `dev` = `main` = `origin/*` = **`aa1864a`** (`fix(scan): auto-asigna scan-IP al activar túnel…`, 2026-07-04). ⚠️ Este §0 documenta con detalle hasta cont. 17 (`3b3ca3e`); `main` avanzó después a `72c9908` → `bbb9a6f` (cont.16 docs) → `11bb27f` (cont.17 Spinner/UI, que en cont.17 figuraba "sin commitear" y sí se commiteó) → `87488fb` (`fix(vpn): MEMBER logout en cadena + escaneo 0 por wg0 sin ruta + collation MariaDB11`) → `aa1864a`. Narrativa de esos últimos commits NO capturada aquí; ver `git log`. Repo VPS `/root/GestionVPN-1.0`, compose `gestionvpn-10`. **Prod DESPLEGADO y VERIFICADO en `aa1864a`** (2026-07-13, por SSH): checkout, imagen backend `gestionvpn-10-backend` (build 2026-07-05 02:23 UTC, **2 min tras el commit** → contiene `aa1864a`) y frontend, los 3 contenedores `healthy`. **`vps_prod` (§4.32) = este mismo `aa1864a` = prod real.** Autosync wg0 (cont. 10) sigue activo y verificado.
- **Última línea de trabajo (2026-07-04, cont. 17):** **Pasada de consistencia visual del sistema de diseño (auditoría profunda con agente `frontend-dev`) — SIN commitear.** `audit:design` ganó reglas **DS07/DS08/DS09** (botón `.btn-*` sin padding → texto cortado / botón sin color / clases muertas del plugin `tailwindcss-animate`, que **nunca estuvo instalado**). 15+ call-sites con animación muerta (toasts, drawer móvil, `<main>` de App) migrados a utilidades propias **`anim-*`** (index.css §55); `styles/animations.css` **eliminado** (100% muerto). Nuevo **`Common/Spinner`** (SVG stroke-dasharray canónico) en los 10 loaders centrales; `Loader2` queda solo dentro de botones. ConfirmModal migrado al sistema (tenía hover danger invertido y footer 50/50); 12 "Cancelar" a mano → `btn-ghost btn-md`; M5FullInfoModal legible en dark; SecretsTable + 6 popovers con dark unificado; ProvisionActionButtons sin gradiente cross-hue. Docs: `DESIGN_SYSTEM.md` §6.1–6.2 + `CLAUDE.md` Motion. Estado: `tsc` 0 · 66 tests · audit 0 errores (21 warnings/infos heredados). **Pendiente: commit de la pasada + backlog del informe (footers `.modal-footer`, `.th-grid`, DS10–DS18).** Detalle en LOG cont. 17.
- **Trabajo previo (2026-07-03, cont. 16):** **Diagnóstico de infraestructura: RouterOS core perdió TODA la configuración de torres.** Síntoma: usuario reportó "túnel de Housenet se borró hace una hora". **Diagnóstico:** probe reveló 0 PPP secrets (deberían haber 12+), 0 WireGuard node interfaces `WG-ND*` (deberían 12+), solo 3 management interfaces vivos. **Causa raíz = FÍSICA no del código:** el telemetry fix de cont. 15 (commit `72c9908`, solo frontend) no pudo tocar la config del router. La data loss es entre 2026-06-25 (último estado conocido) y 2026-07-03 → prob. reboot con backup viejo, reset de config manual, o loss de BD vía corte de poder. **Acción:** (1) **Limpieza de BD** — deleted 13 filas del `nodes` (arqueología del router viejo, ND1–ND13 muertos desde 17-jun). (2) **Re-provisión de Housenet** — alto de nuevo nodo SSTP (Torre Housenet, ND2, LAN `10.1.1.0/24`) desde el panel: el backend regeneró el secret PPP, la ruta de retorno, el VRF y entregó el script para pegarle al CPE. Script pegado ✅. Nodo debe estar **running** (verde) cuando conecte la torre. **Pendiente:** activar el túnel y validar telemetría (test post-fix de cont. 15). **Recomendación operativa:** revisar `/system history` del router (quién/qué borró la config) y evaluar si restaurar un backup o recuperar por inventario manual de torres. **No es bug de código** — es una caída de la infraestructura que necesita investigación física.
- **Salud:** `tsc` frontend 0 · `node --check` 0 · **313 tests backend** · **66 tests frontend** · contratos compilan · pre-commit OK · **`audit:semgrep` 0 findings** (630 archivos, 144 reglas) · `audit:design` 0 errores. **Prod (`/api/health` público):** mysql ok · **smtp ok** (puerto alterno del usuario) · **routeros ok** (mide última ESCRITURA).
- **Última línea de trabajo (2026-07-03, cont. 15):** **Auditoría profunda del branch con `/auditoria-completa` (5 dominios, read-only) + fixes de hallazgos.** 0 críticos; semgrep 0 findings. Arreglado (2 commits, FF a `main`): (`df0fde6`) índice `idx_tsl_ws_created` en `tunnel_session_logs` (las queries de auditoría filtraban por `workspace_id`+`created_at` sin índice → full-scan/filesort) + fix del `splitStatements` CRLF-safe en `initRbac.js`/`initMultiuser.js` (bug latente: solo `db.service.js` tenía el fix `b964309` → `init:multiuser` fallaba en BD fresca Windows). (`3b3ca3e`) 409 `AP_NO_NODE` accionable para AP sin nodo en VPS + `SMTP_VERIFY_TIMEOUT_MS` 4s→8s + credencial de prod redactada del handoff. Regla nueva §4.31. Detalle en LOG cont. 15. **PENDIENTE de desplegar** (junto con cont.13–14).
- **Trabajo previo (2026-06-25, cont. 14):** **Panel "Actividad reciente" (auditoría, módulo Equipo) operativo.** Salía siempre vacío porque `auditRepo` leía la tabla vieja `tunnel_logs` (que nadie llena) en vez de `tunnel_session_logs` (donde `sessionRepo.log` escribe los eventos reales de túnel). Fix `4f68052`: repuntado a la tabla correcta + SSE `'tunnel'` desde `tunnelService`/`expirationJob` para el "En vivo". `f76a7c9`: retención rodante de 7 días (`auditRepo.purgeOlderThan` + `expirationJob` throttle 1h, env `AUDIT_RETENTION_DAYS`). `c998ca4`: paginación cliente 8/página. Proceso en §5. **PENDIENTE de desplegar** (junto con cont.13). Detalle en LOG cont. 14.
- **Trabajo previo (2026-06-25, cont. 13):** **Monitor AP — credenciales SSH persistidas en el backend + la mangle de escaneo pasa a ser propiedad EXCLUSIVA del túnel.** Tips `dev`=`main`=**`e514b4d`**. (1) La clave que autenticó la antena vivía solo en `credCache` (navegador), no en `aps.clave_ssh_enc` → "Sin SSH" + 500. Fix `221aa63`: el guardado propaga la clave (rellena desde `credCache`), backfill en `load` cura los ya guardados, 400 (no 500) sin creds, y "Ver clave SSH" (`/reveal-ssh`). (2) La mangle `SCAN-WS` "se caía sola cada minuto": `apPollJob` la montaba/desmontaba por tick y `poll-direct`/`scan` la creaban bajo demanda. Fix `e514b4d` (supersede a `a4a4e08`): la mangle la gestiona **SOLO el túnel** (`scanMangleSync` en activate/deactivate); Monitor AP solo ata a la scan-IP y **exige túnel activo** → si no, **409 `TUNNEL_NOT_ACTIVE`** + popup "Activá el túnel". Regla §4.30. **`221aa63` desplegado; `a4a4e08`+`e514b4d` PENDIENTES (rebuild backend+frontend).** Detalle en LOG cont. 13. Skill: `mikrotik-vpn-expert`.
- **Trabajo previo (2026-06-25, cont. 12):** **Fix del scan-lock anti-fuga (elimina el 504 que obligaba a reiniciar el backend) + mangle de escaneo atada al ciclo del túnel.** Commits `4b420c4`/`0571409`/`3a4746a` (**desplegados y verificados en VPS**). Causa del 504: un **lock filtrado** (`req.on('close')` registrado DESPUÉS del `await acquire` → petición muerta esperando dejaba el lock 5–7 min, reintentos apilados). `scanLock` reescrito + `acquireOrNull` (espera acotada→**409 `SCAN_BUSY`**, cancelable) + preempción (un escaneo nuevo aborta el anterior). La mangle `SCAN-WS` ahora **vive y muere con el túnel** (`lib/scanMangleSync.js` en activate/deactivate + `expirationJob`), no con un timer. Reglas §4.29. Detalle en LOG cont. 12.
- **Trabajo previo (2026-06-23, cont. 9):** **El ESCANEO desde el VPS quedó OPERATIVO (31 dispositivos) + automatización del ciclo de vida de rutas.** El escaneo daba 0 por **tres capas de routing del VPS**, todas resueltas: (1) el workspace sin scan-IP (`scan:assign`); (2) el `.env.production` con el pool VIEJO `192.168.30.` → corregido a `10.11.252.` (+ `SCAN_RETURN_SUBNET`, `SCAN_IP_POOL_END=50`); (3) **el bloqueador real**: el `wg0` del VPS NO ruteaba la LAN de la torre (`ip route get 10.1.1.1 → dev eth0`) porque faltaba `10.1.1.0/24` en los AllowedIPs del peer → añadido → `dev wg0` → **escaneo OK**. Entendido el flujo (mangle `dst-address-list` + masquerade SSTP para el retorno; el scanner solo detecta Ubiquiti → feature "Otros" pendiente). **Automatización de rutas** (`992ee32`+`fc058b3`): helper `addRouteOnce` (RouterOS permite rutas duplicadas → `writeIdempotent` NO dedup; el dedup causaba el "exceso de rutas"), retornos `distance=2`, SSTP sin `/32` redundante, scan-pool de `wg0` reducido 253→49. Reglas nuevas §4.26/27/28. Detalle en LOG cont. 9. **Todo DESPLEGADO y verificado (2026-06-24): prod sano, router limpio (set canónico de 13 rutas). Pendiente a futuro: feature "Otros" en el scanner + sync automático del `wg0`.**
- **Trabajo previo (2026-06-23, cont. 8):** **Fix del 409 `NO_MGMT_IP` al activar túnel (causa raíz sistémica) + puerto SSTP configurable + etiqueta de rol.** Síntoma: un moderador recibía 409 al pulsar Acceder. **Causa raíz:** el ENUM `user_mgmt_ips.source` NO incluía `'auto-provision'` (el valor que escribe el auto-mapeo en `team.routes.js`) → el upsert lanzaba "Data truncated", el try/catch lo tragaba → **el mapeo user→IP nunca se escribía en ninguna provisión** → 409 universal. Fix en `dev` (sin desplegar): migración `migrateMgmtIpSource.js` (ensancha enum +`auto-provision`/`auto-heal` + backfill orphan-safe desde `member_wireguard`), **auto-heal** en `activateTunnel` (resuelve la IP del peer vivo si falta el mapeo — `lib/mgmtIpResolver.js`), `roleLabel()` en el Sidebar (mostraba "Admin" para un OWNER), y setting global `sstp_port` (el script SSTP salía en 443 y el Core escucha en 4443). **Prod ya parcheado a mano (2026-06-23):** enum `source` ensanchado en vivo (`ALTER`), mapeos de los 2 usuarios insertados (`fernandodiazm.5`→`10.13.250.22`, `fernandodiazmeza12`→`10.13.250.23`) y huérfanos del plano viejo (`192.168.21.x`) purgados (0/0/0). ⇒ prod ya NO recae en el 409 para altas nuevas, **incluso sin el redeploy**. Barrido de otros ENUMs: limpio (era el único). Reglas nuevas §4.23/24/25. Detalle en LOG cont. 8. **Pendiente: redeploy del VPS (para llevar auto-heal/etiqueta/`sstp_port`) + `sstp_port=4443` en Ajustes prod + `SCAN_RETURN_SUBNET` viejo en `.env.production`.**
- **Trabajo previo (2026-06-22, cont. 7):** **Despliegue del código nuevo en el VPS + arreglo del túnel WG VPS→router.** `dev`→`main` (FF) y re-deploy en el droplet (`git reset --hard origin/main` + `up -d --build`); migraciones idempotentes OK. **OJO — la BD de prod CONSERVA los datos previos** (los volúmenes `db-data`/`backend-data` sobreviven al redeploy; las migraciones no hacen DROP) → por eso quedaron settings/usuarios/peers VIEJOS (incl. `MT_IP=192.168.21.1` y el password admin de antes). El `wg0` del VPS estaba en el **plano viejo `192.168.21.x`** (peer→`:13231`, scan-pool `192.168.30.x`) → reescrito al **plano `10.x`** correcto: `Address=10.12.250.60/32`, peer `VPN-WG-VPS` pubkey `R79mbZb0bRcu5+NL40ZAMiX5VtjWJ9JJ+AqltA15uQM=` `:13232`, scan-pool `10.11.252.x`, AllowedIPs `10.x` + LANs torre `142.x`. **Verificado: handshake OK, `ping 10.12.250.1` 0% loss, `nc 8728` OK.** La clave privada del VPS NO cambió (su pubkey `BV8z…` ya era el peer registrado). Pendientes operativos abiertos en §7 (MT_IP en prod, login admin, peer huérfano, persistencia wg0, rotar clave VPS, telegram 409). Detalle en LOG cont. 7.
- **Trabajo previo (2026-06-22, cont. 6):** **Auditoría visual UI + correo/Telegram operativos.** (1) **Fix visual** (commit `3945f08`): los pickers de columnas (`ColumnPicker` de Escanear y `NodeColumnPicker` de Nodos) quedaban tapados/recortados por la tabla (`overflow-x-auto` + columna sticky-right `z-10`); ahora usan el patrón ya probado en `NodesExportMenu` (`useKebabMenu` + `createPortal` + `position:fixed` + `z-[60]`). Además botones `btn-*` sin padding (texto desbordaba la píldora) en `DeepLinkBanner` y `NotificationsTab` (Reanudar/Pausar) → corregidos. **Causa de fondo: las clases `.btn-*` solo dan color/forma; el padding/flex va en el call-site.** (2) **Telegram/SMTP estaban "desactivados" por el `.env`** (commit `eca46ca`): `dotenv.config()` sin `path` leía el `.env` del cwd (`server/`), pero el `.env` vive en la **raíz**; ahora se carga por ruta absoluta → token y SMTP cargan. **SMTP verificado real** (`verify()` OK 1.2s contra Gmail; los correos salen). (3) **Feature: vinculación Telegram en 3 pasos** (commit `707829f`): nuevo `TelegramLinkSteps` (instalar app → copiar código → abrir bot y pegar); `lib/telegram.getBotUsername()` autodetecta el `@username` vía `getMe` (cacheado) y lo expone en `GET /account/notifications`; botón "Vincular" pasó a `btn-primary` (índigo, con feedback). Nuevas reglas **§4.21/§4.22** y proceso en **§5**.
- **Trabajo previo (2026-06-22, cont. 5):** **El escaneo "no buscaba de manera local"** — diagnóstico end-to-end: red, probe, worker y backend estaban OK (el worker hallaba 32 dispositivos en `10.1.1.0/24`). La causa: `scan_mode='local'` con `local_scan_ip='10.14.250.20'`, una IP que el laptop **no tiene** (su WG es `10.13.250.20`, plano CLIENTES) → en modo local el escaneo ata cada socket a esa IP y el `bind()` falla en silencio → 0 resultados. El placeholder engañoso del campo (`10.14.250.20`, plano ADMIN) indujo el valor. **Fix de datos:** `local_scan_ip` → `10.13.250.20`. **Fix de código (commit `bb81074`):** nuevo `lib/wgDetect.js` (detección read-only de IPs WG locales); en modo local el escaneo ahora **alerta** (409 `LOCAL_SCAN_IP_STALE`) si la scan-IP no está viva en el equipo, en vez de fallar callado; `GET /settings/scan-local-check` + banner ámbar en `ScanModeToggle` avisan al admin; placeholder corregido. Nueva función vigente en **§5**. **Requiere reiniciar el backend.**
- **Trabajo previo (2026-06-22, cont. 4):** **Fix del 404 al eliminar un nodo del workspace** (commit `60563b0`). Causa raíz: **inconsistencia visibilidad↔propiedad** en nodos legacy. `filterNodesForRole` mostraba el nodo por `ppp_user` **o `nombre_vrf`**, pero `nodeBelongsToRequester`/`deleteNode` solo miraban `ppp_user`; en el nodo TorreHousenet el secret del router se llama `TorreHousenet` mientras el registro de provisión en BD tenía `ppp-torrehousenet-nd2` (divergen; solo comparten el VRF) → visible pero imborrable. Fix (durable): identidad de nodo consistente = `ppp_user` **O** `nombre_vrf` en `nodeBelongsToRequester(req,pppUser,vrfName)` y `deleteNode(pppUser,vrfName)` (borra ambas filas: provisión + caché del router); `/node/deprovision` pasa `vrfName`. Además `filterNodesForRole` ya NO deja colar filas cacheadas con `workspace_id` NULL/ajeno que compartan VRF (match por presencia de la propiedad), y `EliminarNodo.tsx` tolera respuestas de error sin `steps`. Nueva regla **§4.20**.
- **Trabajo previo (2026-06-21, cont. 3):** **La aceptación de invitación ya no se traga el fallo de provisión WG** (commit `7ac2543`, cierra pendiente §7). `AcceptResponse` gana `wgError {code,message}` (`ROUTER_UNREACHABLE`|`ROUTER_NOT_CONFIGURED`|`PROVISION_FAILED`); `/accept` e in-app lo pueblan (clasifica con `isUnreachable`, sin filtrar IPs/creds); `AcceptInvitationForm` muestra el motivo + botón **"Reintentar"** (self-service `/me/wireguard`, la cookie ya está puesta) que renderiza `.conf`/QR si el router ya responde. El alta de cuenta sigue siendo best-effort.
- **Incidente de red resuelto (2026-06-21):** el usuario borró su propio peer WG de administración → el panel perdió acceso al router (todo 503, provisión WG "saltada"). Recreado el peer admin (**`10.14.250.2` → router `10.14.250.1`**, plano `VPN-WG-ADMIN`); verificado API OK. Regla durable nueva: **§4.19 — nunca borrar ese peer**. (No era bug de código.)
- **Trabajo previo (2026-06-21, cont. 2):** **Fix del cuelgue de la API RouterOS** (commit `654b09d`). Síntoma: el modal "Eliminar moderador" se quedaba pensando para siempre. Causa raíz doble: (1) **código** — `connectToMikrotik` confiaba en el `timeout` de `node-routeros` (`socket.setTimeout`), que NO dispara durante el login de la API; el router aceptaba el TCP pero, si no respondía al `/login`, la promesa quedaba colgada y el `fetch` del frontend (sin timeout) dejaba el spinner eterno. Fix: deadline DURO de 9s (`connectWithDeadline`, destruye el socket y rechaza) + el DELETE ahora borra en BD y responde primero, con la limpieza del router en segundo plano (best-effort). (2) **router** — `/ip service api address=` tenía su PROPIA allow-list (independiente del firewall/`LIST-MGMT-TRUSTED`) con solo el plano viejo `192.168.21.0/24`; el backend sale por `10.13.250.x`, no incluido → login descartado en silencio. **Arreglado en el router en vivo** (usuario corrió `/ip service set api/api-ssl address=` con los `10.x`); verificado: login OK en ~130ms a `10.13.250.1` y `10.14.250.1` (`GW-VPN-CORE-ISP`). `migrate-mgmt-net.rsc` ahora trae **FASE 2b** para no repetir el olvido en otros entornos.
- **Trabajo previo (2026-06-21, cont.):** **Auditoría integral** del branch con la nueva skill `/auditoria-completa` (orquesta seguridad/lógica/eficiencia/BD/validación en informe por severidad, read-only). 0 críticos; **5 hallazgos arreglados** (commit `b964309`): (1) collation de `workspace_scan_ip` `unicode_ci`→`general_ci` (los JOIN reventaban "Illegal mix of collations"; `ALTER` idempotente en `migrateScanIp.js`, también arregla prod en el próximo deploy). (2) Eliminada la ruta viva `POST /tunnel/mangle-access` que inyectaba mangles GLOBALES `ACCESO-ADMIN` con `src=<toda la /24>` (violaba §4.3; sin consumidor). (3) `splitStatements` CRLF-safe (`split(/\r?\n/)`) — el `\r` dejaba pasar el `;` de comentarios inline y truncaba `CREATE TABLE aps`/`mgmt_peer_owners` en BD fresca Windows. (4) `buildClientConf` **lanza** si falta `allowedIps` en vez de caer a `0.0.0.0/0` (footgun §4.10). (5) Seed con passwords por env (`SEED_ADMIN_PASSWORD`/`SEED_MOD_PASSWORD`). **Tooling** (commit `64adc7b`): `npm run audit:semgrep` (imagen Docker fijada, no hay binario Windows) + skill `auditoria-completa`. Migraciones notif/monitoring/dropcomod verificadas/aplicadas en local.
- **Trabajo previo (2026-06-21):** Opción C reforzada (scan-IP amarrada al workspace al crearse, `scanLock.tryAcquire` en Monitor AP) + retiro total de co-moderadores (`CO_MODERATOR` fuera de enum/contratos/guards/rutas/UI/tests, `migrate:dropcomod`). Detalle en LOG.
- **Trabajo previo (2026-06-20):** ciclo de vida WireGuard de gestión + borrado en cascada (`.conf` split-tunnel, recuperación WG self-service, `nodeDeprovision`, reutilización de IPs, 503 en cortes). Detalle en LOG.
- **Estado de datos (2026-06-20):** **0 nodos** en BD (ND2 TorreHousenet + ND3 TorreOmar borrados — tenían `workspace_id=NULL`); **2 workspaces** (Soporte, Espacio de admin), 4 users; zombies del soft-delete purgados.
- **Acciones de RED pendientes del usuario:** ver §7 (pendientes) y los runbooks de §6.

> Resumen de cada sesión anterior: ver [`HANDOFF_LOG.md`](./HANDOFF_LOG.md). Para no inflar este doc, **aquí solo va lo que sigue siendo cierto hoy.**

---

## 1) Producto y roles (RBAC)

Panel **multi-tenant (SaaS)** para administrar túneles VPN sobre un **MikroTik central compartido** (SSTP + WireGuard) y monitorear equipos **Ubiquiti airOS** (AC/M5, APs/CPEs) en las LAN remotas vía VRF.

| Rol | Flag | Ve | No ve |
|---|---|---|---|
| **Administrador** (Sistemas) | `is_platform_admin=1` | Dashboard · Moderadores · **Ajustes** (config router core) | Nodos/Escanear/etc. |
| **Moderador** | `OWNER` de su workspace | Nodos · Escanear · Usuarios · Equipo · Monitor AP | Ajustes / config router |
| **View** (MEMBER) | — | Sus túneles asignados + perfil WireGuard | Todo lo demás |

**Un solo moderador por workspace:** cada workspace = **1 OWNER (moderador) + sus MEMBERs**. El rol `CO_MODERATOR` fue **retirado** (enum BD `('OWNER','MEMBER')`). Solo el OWNER escanea/gestiona; el MEMBER no. La scan-IP del VPS se amarra **al workspace** (no al usuario) al crearse (Opción C); el `scanLock` ahora serializa el escaneo interactivo del moderador contra el job de Monitor AP del mismo workspace.

**Seguridad es exclusiva de plataforma:** únicamente `platform_admin` ve el módulo y puede consumir `/api/admin/security/*`. `OWNER` y `MEMBER` no tienen vista ni operaciones parciales de Seguridad.

---

## 2) Arquitectura y stack

| Capa | Tecnología |
|---|---|
| Frontend | **React 19** + **TypeScript (strict)** + **Vite** + **Tailwind v3** + `lucide-react` + `qrcode` + `localforage` |
| Estado | Context API (`VpnContext`, `WorkspaceSessionProvider`) + hooks por feature |
| Backend | **Node.js + Express** (JS plano), `mysql2/promise`, `node-routeros` (API :8728), `ssh2` (airOS), `bcryptjs`, `jsonwebtoken`, `zod`, `cookie-parser`, `nodemailer` |
| BD | **MySQL/MariaDB** — única BD (operativa + RBAC). XAMPP en local (`vpn_manager`); MariaDB 11 en prod (Docker) |
| Cripto | AES-256-GCM (`.db_secret`) para credenciales · JWT HS256 con issuer/audience, `kid` active/previous y `jti` server-side (`auth_sessions`) · cookies `vpn_session` HttpOnly + `vpn_csrf` HMAC |
| Puertos | Backend **:3001** · Frontend **:5173** (base `/GestionVPN-1.0/`) · Router MikroTik gestión `10.12.250.1` (plano `10.x` nuevo) |

**Auth:** cookie HttpOnly `vpn_session` como único transporte; `Authorization: Bearer` y token por query están rechazados. Login por email, `usuario@local.app` o nombre. Las mutaciones con cookie requieren Origin canónico y `X-CSRF-Token` ligado al `jti`.

**Despliegue prod:** VPS DigitalOcean `134.199.212.232`, dominio `https://joinpoint.cloud/`, Docker (`docker-compose.prod.yml`: MariaDB 11 + backend `network_mode: host` + frontend nginx). **Rama de trabajo y despliegue: `vps_uni` (ÚNICA para este VPS — §4.32).** `vps_prod` corresponde a otro VPS. Detalle en [`DESPLIEGUE_VPS.md`](./DESPLIEGUE_VPS.md).

**Plano de red de gestión (`10.x`, migrado desde `192.168.21.x`):** nodos WG `10.11.250.<ND>` · nodos SSTP `10.11.251.<ND>` · scan-pool VPS `10.11.252.0/24` · **VPN-WG-VPS** `10.12.250.1/24` · **VPN-WG-CLIENTES** `10.13.250.1/24` (mod/members) · **VPN-WG-ADMIN** `10.14.250.1/24` (admin). Fuente de verdad: `server/lib/mgmtNet.js` (backend) + `vpn-manager/src/config.ts` (frontend). Runbook: [`MIGRACION_RED_GESTION.md`](./docs/archive/network-migrations/MIGRACION_RED_GESTION.md).

---

## 3) Datos y APIs (referencia rápida)

**Esquemas MySQL** (`server/sql/`): `schema_ops.sql` (operativo: `nodes`, `node_ssh_creds`, `aps`, `cpes`, `signal_history`, `vpn_users`, `app_settings`, …) · `schema_rbac.sql` (`users`, `workspaces`, `workspace_members`, `invitations`, `tunnel_assignments`, `member_wireguard`, …) · `schema_multiuser.sql` (`user_mgmt_ips`, `tunnel_user_sessions`, `tunnel_session_logs`) · `workspace_scan_ip` (Opción C).

**Fuentes de verdad únicas (NO dupliques lógica fuera de aquí):**
- Script del CPE → `server/lib/cpeScript.js` (`buildCpeWgScript` + `buildCpeSstpScript`).
- Credenciales SSTP server-side → `server/lib/sstpCreds.js`.
- Red de gestión → `server/lib/mgmtNet.js` + `vpn-manager/src/config.ts`.
- Contratos API compartidos → paquete `@gestionvpn/contracts` (`packages/contracts`).

> Detalle de endpoints y código core: §3 y §6 de [`HANDOFF_LOG.md`](./HANDOFF_LOG.md).

---

## 4) 🔒 Reglas e invariantes del proyecto (NO romper)

Estas reglas son **durables**: aplican a toda sesión y a todo cambio. Si una feature necesita salirse de aquí, documentarlo explícitamente antes.

1. **Alta de nodo = todo server-side.** Dar de alta un nodo debe ser como dar de alta un usuario: el servidor **genera TODO** (llaves WG, usuario/contraseña SSTP, rutas de retorno) y entrega **un único script para pegar en el CPE**. El modo experto (pegar llaves/usuario propios) va OCULTO tras "Opciones avanzadas". Nunca volver a pedir la clave pública del CPE en el flujo normal.
2. **Toda la infra por-túnel se crea sola** (mangle, rutas de retorno, scan-route, IP de gestión por nodo). Nada de `.rsc` manual en el flujo normal.
3. **Aislamiento multi-usuario — mangle POR-USUARIO (sin colisión).** Cada moderador marca **solo su propio tráfico**: `comment=ACCESO-USER-<tag>`, `src-address=<su mgmt_ip>` → `new-routing-mark=<su VRF>`. **PROHIBIDO** crear reglas mangle GLOBALES (`ACCESO-ADMIN`/`src=<toda la /24>`): rompen el aislamiento y el backend las elimina. `mgmt_ip` + VRF se resuelven **server-side** desde la sesión activa (`sessionRepo.getActiveByUser`), **nunca** desde un IP del body. Usar `tunnelProvisioner.addUserMangle`. Aplica a activate, keepalive **y reparación**.
4. **Varios nodos pueden compartir la misma LAN a propósito** (arquitectura VRF + mangle). NO añadir avisos de "solape de LAN" como si fuera un error (memoria `arquitectura-lan-compartida-vrf-mangle`).
5. **Antispoofing en escaneo/Monitor AP:** la IP y credenciales del equipo se resuelven **server-side** del nodo/AP dueño (`ipInOwnedSubnet`, `ownsApUuid`), nunca del body del cliente. El navegador no envía contraseñas SSH.
6. **🛑 Operaciones SSH sobre antenas Ubiquiti airOS — SOLO:** lectura de estado + diagnóstico activo (ping/traceroute) + reinicio con confirmación explícita. **NUNCA:** actualizar firmware, borrar archivos, restaurar a fábrica, modificar config persistente, apagar, gestionar usuarios SSH, ni **polling SSH automático** (§43 del LOG). Detalle completo y matriz de comandos: sección "Política SSH" en [`HANDOFF_LOG.md`](./HANDOFF_LOG.md).
7. **Sistema de diseño:** color = intención, movimiento = estado (no decorativo). Usar clases del sistema (`.btn-*`, `.badge-*`, `.card`, `.modal-overlay/.modal-panel`, `.skeleton`, `.status-live`). Dark mode por clase; toda animación respeta `prefers-reduced-motion`. `text-xs` mínimo. Gate CI `audit:design` debe quedar en **0 errores**. Menús de acción **con color** (no gris) — memoria `pref-menu-accion-con-color`.
8. **No versionar secretos:** `.jwt_secret`, `.db_secret`, `database.sqlite*`, `.claude/worktrees/`, binarios. (El historial ya se purgó 2×; en el VPS `134.199.212.232` usar `git fetch origin && git reset --hard origin/vps_uni`, NUNCA `git pull`; `vps_uni` es la única rama de despliegue para este servidor según §4.32.)
9. **`/settings/save` y config del router core = solo `platform_admin`.** Un moderador no muta settings globales por API.
10. **`.conf` de gestión = SPLIT-TUNNEL, NUNCA `0.0.0.0/0` (ni `0.0.0.0/1, 128.0.0.0/1`, que es lo mismo).** El router no da salida a internet a los clientes de gestión (CLIENTES); solo el plano ADMIN tiene internet (`Admin MGMT libre`+NAT). `0.0.0.0/0` deja a CLIENTES **sin internet**. AllowedIPs = base RFC1918 (`mgmtNet.mgmtAllowedIps`, env `MGMT_ALLOWED_IPS`) **+ LAN públicas de torre leídas DINÁMICAMENTE** del address-list `LIST-NET-REMOTE-TOWERS` del router (`readTowerLans` + `mgmtAllowedIpsFor(ws, {addressList})` en `lib/mgmtAllowedIps.js`) → cubre `142.152.7.0/24`, `142.153.0.0/24`, etc. sin hardcodear. Las privadas ya las cubre la base.
11. **Clasificar caídas del túnel como inalcanzable (503), no 500.** `isUnreachable` debe incluir cortes transitorios (`ECONNRESET`/`EPIPE`/`EHOSTDOWN`/`socket hang up`) para que la UI muestre el overlay "Router no alcanzable · Reintentar" en vez de un 500 duro.
12. **Borrar un moderador = de-provisionar sus nodos del router.** El cascade (`DELETE /admin/moderators/:id`) debe llamar `deprovisionNodeOnRouter` (`lib/nodeDeprovision.js`) por cada nodo (VRF + interfaz + rutas + mangle + peer), además de borrar peers de gestión (`routerCleanup`/`routerPeerState`, que usan `mgmtNet.userIfaces`, NO `VPN-WG-MGMT`). Best-effort: el router caído no bloquea el borrado en BD.
13. **NUNCA tocar el address-list `LIST-NET-REMOTE-TOWERS` al de-provisionar.** Varias torres comparten la misma LAN (aislamiento por VRF+mangle) → borrar la entrada rompería a los nodos hermanos. Las entradas sin ruta/VRF quedan inertes.
14. **IPs de gestión: amarradas al usuario, liberadas y reutilizadas.** `user_mgmt_ips` (`uq_umi_user`+`uq_umi_ip`) amarra 1 IP↔1 usuario; al borrar se libera. La asignación usa **menor octeto libre** (`lib/ipAlloc.lowestFreeOctet`), no `max+1` → reutiliza huecos de usuarios borrados.
15. **Scan-IP del VPS = amarrada al WORKSPACE, asignada al crearse.** `workspace_scan_ip` (`uq_wsi_workspace`+`uq_wsi_ip`) liga 1 scan-IP↔1 workspace. Se asigna **automáticamente** al crear el workspace (`scanIpRepo.allocateInTx` dentro de `workspaceRepo.createForOwner`, menor IP libre del pool `10.11.252.0/24`), best-effort: pool agotado NO rompe la creación (cae a escaneo legacy). NUNCA tomar la IP del request del cliente — siempre server-side (`resolveForWorkspace`). Se eligió el **workspace** (no el usuario) porque es el contenedor estable: sobrevive a rotación del moderador y el namespace de mangle ya es `SCAN-WS-<ws>`.
16. **Un solo moderador (OWNER) por workspace — sin co-moderadores.** El rol `CO_MODERATOR` está **retirado** del enum, contratos, guards, rutas y UI. `isModerator` = `OWNER` (o `platform_admin`). No hay promover/degradar (endpoint `/team/role` eliminado); `/team/invite` solo crea MEMBERs. El alta de un moderador la hace el Administrador (invitación rol `OWNER`). Job de Monitor AP: `scanLock.tryAcquire` (no bloqueante) para no estancar otros workspaces durante un escaneo interactivo largo.
17. **Toda conexión al router debe estar acotada por un deadline PROPIO.** `connectToMikrotik` (`routeros.service.js`) impone `connectWithDeadline` (9s) que destruye el socket y rechaza. **NO confiar en el `timeout` de `node-routeros`** (`socket.setTimeout`): NO dispara durante el login de la API — el router acepta el TCP pero, si no responde al `/login`, la promesa queda colgada para siempre. Toda operación de router es best-effort: una caída/cuelgue del router **nunca** debe bloquear una petición HTTP (el frontend no tiene timeout de fetch). Patrón: hacer el trabajo de BD y responder primero; la limpieza/sync del router corre aparte.
18. **El plano de gestión `10.x` debe estar en `/ip service api address=` (y `api-ssl`), no solo en el firewall.** `/ip service api` tiene su PROPIA allow-list por `address=`, **independiente** del firewall y de `LIST-MGMT-TRUSTED`. Si el origen del backend (`10.13.250.x`/`10.14.250.x`) no está ahí, RouterOS acepta el TCP pero **descarta el login en silencio** (no `!trap`, no cierre: cuelgue). Lo aplica `migrate-mgmt-net.rsc` FASE 2b. Síntoma de diagnóstico: TCP a :8728 abre en ms pero `connectToMikrotik` no retorna → revisar esta lista antes que credenciales/red.
19. **NUNCA borrar el peer WG de administración que da acceso al router.** El backend habla con el MikroTik (`MT_IP=10.14.250.1`) **solo** mientras la interfaz WG de gestión del admin esté arriba. El peer admin es **`10.14.250.2` → router `10.14.250.1`** (plano `VPN-WG-ADMIN`). Si se borra ese peer (o se cae el túnel del laptop), el panel pierde TODO acceso al router y **cada endpoint que toca el router devuelve 503** (router inalcanzable), y la provisión WG del moderador "se salta" en silencio (§7). Diagnóstico: `ping 10.14.250.1` / TCP a :8728 en timeout = túnel admin caído, NO es bug de código. Mantener ese peer y un handshake activo (PersistentKeepalive ya va en el `.conf`, `wgkeys.js`).
20. **Identidad de nodo = `ppp_user` O `nombre_vrf` (consistente en VISIBILIDAD y PROPIEDAD).** El identificador ESTABLE de un nodo físico es su VRF (`VRF-ND<n>-<NOMBRE>`); el `ppp_user` (nombre del secret en el router) puede DIVERGIR del registro de provisión en BD en nodos legacy/manuales (p.ej. secret `TorreHousenet` vs fila `ppp-torrehousenet-nd2`; solo comparten el VRF). Las comprobaciones de pertenencia/borrado (`nodeBelongsToRequester(req,pppUser,vrfName)`, `deleteNode(pppUser,vrfName)`) DEBEN matchear por `ppp_user` **o** `nombre_vrf`, igual que `filterNodesForRole` (visibilidad) — si difieren, un nodo VISIBLE queda IMBORRABLE (404 "Nodo no encontrado en tu workspace"). El borrado elimina TODAS las filas del nodo físico (registro de provisión + caché del router). El listado `/nodes` cachea filas keyed por el nombre del secret del router y `saveNode` NO setea `workspace_id` (→ quedan NULL); por eso `filterNodesForRole` solo confía en el `workspace_id` PROPIO de una fila cacheada (por PRESENCIA de la propiedad) y nunca deja colar una fila NULL/de otro workspace que comparta VRF.

21. **El backend carga el `.env` de la RAÍZ del repo por ruta absoluta, no por cwd.** `server/index.js` hace `dotenv.config({ path: path.resolve(__dirname,'..','.env') })` (+ fallback a `server/.env`). Motivo: `npm run dev` corre desde `server/`, pero el `.env` (con `TELEGRAM_BOT_TOKEN`, `SMTP_*`) vive en la raíz; un `dotenv.config()` pelado leía el cwd y dejaba esas vars sin cargar → bot Telegram no arrancaba y correo caía a modo DEV (sin enviar). En prod NO se rompe: compose inyecta env vía `env_file: server/.env.production` y dotenv no sobreescribe vars ya presentes. Síntoma de diagnóstico: "Telegram desactivado / correos no salen" pese a `.env` correcto → revisar DÓNDE se carga el `.env`, no las credenciales.
22. **UI — un dropdown/menú anclado a un botón DENTRO o junto a una tabla debe renderizarse en PORTAL, no con `absolute z-N`.** La tabla tiene `overflow-x-auto` y una columna de acciones `sticky right-0 z-10`; un panel `position:absolute` queda recortado o por debajo del sticky aunque suba el `z-index`. Patrón canónico (ya en `NodesExportMenu`): hook `useKebabMenu` (`VPN/NodeCard/hooks`) + `createPortal(…, document.body)` + `position:fixed` con coords del `getBoundingClientRect()` + `z-[60]`. Los menús altos calculan el espacio real arriba/abajo, abren hacia el lado con más capacidad, fijan `maxHeight` al área visible y permiten scroll interno; el listener global nunca debe cerrar el dropdown por el scroll del propio menú. Aplica a `ColumnPicker`, `NodeColumnPicker` y cualquier menú futuro sobre tablas. **Corolario de botones:** las clases `.btn-*` (`index.css`) aportan SOLO color/forma — el padding y `inline-flex items-center gap` van en el call-site; un `className="btn-success"` pelado hace que el texto desborde la píldora.

23. **El mapeo `user_mgmt_ips` (user→IP de gestión) es el gate de `tunnel/activate`; su ausencia = `409 NO_MGMT_IP`.** Lo escribe `mgmtIpRepo.upsert` y su columna `source` es un **ENUM** — cualquier valor nuevo de `source` que use el código DEBE existir en el enum (`schema_multiuser.sql` + migración), o el upsert lanza "Data truncated" y, como va envuelto en try/catch best-effort, **falla en silencio y el mapeo no se crea** (esto causó un 409 universal: `'auto-provision'` no estaba en el enum). Valores válidos hoy: `('member_wg','mgmt_peer','manual','auto-provision','auto-heal')`. Defensa en runtime: `activateTunnel` **auto-cura** si falta el mapeo, resolviendo la IP del peer VIVO del usuario server-side (`lib/mgmtIpResolver.js`: admin→plano ADMIN; OWNER/MEMBER→su `member_wireguard.public_key` en CLIENTES) y persistiéndola; con 0 candidatos da 409 accionable, con >1 pide elegir. Migración de reparación + backfill: `migrate:mgmtipsource`.
24. **La etiqueta de rol en la UI sale de `roleLabel(session)` (RBAC), NUNCA de `credentials.role`.** `credentials.role` arrastra el string legacy `'admin'` que `mapRbacRole` da también al OWNER → mostraba "Admin" para un moderador. Fuente única: `utils/permissions.roleLabel` (platform_admin→Administrador · OWNER→Moderador · MEMBER→View).
25. **Filas referenciando un workspace inexistente = data corrupta heredada (FK sin CASCADE + import sin FK checks).** Las FK a `workspaces` son RESTRICT y el borrado de workspace limpia hijos en orden (`admin.routes.js`), así que un huérfano (ej. `member_wireguard`/`mgmt_peer_owners` con `workspace_id` muerto, típico con IPs `192.168.21.x` del plano viejo) solo aparece por importaciones con `FOREIGN_KEY_CHECKS=0`. Limpieza: `npm run purge:rbac` (detecta workspaces fantasma) o `DELETE … LEFT JOIN workspaces … WHERE w.id IS NULL`. Bloquea backfills con FK 1452 si no se filtra.

26. **RouterOS PERMITE rutas duplicadas → `/ip/route/add` NUNCA por `writeIdempotent` pelado; usar `addRouteOnce` (verificar antes).** `writeIdempotent` se apoya en el error "already have", pero `/ip/route/add` NO lo lanza (lo trata como ECMP) → cada re-provisión ACUMULA duplicados. `provision.routes.addRouteOnce` lee `?dst-address`+`?routing-table` y solo añade si no existe (misma disciplina que `tunnel-repair`). **Set canónico por nodo = 5 rutas en el VRF:** 1 ida (`<LAN> → iface@VRF`, dist 1) + 4 retorno (`10.13/10.14/10.12.250.0/24` + scan `10.11.252.0/24`, **`distance=2`** — regla de arquitectura para retornos WG a gestión). En **SSTP NO** se crea la `/32` del mgmt-ip del nodo (el PPP la da dinámica vía remote-address); en **WG sí** (no hay PPP). El de-provision borra todas las rutas del `routing-table` del VRF. Distance en VRF aislado es cosmético (no choca con la default de `main`; tablas separadas).
27. **El `wg0` del VPS debe rutear CADA LAN de torre en sus `AllowedIPs` para poder escanearla.** El `wg0` es config MANUAL (no la gestiona la app). El escaneo sale atado al **scan-pool `10.11.252.x`** (no a `10.12.250.60`); para que el probe ENTRE al túnel, `wg0` necesita la LAN de la torre (ej. `10.1.1.0/24`) en los `AllowedIPs` del `[Peer]` (si no, sale por `eth0`/internet y se pierde → escaneo 0). Diagnóstico: `ip route get <ip-LAN>` debe dar `dev wg0`. Pendiente: script que sincronice `AllowedIPs` desde `LIST-NET-REMOTE-TOWERS`. El scan-pool de `wg0` se pre-crea con `seq 2 50` (rango = `SCAN_IP_POOL_END`), no las 253.
28. **El escaneo detecta SOLO equipos Ubiquiti airOS** (`probeUbiquiti`: status.cgi 80/443 + SSH banner 22, atado a la scan-IP). Los no-Ubiquiti (routers, etc.) NO aparecen — feature "Otros" pendiente. El **retorno del scan SSTP lo resuelve el `masquerade out-interface-list=LIST-VPN-SSTP`** del Core (reescribe el origen a la PPP-local `10.11.251.1` → el CPE no necesita ruta del scan-pool; conntrack des-NATea). La mangle `SCAN-WS-<ws>` marca `src=scan-IP AND dst-address-list=LIST-NET-REMOTE-TOWERS` → por eso la LAN DEBE estar en esa address-list (la añade `addTowerEntries` al provisionar).

29. **La mangle de ESCANEO (`SCAN-WS-<ws>`) vive y muere con el TÚNEL del moderador, NO con un timer; y el scan-lock debe ser anti-fuga.** (a) **Ciclo de vida:** la mangle `src=scan-IP → VRF` se crea/re-apunta al **activar** el túnel y se borra al **desactivar/expirar** (`lib/scanMangleSync.onTunnelActivated/onTunnelClosed`, disparadas por `tunnelService` y `expirationJob`; best-effort, `tryAcquire`, no-op sin scan-IP). El escaneo interactivo sigue haciendo `setup` idempotente como red de seguridad. Válido porque el sistema fuerza **1 sesión ACTIVE por usuario** (`sessionRepo.createSession`) + **1 moderador/ws** → la scan-IP solo apunta a 1 VRF a la vez. Entre workspaces son independientes (cada uno su scan-IP del pool `10.11.252.0/24`) → escaneo en paralelo intacto. (b) **Lock anti-fuga:** `scan.routes` DEBE registrar el cierre del cliente (`req.on('close')`/flag `clientGone`) **ANTES** de `await scanLock.acquire*`, o una petición que muere esperando el lock (nginx corta a ~60s → 504) lo deja tomado 5–7 min y los reintentos se apilan (síntoma: "hay que reiniciar el backend"). Usar `scanLock.acquireOrNull(key, waitMs, maxMs)` (espera ACOTADA, cancelable sin filtrar la cola) → responder **409 `SCAN_BUSY`** si el lock está ocupado, nunca bloquear hasta el 504. NO tocar el job de Monitor AP (`apPollJob` se auto-gestiona bajo el mismo lock con `tryAcquire`).

30. **La mangle de escaneo (`SCAN-WS`) es propiedad EXCLUSIVA del túnel; Monitor AP exige túnel activo; las credenciales SSH viven en el backend.** (a) **Ciclo de vida de la mangle:** la crea **SOLO** `scanMangleSync.onTunnelActivated` (al activar el túnel del nodo) y la destruye **SOLO** `onTunnelClosed` (deactivate/expiración). **NINGÚN otro sitio la toca** — ni `scan-stream`, ni `poll-direct`, ni `apPollJob` (que ANTES la montaba/desmontaba cada tick de 60s → la "tumbaba sola luego de un minuto"). Como hay 1 túnel activo por usuario, hay 1 mangle de escaneo a la vez, amarradas. `scanMangleSync` NO usa lock (es el único gestor → sin contención). (b) **Monitor AP solo LEE, atando el SSH a la scan-IP** (la mangle ya está viva si el túnel está activo). Toda operación SSH de Monitor AP (`poll-direct`, `cpe/ap-detail-direct`, `enrich-batch`, `apPollJob`) pasa por `resolveTunnelBinding(req, vrf)`: sin scan-IP (dev local) → SSH directo; con scan-IP (VPS) y **túnel del nodo NO activo** (no hay sesión viva de ese VRF) → **409 `TUNNEL_NOT_ACTIVE`** (el `apPollJob`, de fondo, simplemente salta el AP). El frontend muestra `TunnelInactiveModal` ("Activá el túnel" → `setActiveModule('nodes')`). NUNCA montar la mangle desde Monitor AP. (c) **La clave SSH validada durante el escaneo sólo vive temporalmente en memoria del tab y DEBE llegar al backend (`aps.clave_ssh_enc`) al guardar**; nunca persistirla en IndexedDB/sessionStorage/localStorage. Monitor AP/`apPollJob` leen exclusivamente `aps`. Revelar la clave: `POST /api/ap-monitor/reveal-ssh` (descifra server-side, `ownsApUuid`).

31. **Todo runner de `.sql` DEBE partir por `split(/\r?\n/)`, NUNCA `split('\n')`, antes de quitar comentarios.** Los `.sql` del repo se leen, se les quitan los comentarios `--` con `/--.*$/` y se parten por `;`. Con checkout **CRLF** (Windows) el `\r` queda al final de cada línea y `.` del regex NO lo cruza → el comentario **sobrevive**, y un `;` dentro de un comentario inline parte el `CREATE TABLE` a la mitad (`ER_PARSE_ERROR near ''`). Afecta a BD fresca en Windows. `db.service.js` (`b964309`) ya lo tenía; `initRbac.js`/`initMultiuser.js` (`df0fde6`) se alinearon. Cualquier runner/migración nueva que parsee SQL sigue el mismo patrón. **Corolario:** los índices de perf viven en `schema_perf_indexes.sql` (aplicados por `migrate:perf`, idempotente por `information_schema`); si añades un índice al hot path, ponlo ahí **y** en el `CREATE TABLE` del schema base (para BD fresca).

32. **La ÚNICA rama que se trabaja y despliega en el VPS `134.199.212.232` (`joinpoint.cloud`) es `vps_uni`.** (Decisión vigente desde 2026-08-19.) `vps_uni` es la **fuente de verdad para los despliegues de este VPS**. En el servidor: `git fetch origin && git reset --hard origin/vps_uni` (NUNCA `git pull`, §4.8; los `.env`/secretos del VPS persisten al reset). **NUNCA desplegar `vps_prod`, `dev` ni `main` directamente en este VPS.** `vps_prod` corresponde a otro VPS y mantiene un ciclo de trabajo separado. Este workspace `C:\Users\i201720174\Desktop\vps_uni` es la copia local para preparar y verificar sus despliegues. Cualquier instrucción histórica que asocie `134.199.212.232` con `vps_prod` queda supersedida por esta regla.

33. **Sesión web y autorización sensible: cookie-only + CSRF + fail closed.** `vpn_session` HttpOnly es el único transporte admitido: nunca reintroducir Bearer, tokens en query, `/auth/refresh`, JWT en storage del navegador ni respuestas de login con `token`. Toda mutación autenticada pasa Origin + double-submit `vpn_csrf`/`X-CSRF-Token` ligado al `jti`. Cada request valida `auth_sessions`, usuario, workspace y membresía en MySQL; una caída devuelve `503 AUTH_STATE_UNAVAILABLE`. Rotación/renovación usa nuevo `jti`; cambios sensibles y suspensión revocan server-side. La firma usa keyring active/previous con `kid`, no una única clave irrotatable. Toda ruta que revele credenciales/scripts/detalle/historial de nodo valida `workspace_id`; un MEMBER además requiere asignación al nodo.

34. **UI asincrona: no confundir error con vacio.** Toda consulta que reemplaza una vista implementa `loading` con skeleton, `error` visible con `Reintentar`, `empty` y exito. Una mutacion usa `idle/submitting/error/success` (no se inventa `empty`). Polling conserva el ultimo dato y muestra error no bloqueante. Errores inesperados se reportan por `reportFrontendError`; nunca incluir bodies, secretos ni storage. `POST /api/error-reports` solo envia a `ERROR_REPORT_EMAIL`/`SMTP_USER`, valida/redacta/deduplica/limita y el cliente nunca elige destinatario.

35. **Metricas de clientes no incluyen al platform admin.** Dashboard global y listados administrativos deben aplicar el mismo universo: usuarios vivos, workspaces vivos y `users.is_platform_admin = 0`. El workspace bootstrap interno del administrador sostiene su contexto de sesion, pero no representa un cliente, moderador, miembro ni workspace comercial y nunca se cuenta como tal.

36. **Correo de recuperacion y reportes: identidad verificada + SMTP server-side.** El email de `users` es login y destino de recuperacion; cambiarlo exige OTP al nuevo buzon y contrasena actual. El destinatario tecnico se guarda validado en `app_settings.error_report_email`, con prioridad sobre `ERROR_REPORT_EMAIL` y fallback final `SMTP_USER`. El endpoint de reportes resuelve el destino server-side en cada evento; el cliente nunca lo envia. `SMTP_HOST/USER/PASS/FROM` permanecen exclusivamente en env y no se exponen ni editan desde la UI.

37. **SSE de tunel: same-origin, una conexion por sesion y reconexion nativa.** Sin `VITE_API_URL` explicito, todo cliente usa rutas `/api` relativas; Vite en desarrollo y Nginx en produccion hacen proxy al backend. Nunca hardcodear `localhost` porque un frontend abierto por `127.0.0.1` convierte cookies SameSite y EventSource en trafico cross-site. `VpnProvider` vive por encima de la navegacion y no debe recrear `/api/tunnel/events`. El stream envia `X-Accel-Buffering: no`, heartbeat cada 25 s y `retry: 3000`; su cleanup es idempotente ante `req.close`, `req.aborted`, `res.close` y `res.error`. El frontend deja que `EventSource` reconecte automaticamente y no aplica `/tunnel/status` tras logout/unmount. Un reset aislado durante restart/redeploy describe un socket cortado despues del handshake; resets repetidos exigen revisar origen/cookie/proxy.

38. **Un Core existente nunca se aprovisiona para seleccionarlo.** `Preparar desde cero` es exclusivamente para un MikroTik limpio: la WAN sólo identifica la interfaz de salida usada por las reglas de seguridad (se detecta desde la ruta default y no configura al ISP) y la clave pública WG del VPS crea el peer de gestión; nunca se solicita ni almacena la clave privada del VPS. Cambiar o seleccionar un servidor existente debe ser una operación sin escrituras RouterOS. El futuro selector por perfiles debe iniciar en `Sólo observación`, desactivar por defecto respaldos/jobs mutantes en perfiles de laboratorio y exigir modo `Operativo` explícito antes de permitir altas, bajas o cambios. Volver al perfil principal no debe alterar sus peers, VRF, PPP secrets ni firewall.

39. **Publicación directa a GitHub: usar la autenticación HTTPS ya configurada en `git`.** Este workspace publica para `joinpoint.cloud` con `git push origin vps_uni`; para un push directo **no se debe exigir ni instalar GitHub CLI (`gh`)**. `gh` sólo es necesario si la tarea requiere funciones adicionales como crear o administrar un pull request. Antes de publicar, confirmar `git remote -v`, rama `vps_uni`, ausencia de secretos y que el diff corresponda al alcance; después verificar que `git rev-parse HEAD` coincida con `git ls-remote --heads origin vps_uni`.

40. **Análisis general Gemini/AirOS = preselección local de STA, nunca red completa cruda.** AP y roles desconocidos se excluyen; un STA es candidato sólo con score `>=40`, se ordena de peor a mejor y se envían como máximo 10, sin rellenar cupos con sanos. Bandas de señal: `>-55` excelente, `-55..-60` observación, `-61..-67` deficiente, `-68..-74` mala, `<=-75` crítica; CCQ: `90..100` excelente, `75..89` aceptable, `50..74` deficiente, `30..49` mala, `<=29` crítica; SNR: `>=30` excelente, `25..29` aceptable, `18..24` deficiente, `10..17` mala, `<10` crítica. Una métrica crítica fuerza nivel crítico; dos condiciones deficientes o peores fuerzan malo y tres fuerzan crítico. El operador debe ver y poder ajustar la lista antes de confirmar. Gemini recibe sólo aliases y métricas compactas; IP/MAC/nombre se reincorporan localmente al informe/PDF y nunca se envían ni persisten. Con cero candidatos, responder sin invocar ni consumir Gemini.

41. **Firebase autentica; MySQL autoriza y el piloto Google nace apagado.** `FEDERATED_AUTH_ENABLED=false` y `VITE_FEDERATED_AUTH_ENABLED=false` son obligatorios hasta staging/canary aprobado. El flujo normal es autoservicio: sesión local + contraseña actual → popup Google → ID token reciente/revocable con `sign_in_provider=google.com`, correo verificado e idéntico → mapping único en `auth_identities`; el UID nunca se pide al usuario ni va al `.env`. No auto-crear usuario/workspace/rol ni aceptar claims como RBAC. La sesión final siempre es `vpn_session` local respaldada por `auth_sessions`. El SDK se carga dinámicamente, usa persistencia sólo en memoria y hace `signOut` inmediato; nunca guardar tokens Firebase en storage. Desvincular conserva el mapping con `disabled_at` y revoca refresh tokens Firebase; el rollback operativo también revoca sesiones locales. Conservar `password_hash` y login local. Credenciales Google sólo por ADC fuera del repo/imagen, WIF preferido; nunca JSON en Git. No importar contraseñas ni hashes Argon2 a Firebase.

42. **Git y autorización explícita son obligatorios ANTES de cualquier cambio en el VPS de producción.** Todo cambio de código o configuración versionada para `134.199.212.232` debe seguir este orden: validar localmente → crear commit en `vps_uni` → publicar en `origin/vps_uni` → comprobar que el SHA remoto coincide → presentar al propietario el commit, archivos/cambios, servicios afectados, migraciones o posible indisponibilidad y plan de retorno → solicitar autorización → recién después de recibir un “sí” explícito desplegar ese SHA en el VPS. La autorización no se presume por haber solicitado el desarrollo ni por aprobar el commit. Está prohibido editar código manualmente en el VPS, desplegar cambios sin commit y push previos, o ejecutar el despliegue mientras la autorización esté pendiente. Si aparece una regresión, preservar el historial con `git revert`, publicar el commit de reversión y volver a solicitar autorización antes de desplegarlo, salvo una orden explícita previa para ejecutar el rollback inmediato. Los `.env`, ADC, llaves y demás secretos permanecen fuera de Git; sus cambios requieren respaldo y procedimiento de retorno independientes.
43. **El estado de un túnel es un lease server-authoritative y cerrar significa revocar primero el router.** El cliente renueva, pero no decide vigencia. Ningún timeout, logout o job puede marcar `CLOSED` antes de confirmar que la mangle `ACCESO-USER-*` fue eliminada; ante fallo RouterOS se conserva `ACTIVE` y se reintenta. La renovación debe ser condicional para no resucitar leases vencidos, y las revocaciones simultáneas del mismo usuario se deduplican sin bloquear a otros usuarios.

43. **Revocar un túnel es una operación confirmada por servidor, nunca una suposición del cliente.** `deactivateAllNodes` sólo puede limpiar `activeNodeVrf` y `tunnelExpiry` después de recibir HTTP exitoso con `success:true` de `/api/tunnel/deactivate`; timeout, HTTP no exitoso, body inválido o `success:false` deben conservar el estado activo y propagar un error visible. La expiración reintenta sin solapar solicitudes. Al recibir una transición confirmada activo→inactivo por acción local, SSE, otra pestaña, expiración o cambio de nodo, la consola elimina mensajes de activación obsoletos; los errores locales permanecen hasta cierre manual o nueva operación.

44. **Guardar un equipo es transaccional respecto del backend.** AirOS puede devolver `null` para datos técnicos ausentes; la frontera `toDevicePersistencePayload` debe convertirlos en campos omitidos y nunca enviar `cachedStats`. `deviceDb.saveSingle` valida `response.ok` y `success`, propaga `status/code/fields` y sólo después actualiza los cachés locales. La UI no agrega `savedIds`, no muestra éxito ni elimina una selección masiva hasta recibir confirmación. Un fallo individual queda reintentable y un lote cuenta únicamente respuestas confirmadas. Los settings globales, incluida `server_public_ip`, permanecen de solo lectura para OWNER/MEMBER y sólo los modifica `platform_admin`.

45. **El nombre y el slug del workspace son inmutables después de crearlo.** El nombre se elige durante la creación directa o invitación del moderador y desde ese momento se presenta sólo como lectura. No reintroducir endpoints, contratos ni controles de UI para renombrarlo; esta estabilidad protege `/dm/<slug>/…`, marcadores, invitaciones y enlaces compartidos. Cualquier excepción futura exige diseñar antes aliases y redirecciones de slugs históricos.

46. **MikroWisp es una integración estrictamente de solo lectura.** Joinpoint y sus bots de Telegram sólo pueden consultar endpoints MikroWisp y nunca invocar operaciones que creen, editen, activen, suspendan, facturen, cobren o modifiquen clientes, servicios o configuración. El adaptador no puede aceptar rutas arbitrarias ni actuar como proxy: sólo expone métodos nominales de una allowlist exacta, protegidos además por validación de destino y pruebas negativas de escritura. El token permanece cifrado y exclusivamente en backend; las respuestas deben aplicar allowlist y excluir credenciales PPP/Hotspot, contraseñas y otros secretos. El `id` de cliente es la identidad externa estable; IDs de servicio, perfil, nodo y futuros catálogos son referencias externas, no nombres. La caché local de catálogos debe ser genérica/extensible y actualizarse sólo por acción manual. Una referencia desconocida nunca hace fallar la ficha: se muestra su valor original como `Pendiente de sincronizar/enlazar` y se sugiere actualizar el catálogo.

Ver también `vpn-manager/CLAUDE.md` y `DESIGN_SYSTEM.md`.

---

## 5) ⚙️ Procesos / funciones vigentes

> Aquí van los **flujos que siguen activos** (no cómo se construyeron). Cuando el usuario pida "enviar al handoff" una regla/función/proceso, va a §4 o §5.

- **Alta de nodo (WG):** backend genera el par de llaves si no se pega una propia, registra la pública en el peer del Core, embebe la privada en el script + rutas de retorno. Columnas `nodes.wg_cpe_public` + `wg_cpe_private_enc` (AES-GCM). Salida: script autocontenido.
- **Alta de nodo (SSTP):** no requiere ruta de retorno (RouterOS la arma con la `remote-address` del PPP). Usuario/contraseña PPP se generan server-side (`ppp-<nombre>-nd<ND>` + pass segura) y se embeben en el script.
- **Invitación de MEMBER (modelo seguro):** el invitado envía **solo su public key**; el server crea el peer, asigna el túnel de la invitación y devuelve `{allowedIp, serverPublicKey, endpoint, allowedIps}` para que arme su `.conf` (la clave privada nunca sale del dispositivo). Provisión WG = best-effort.
- **Recuperación WireGuard self-service:** `POST /api/team/me/wireguard` — el moderador/member (re)genera su propio acceso WG si quedó sin él (la provisión al aceptar es best-effort y puede fallar si el router está caído). UI en **Ajustes → tab WireGuard** (`WireGuardTab.tsx`) con QR + descargar `.conf` + regenerar. Idempotente: limpia el peer anterior al regenerar.
- **Vinculación de Telegram (3 pasos):** al tocar **Vincular** el server genera un código (`/link CODE`, 6 hex, TTL) y la UI muestra `TelegramLinkSteps`: **1)** instalar Telegram (link Play Store) → **2)** copiar el código → **3)** abrir el bot (`https://t.me/<username>`) y pegarlo. El `@username` lo autodetecta el backend (`lib/telegram.getBotUsername()` vía `getMe`, cacheado; override `TELEGRAM_BOT_USERNAME`) y viaja en `GET /account/notifications` como `telegramBotUsername`; si es null, la UI degrada (muestra el código sin enlace directo). El bot interactivo (long-polling) confirma con `/link CODE` (`telegramBot.js`). Local: SMTP por Gmail (`verify()` OK); **en el VPS DO bloquea 25/465/587** → email por puerto/relay alterno (lo gestiona el usuario), y el alta de moderador no depende del correo igualmente.
- **Enlace manual de invitaciones:** `invite-moderator` no falla si el email falla y devuelve `acceptUrl`; `GET /api/admin/invitations` lista pendientes y `POST .../:id/link` regenera el enlace. El admin lo comparte a mano cuando el correo no llega.
- **IP pública WAN:** setting global del admin (`server_public_ip` en Ajustes); `NuevoNodo` la consume en solo-lectura.
- **Escaneo Opción C (VPS):** una scan-IP por workspace (`workspace_scan_ip`), **asignada al crear el workspace**; mangle namespace `SCAN-WS-<ws>`; el SSH/HTTP se ata a la scan-IP (`localAddress`) y la mangle por-origen del router lo enruta al VRF. Serializado por `scanLock` (interactivo `acquireOrNull` con espera acotada → 409 `SCAN_BUSY` si ocupado; Monitor AP `tryAcquire`). La mangle `SCAN-WS` se crea/borra atada al ciclo del túnel (activate/deactivate/expiración, §4.29) — el escaneo solo hace `setup` idempotente de respaldo. Sin scan-IP → escaneo legacy (dev local). Ruta de retorno del scan-pool se crea sola al provisionar nodo (`provision.routes.addScanReturnRoute`); `db/checkScanRoute.js` la verifica/repara.
- **Modo de escaneo `local` (1 equipo) + alerta de IP obsoleta:** `app_settings.scan_mode='local'` hace que TODO el escaneo (interactivo, Monitor AP, auth SSH de antena — los 3 vía `scanIpRepo.resolveForWorkspace`) se ate a `local_scan_ip`, la IP WG de gestión de ESTE equipo. **Esa IP debe ser una interfaz viva del equipo** (la del cliente WireGuard, plano CLIENTES `10.13.250.x` para moderadores); si no, el `bind()` del probe falla en silencio → 0 resultados. Guardas: `lib/wgDetect.js` (detección read-only de IPs WG locales por plano, vía `mgmtNet`); el escaneo **corta con 409 `LOCAL_SCAN_IP_STALE`** (mensaje accionable) si la IP no está viva — **solo en modo local**; `GET /api/settings/scan-local-check` (platform_admin) + banner ámbar en `ScanModeToggle` avisan al admin con la IP WG detectada. El campo se configura en **Ajustes → Modo de escaneo** (`local_scan_ip`); NO se autodetecta/escribe (decisión: solo alertar, no auto-cambiar).
- **Autosync del `wg0` del VPS (event-driven, §4.27):** al provisionar una torre, su LAN debe estar en el `AllowedIPs` del `wg0` del VPS para escanearla. Modelo **HARDENED**: `provision.routes` (backend no-root) escribe la LAN en un archivo de **intención** (`/wg0sync/allowedips.desired`, bind-mount) vía `lib/wg0Sync.appendWg0Intent` — best-effort, `setImmediate`, no-op fuera del VPS. Un **watcher del host** (`systemd .path` → `wg0-autosync.sh` awk, root) añade la LAN al `wg0.conf` SOLO si falta y recarga con `wg syncconf` (sin cortar el túnel). **Guarda de idempotencia** en ambos lados (solo actúa si la LAN es nueva). El backend NO gana `NET_ADMIN`/root ni ve la clave privada. Reconciliación total/manual: `npm run sync:wg0` (CLI `db/syncWg0.js`, dry-run por defecto). Instalación host + verificación: [`deploy/wg0-autosync/README.md`](./deploy/wg0-autosync/README.md). Toggle: `WG0_AUTOSYNC=false`.
- **Auditoría "Actividad reciente" (módulo Equipo):** timeline de acceso a túneles del workspace. **Fuente de verdad = `tunnel_session_logs`** (escrita por `sessionRepo.log` en ACTIVATE/SWITCH/DEACTIVATE/EXPIRE/ERROR); `auditRepo.list`/`listForExport` leen de ahí (NO de la tabla legacy `tunnel_logs`, que nadie llena). **En vivo:** `tunnelService` y `expirationJob` emiten SSE `'tunnel'` → `useWorkspaceEvents` refresca el panel. **Retención rodante de 7 días:** `auditRepo.purgeOlderThan` lo dispara el `expirationJob` (throttle 1×/hora); env `AUDIT_RETENTION_DAYS` (default 7). **UI:** `AuditTimeline` pagina 8/página; export CSV/JSON por rango (`POST /api/audit/export`).
- **Canary Google/Firebase en staging:** seguir `docs/security/FIREBASE_STAGING_CANARY_RUNBOOK.md`. `firebase:preflight` es sólo lectura y exige `FIREBASE_PILOT_ENV=staging`, backend habilitado, esquema completo y `--provider` para declarar el entorno listo. Después se habilita el frontend de staging: el OWNER entra localmente, abre **Perfil y seguridad → Google**, reingresa su contraseña y enlaza la cuenta con el mismo correo. `firebase:canary status|disable` queda como herramienta operativa; las escrituras CLI conservan dry-run salvo `--apply` + frase literal. `disable` conserva auditoría, bloquea el mapping y revoca sesiones locales antes de Firebase.

---

## 6) ⚡ Arranque rápido + runbooks

**Local (dev):**
1. XAMPP **MySQL** arriba.
2. (1ª vez tras pull) `cd server && npm run init:multiuser` (+ `migrate:apnode` opcional).
3. `cd server && npm run dev` → debe imprimir `[ROUTEROS] Parche !empty aplicado...`.
4. `cd vpn-manager && npm run dev` → `http://localhost:5173/GestionVPN-1.0/`.
5. Usar credenciales locales administradas fuera del repositorio y del handoff.
6. 401 de sesión vieja → F12 → Application → *Clear site data* → re-login.
7. Puerto 3001 ocupado por zombie → `Get-NetTCPConnection -LocalPort 3001` → `Stop-Process`.

**Scripts útiles:** `npm run init:rbac | init:multiuser | migrate:* | seed:roles` · `cd server && npm run migrate:auth-sessions && npm run migrate:auth-identities` · `npm run firebase:preflight --prefix server -- --provider` · `npm run firebase:canary --prefix server -- status --email <correo>` · `npm run analyze:queries` · `npm run audit:design` · `npm run audit:semgrep` · `npm run check:security-routes` · `cd server && npm run security:routes | security:hash-benchmark` · `npm run scan:assign <workspaceId>` · `npm run purge:orphans`.
**Diagnóstico (read-only, seguro):** `npm run diagnose` (IPs de gestión/scan, sesiones, salud de cifrado MT_* — sin imprimir secretos) · `npm run check:scanroute [-- --apply]` (verifica/repara la ruta de retorno del scan-pool en cada VRF). Reemplazan los scripts sueltos de depuración ya retirados.

**Runbooks de RED (acciones del usuario en VPS/MikroTik):** [`DESPLIEGUE_VPS.md`](./DESPLIEGUE_VPS.md) · [`MIGRACION_RED_GESTION.md`](./docs/archive/network-migrations/MIGRACION_RED_GESTION.md) · `PLAN_IP_UNIFICADA.md`.

**Despliegue Docker de producción:** usar exclusivamente `docker compose -f docker-compose.prod.yml …`; no combinarlo con `docker-compose.yml`, que representa desarrollo. El backend productivo exige `CORS_ORIGINS` HTTPS y debe quedar `healthy` antes de avanzar al frontend. Validar siempre preflight de `/api/auth/login`, login inválido como `401` y origen hostil como `403`, nunca `500`.

**Acceso administrativo SSH al VPS (endurecido 2026-07-30):** desde este equipo usar `ssh gestionvpn-vps`. El alias apunta a `vpsadmin@134.199.212.232` y a la llave `id_ed25519_gestionvpn_admin`; su ruta y fingerprint están identificados en el `.env` local ignorado. Existe una segunda llave ya probada como fallback. Nunca copiar una llave privada al repositorio, al handoff ni al `.env`. En el servidor sólo se admite `vpsadmin` mediante clave pública; `root`, contraseñas y keyboard-interactive están deshabilitados. Antes de alterar SSH validar una segunda sesión, `sudo -n whoami` y `sshd -t`, y preparar rollback temporizado. Respaldo de configuración: `/root/ssh-hardening-20260730T050400Z/system-config.tar.gz`. Fail2ban/UFW: `sudo fail2ban-client status sshd` y `sudo ufw status numbered`.

**Documentación vigente:** [`README.md`](./README.md), [`ARQUITECTURA.md`](./ARQUITECTURA.md), [`DESPLIEGUE_VPS.md`](./DESPLIEGUE_VPS.md) y [`MANUAL_USUARIO.md`](./MANUAL_USUARIO.md). Los planes e informes superados se conservan en [`docs/archive`](./docs/archive/README.md).

**Credenciales prod:** usuario `admin` (en `vpn_users`); el password NO se documenta aquí (regla: sin secretos en el handoff) — de hecho el password del handoff viejo ya NO es válido en prod (login 401, ver §7: recuperar por rehash). ⚠️ Considerar comprometidas las credenciales que vivieron en el viejo `database.sqlite` / que se expusieron en chat (rotar SMTP de Brevo).

---

## 7) 📌 Pendientes activos

| Prioridad | Tarea |
|---|---|
| 🟢 Integraciones globales/workspace — desplegadas | Runtime `51ca7bb`; Brevo, Telegram, Gemini y Firebase globales importados/activos, tablas y salud verificadas. Pendiente humano opcional: revisión visual autenticada y probar comandos administrativos de Telegram desde la cuenta vinculada. |
| 🔵 MikroWisp + control Telegram — fases 1–5 + migración local | Funcionalidad y SQL aditivo verificados: nueve tablas nuevas, 17 FK e inicialización idempotente. Sin deploy ni prueba Telegram real. Siguiente: aportar/configurar bot, supergrupo foro y usuario piloto de laboratorio; ejecutar canary y sólo después push/despliegue controlado con backup/rollback. No almacenar mensajes, archivos ni conversaciones. Plan: `docs/implementation/PLAN_MIKROWISP_TELEGRAM_HISTORIAL_CLIENTES_2026-08-28.md`. |
| 🟡 Escaneo `/22` — validación humana | Código, VPS y Core corregidos en `cda8ad7`. Activar el túnel Housenet desde la UI y ejecutar un escaneo real; confirmar dispositivos encontrados y contadores de `SCAN-WS`/`GVPN:TOWERS-RETURN`. |
| 🟡 Borrado sitio/equipos — deploy | Implementación local verificada. Antes de desplegar: respaldo, dry-run/preview de sitios productivos para detectar `node_id NULL`, canary con sitio controlado y validación cruzada de otros workspaces. Job durable de reintento queda como hardening posterior. |
| 🔵 Suscripciones — decisiones | Matriz definida: Básico 5 sitios/2 miembros sin escaneo; Intermedio 10/5 con escaneo, guardado e inventario; Avanzado ilimitado bajo uso razonable con monitor, diagnóstico y exportaciones, salvo IA que será piloto. Falta confirmar si miembros excluyen OWNER, revisar precios por el ilimitado y cerrar anticipación, `OVERDUE`, saldos, archivos, identidad y reglas del piloto IA. |
| 🟡 Protección web — autorización de despliegue | Preflight ya está en GO técnico: disco 43%/14 GiB, dump restaurado/verificado, rollback preservado, permisos corregidos y servicios sanos. Pendiente autorización explícita para avanzar checkout, instalar agente/jail, migrar y construir con `docker-compose.prod.yml`, manteniendo `observe`/rollout 0%. |
| ✅ Desplegado 2026-07-31 | Autosync backend-only endurecido en producción `69c368f`: servicio común, CIDR estricto, deduplicación/serialización, sync inmediato, reconciliación periódica y retiro limitado al VRF. Sin UI/CRUD; bajas globales apagadas. |
| 🔴 Google en producción — resolver popup omitido | Firebase y la Web API Key sí corresponden a `vpn-noc`. `initializeAuth()` omitía `browserPopupRedirectResolver`, necesario para `signInWithPopup`; `55816fa` lo corrige y está publicado, sin deploy. Con nueva autorización: respaldar imagen, avanzar el checkout del VPS y reconstruir sólo frontend; después probar popup, enlace/login, suspensión, unicidad y desvinculación. No hay cambios de `.env`, backend, MySQL ni Firebase. |
| 🟡 Validación humana del lease | Desplegado en producción mediante `dab950b`. Pendiente comprobar con usuarios reales: logout, apagado/suspensión >5 min, dos usuarios simultáneos y reintento con RouterOS temporalmente inaccesible. |
| 🔴 Perfiles de servidores VPN | Implementar el tercer escenario sin sobrescribir `MT_*`: tabla/perfiles `Producción` y `Laboratorio`, credenciales cifradas, perfil activo visible, prueba de conexión, cambio/retorno con un clic y respaldo/jobs configurables por perfil. Añadir modos `Sólo observación` (default y sin mutaciones RouterOS) y `Operativo`. Antes de permitir pruebas operativas, aislar inventario y entidades por `core_server_id`; de lo contrario nodos/peers/VRF de prueba se mezclarían con el Core principal en MySQL. |
| 🟡 Validar servidor VPN | La implementación greenfield/health/respaldo dual está local y sin deploy. Probar en RouterOS 7.x de laboratorio: detección WAN, peer WG del VPS, SSTP, firewall, SFTP temporal, par `.backup`+`.rsc`, SMTP real y limpieza en fallos. Nunca ejecutar `Preparar desde cero` sobre un Core existente con peers/VRF/PPP operativos. |
| ✅ Publicado 2026-07-15 | Revocación/expiración de sesiones, health/aprovisionamiento/respaldo del Core, métricas, login/navegación, cuenta/reportes y robustez SSE publicados en `origin/vps_prod` mediante `2e8d07b`. Pendiente: deploy y validación funcional en producción/laboratorio. |
| ✅ Hecho 2026-07-14 | ~~Plan frontend FE-0..FE-7~~ completado en `1976c59`; IndexedDB activo conservado y codigo muerto confirmado retirado. |
| ✅ Publicada 2026-07-14 | Auditoria async implementada en `1453274` y publicada en `origin/vps_prod`. Pendiente operativo: configurar `ERROR_REPORT_EMAIL` en `server/.env.production` (o confirmar fallback `SMTP_USER`), desplegar con rebuild backend+frontend y probar un correo controlado. |
| ✅ Desplegado 2026-07-16 | `vps_prod` con `2e8d07b` quedó desplegado mediante rebuild backend+frontend. Servicios y health técnico correctos; migraciones sin fallos. Pendiente validación funcional de suspensión/logout, renovación, login raíz, SSE, correo técnico y respaldo dual. El aprovisionamiento greenfield sólo se prueba primero en laboratorio. |
| ✅ Hecho (cont. 9) | ~~Redeploy `992ee32`+`fc058b3` en el VPS~~ → **desplegado 2026-06-24** (`up -d --build`): 3 contenedores up, migraciones idempotentes OK (`migrate:mgmtipsource` no-op), backend :3001 + jobs arriba, `/api/health` mysql+smtp ok. Set canónico de rutas verificado en el Core (13 rutas totales, VRF limpio). El `.env.production` y el `wg0` del VPS persisten al `git reset`. |
| ✅ Hecho (cont. 9) | ~~Limpieza manual de rutas duplicadas/huérfanas del Core~~ → **realizada** (borradas las de tablas `*2`/`*4`/unknown + dedup de `10.12.250`/`10.14.250` + mangle `SCAN-WS` residual). El VRF quedó con el set canónico (5 rutas). El código (`addRouteOnce`) evita que se vuelvan a duplicar en altas nuevas. |
| ✅ Desplegado (verificado 2026-07-13) | ~~`a4a4e08`→`3b3ca3e` pendientes de desplegar~~ → **YA DESPLEGADO**: `main` avanzó hasta `aa1864a` (posterior a `3b3ca3e`) y prod corre esa imagen (build 2026-07-05 02:23 UTC, 2 min tras el commit; confirmado por SSH: checkout `aa1864a`, contenedores `healthy`). El índice `idx_tsl_ws_created` (cont.15) va solo en `migratePerf.js` del `entrypoint.sh`. **Solo queda VALIDACIÓN FUNCIONAL en vivo (no confirmada):** cont.13 (Monitor AP con túnel activo trae datos, `SCAN-WS` no se cae al minuto, sin túnel → popup, AP sin nodo → 409 `AP_NO_NODE`) · cont.14 (Auditoría "Actividad reciente" lista/refresca/pagina/purga >7d) · cont.15 (`docker logs` backend `migrate:perf` → índice creado; `/api/health` smtp ok). |
| 🟡 Feature "Otros" | El scanner solo detecta Ubiquiti (§4.28). Añadir sondeo genérico (ICMP/TCP) para listar TODO host vivo y marcar no-airOS como "Otros" (el usuario tiene routers en puerto 8075 que no aparecen). |
| ✅ Hecho y DESPLEGADO (cont. 10) | ~~Script que sincronice el `wg0` del VPS~~ → **autosync event-driven HARDENED desplegado y verificado en vivo (2026-06-24)** (commits `dc614af`+`cfa8de0`, en `main`, 296 tests). Al provisionar, el backend (no-root) escribe la LAN en `/wg0sync/allowedips.desired` (bind-mount); el watcher del host (`systemd .path` + `wg0-autosync.sh` awk, root) la añade al `wg0.conf` solo si falta y recarga con `wg syncconf`. **Verificado**: bind-mount escribible (uid 1001), `.path` `active (waiting)`, prueba sintética OK (apply + guarda no-op), revert limpio. Guarda de idempotencia en ambos lados. CLI manual: `npm run sync:wg0`. Instalación/verificación: `deploy/wg0-autosync/README.md`. |
| 🟢 Una vez tras pull | `cd server && npm run migrate:notifications && npm run migrate:monitoring && npm run migrate:dropcomod` (este último degrada co-mods existentes a MEMBER y estrecha el enum). **Local ya aplicadas (2026-06-21)**: notif/monitoring ya estaban; `dropcomod` corrida ahora (0 filas degradadas, enum→`('OWNER','MEMBER')`). En prod van solas en `entrypoint.sh`. Solo re-aplica en entornos nuevos. |
| 🟡 Prod login | Diagnóstico 2026-07-30: los usuarios, workspaces y membresías existen y están activos; los intentos observados devolvieron 401 `BAD_CREDENTIALS`, no “usuario inexistente”. El backend usa un mensaje genérico deliberado. El administrador legacy inicia con username `admin`; moderadores y miembros usan su correo. No se cambió ninguna contraseña. Si la credencial real sigue fallando, ejecutar el flujo de recuperación o acordar un reset explícito y respaldado; nunca modificar hashes sin autorización. |
| ✅ Hecho (cont. 9) | ~~Prod MT_IP en `192.168.21.1` viejo~~ → **`10.12.250.1` confirmado correcto y alcanzable** (`check:scanroute` conectó `admin@10.12.250.1`; `/ip service api address=` ya incluye los `10.x`). |
| 🟡 Prod peer huérfano | El peer WG del moderador borrado **sigue en el router** (la limpieza es best-effort §12 y falló con el router inalcanzable al borrar). Borrarlo a mano en Winbox: `/interface wireguard peers print` → quitar el del email inexistente. **NUNCA** tocar `LIST-NET-REMOTE-TOWERS` (§13). |
| 🟡 RED en VPS/Router | **`wg0` del VPS ya migrado al plano `10.x` y su persistencia quedó confirmada tras reinicio (2026-07-30):** `wg-quick@wg0` está enabled+active, con handshake reciente, ruta correcta a `10.12.250.1` y ping 0% loss. UFW conserva sólo las reglas previstas y 8080 está cerrado desde fuera. Falta únicamente completar/verificar en el ROUTER el resto del runbook `10.x` si aún aplica (FASE 2b `/ip service ... address=` §4.18 · corte final que elimina `VPN-WG-MGMT`). Correo en prod: DO bloquea 25/465/587 y el usuario usa puerto/relay alterno. |
| ✅ Seguridad — clave WG | Rotación de la clave privada WireGuard confirmada por el usuario el 2026-08-10. |
| 🟡 Seguridad — hardening | Aplicar parches pendientes de sistema/Docker/containerd en ventana; pasar CSP de observación a aplicada tras canary; desactivar `X11Forwarding`/`AllowTcpForwarding` si no se usan; evaluar usuario no-root, `no-new-privileges`, capacidades mínimas y filesystem de solo lectura por contenedor. Informe: `docs/security/SECURITY_AUDIT_2026-08-10.md`. |
| 🟡 Telegram 409 | El bot corre en prod y en el PC del usuario con el mismo token: apagar el local (`TELEGRAM_BOT_ENABLED=false`). | (En el router local ya se aplicó FASE 2b en vivo 2026-06-21. `scan:assign` ya NO es necesario en altas nuevas: la scan-IP se amarra al crear el workspace; úsalo solo para workspaces creados antes de este cambio.) |
| ✅ Hecho (cont. 9) | ~~Escaneo > 0 en vivo~~ → **escaneo OPERATIVO desde el VPS: 31 dispositivos, 20 autenticados** (CPEs airOS de Torre Housenet). Falta probar 2 moderadores en paralelo (aislamiento mangle por-usuario). |
| 🟡 Router | **Limpieza puntual** de peers WG YA huérfanos en el router (del moderador borrado ANTES del fix de cascada) — el código nuevo solo cubre borrados futuros. Revisar en Winbox `/interface wireguard peers print` y borrar los que tengan comment de un email inexistente. |
| 🟡 Router | **Dedup del address-list** `LIST-NET-REMOTE-TOWERS`: pueden quedar entradas duplicadas/inertes (M3 solo evita NUEVAS; el borrado de nodo NO las toca a propósito). Un `:foreach` de limpieza puntual cuando convenga. |
| ✅ Hecho (2026-06-21) | ~~`/team/accept` traga el error de provisión WG en silencio~~ → **resuelto**: `/accept` e in-app devuelven `wgError {code,message}` (contrato `AcceptResponse`); `AcceptInvitationForm` muestra el motivo + botón "Reintentar" (self-service `/me/wireguard`). |
| ✅ Hecho (2026-06-21, cont. 4) | ~~El alta de nodo debe setear `workspace_id` (ND2/ND3 salieron NULL)~~ → **diagnóstico corregido**: el alta SÍ persiste `workspace_id` (`provision.routes` líneas 376/494). El `workspace_id` NULL no venía del alta sino de la **caché del router** (`/nodes` → `saveNode` sin `workspace_id`), que crea una fila paralela keyed por el nombre del secret. La raíz del 404 al borrar era la inconsistencia identidad ppp_user↔VRF (ver §4.20), ya resuelta. |
| 🟡 Feature | Toggle **Local/VPS** para `MT_IP`/endpoint del router (local `10.14.250.1` / VPS `10.12.250.1`), análogo a `ScanModeToggle`. |
| 🟡 Backlog | M2 API pública con tokens scoped · M3 Webhooks · M4 Speed test iperf3 (con confirmación) · L1 Reportes SLA · L2 Diagnóstico con LLM · L3 PWA móvil · L4 Predicción de degradación. |
| ✅ Hecho (cont. 15) | ~~Subir el default `SMTP_VERIFY_TIMEOUT_MS` (~4s→8s)~~ → **hecho `3b3ca3e`** (default 8s en `lib/mailer.js`): el handshake en frío ya no marca falso `error` en `/api/health`. Override por env si hiciera falta. |

---

## 8) 🧭 Cómo mantener este documento

Este doc se mantiene con la skill **`handoff-keeper`** (`.claude/skills/handoff-keeper/SKILL.md`). En resumen:
- **Estado, reglas, funciones y procesos** → se **integran/actualizan en este `HANDOFF.md`** (§0, §4, §5) reemplazando lo viejo.
- **Narrativa de "qué hice esta sesión"** → se **añade** como nueva entrada arriba en [`HANDOFF_LOG.md`](./HANDOFF_LOG.md).
- Cuando el usuario diga *"envía esto al handoff"*: clasificar primero (¿regla/función/proceso durable, o evento de bitácora?) y colocarlo en el archivo correcto.
| 🟡 Canary primer sitio `/22` | Core, VPS y software ya están listos. Crear un sitio/CPE real, confirmar que recibe una sola ruta `10.12.248.0/22`, activar túnel y validar acceso a escaneo/clientes/VPS/admin según permisos. No crear datos ficticios. |
- **Estado de entrega MikroWisp/Telegram:** commit local `e1a2769` en `vps_uni`; todavía sin push ni deploy. Canary bloqueado hasta disponer de bot de workspace, supergrupo foro y usuario piloto de laboratorio.
- **Preparación de canary aclarada en la web (2026-08-28):** se reutiliza el bot existente del workspace; la pantalla `Clientes en Telegram` muestra pasos numerados para crear grupo privado, activar Temas, agregar el bot administrador, conceder temas/invitaciones/restricción y enviar `/vinculargrupo`. El OWNER que vincula queda automáticamente como participante `ACTIVE`, por lo que sirve como piloto sin reinvitarse. El formulario MikroWisp visible solicita URL HTTPS, Token API cifrado e ID real de validación.
- **MikroWisp + Telegram desplegado para depuración (2026-08-28; runtime `9be5e22`):** rama `vps_uni` publicada y checkout productivo actualizado. Frontend/backend reconstruidos exclusivamente con `docker-compose.prod.yml`; backend hotfix añade migración portable según collation real. Producción confirmó **9/9 tablas**, **17 FK**, `utf8mb4_uca1400_ai_ci`, migraciones OK, HTTPS/health `ok`, MySQL/SMTP `ok`, CORS 204/403, ruta protegida 401, bundle con la guía nueva, tres contenedores activos y 0 reinicios. Backup verificado `/root/backups/pre-mikrowisp-telegram-20260828T2152.sql.gz` (184 KB); rollback `gestionvpn-10-{backend|frontend}:pre-mikrowisp-20260828T2152`. Disco quedó alto, 91%/2.3 GB libres, aun después de podar sólo caché/imágenes dangling; no borrar imágenes etiquetadas ni volúmenes sin auditoría. Pendiente humano: configurar MikroWisp desde la web, vincular el supergrupo y ejecutar el canary funcional con el moderador piloto.
- **UI Clientes en Telegram reorganizada (2026-08-28):** la vista prioriza `Mis grupos` como tarjetas visibles; seleccionar una muestra explícitamente `Grupo seleccionado` → `Temas del grupo` → `Participantes conocidos`. `Nuevo tema` vive dentro del grupo y su formulario es plegable. `Agregar nuevo grupo` quedó como sección independiente al final y sólo entonces despliega pasos/código/PDF. Responsive en una/dos columnas, estados legibles, foco semántico y controles táctiles ≥44 px. Pruebas focalizadas **2/2**, build y `check:all` verdes.
