# Dependencias
node_modules/
**/node_modules/

# Build output
dist/
dist-ssr/
*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Variables de entorno
.env
.env.local
.env.*.local

# Claude Code settings locales
.claude/settings.local.json

# Backup SQLite tras migración a MySQL (datos sensibles, no versionar)
server/_legacy_sqlite/

# Secretos de cifrado/firma — NUNCA versionar (clave JWT y clave AES de credenciales)
.jwt_secret
.db_secret
**/.jwt_secret*
**/.db_secret*

# Base de datos SQLite legacy (contenía credenciales cifradas)
**/database.sqlite
**/database.sqlite-shm
**/database.sqlite-wal

# Worktrees temporales de agentes (copias completas del repo con secretos)
.claude/worktrees/
